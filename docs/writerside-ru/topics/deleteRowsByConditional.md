<!-- Generated by scripts/docs/generate-topics.mjs. Edits are overwritten. Source: src/appsscript/sheet/deleteRowsByConditional.ts. -->

# deleteRowsByConditional

<link-summary>Удаляет строки, выбранные предикатом.</link-summary>

<web-summary>deleteRowsByConditional() — Удаляет строки, выбранные предикатом. apps-script-utils, Русский.</web-summary>

<tldr>
<p>Модуль: <code>appsscript/sheet</code> · Доступно с: 1.11.0</p>
</tldr>

```typescript
function deleteRowsByConditional(
  target: GoogleAppsScript.Spreadsheet.Sheet | GoogleAppsScript.Spreadsheet.Range,
  predicate: RowPredicate,
  options: RowConditionalOptions | null | undefined = {}
): number;
```

Каждая строка оценивается по листу в том виде, в каком он был прочитан, а удаление затем идёт снизу вверх идущими подряд блоками. Именно это избавляет от ошибки самописного цикла, который удаляет не те строки, когда позиции начинают сдвигаться.

Строки исчезают насовсем: только очищает их `clearRowsByConditional`.

Лист означает весь его диапазон данных, и удаляются строки целиком. Диапазон означает только его ячейки: они удаляются, ячейки под ними поднимаются, а столбцы рядом с диапазоном остаются на месте. В обоих случаях предикат получает позицию на листе, а отказ опустошить лист касается формы с листом, где строка должна остаться хотя бы одна.

## Параметры

| Параметр                            | Тип                                                                        | Описание                                                                                                                                                                   |
| :---------------------------------- | :------------------------------------------------------------------------- | :------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `target`                            | `GoogleAppsScript.Spreadsheet.Sheet \| GoogleAppsScript.Spreadsheet.Range` | Лист, с которым работаем, или диапазон, внутри которого работаем: читаются только его ячейки, и удаляются только они.                                                      |
| `predicate`                         | `RowPredicate`                                                             | Получает ячейки строки, её позицию от единицы и — если задан `headerRow` — строку с ключами по именам столбцов. Верните `true` для строк, с которыми нужно что-то сделать. |
| `options` _(необязательный)_ `= {}` | `RowConditionalOptions \| null \| undefined`                               | `headerRow` указывает строку с именами столбцов. Если она задана, каждая строка приходит с ключами по этим именам, а сама строка заголовков выбывает из кандидатов.        |

## Возвращает

`number` — сколько их было затронуто.

## Исключения

| Исключение                 | Условие                                                           |
| :------------------------- | :---------------------------------------------------------------- |
| `InvalidSheetException`    | первый аргумент не является ни листом, ни диапазоном.             |
| `IllegalArgumentException` | предикат не функция или позиция заголовка не положительное целое. |

## Примеры

### Применение

```javascript
const sheet = SpreadsheetApp.getActiveSheet();

deleteRowsByConditional(sheet, (values, position, row) => row.status === "done", {
  headerRow: 1
});
```

### Внутри одного блока

```javascript
const sheet = SpreadsheetApp.getActiveSheet();

// Only B2:D100 is read, and only those cells move up.
deleteRowsByConditional(sheet.getRange("B2:D100"), (values) => values.every((cell) => cell === ""));
```

## Смотрите также

- [`clearRowsByConditional`](clearRowsByConditional.md)
- [`deleteColumnsByConditional`](deleteColumnsByConditional.md)
- [Class Sheet](https://developers.google.com/apps-script/reference/spreadsheet/sheet)
- [Class Range](https://developers.google.com/apps-script/reference/spreadsheet/range)
- [](reference-appsscript.md)

## Исходный код

[`src/appsscript/sheet/deleteRowsByConditional.ts`](https://github.com/MaksymStoianov/apps-script-utils/blob/main/src/appsscript/sheet/deleteRowsByConditional.ts)
