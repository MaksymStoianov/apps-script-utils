<!-- Generated by scripts/docs/generate-topics.mjs. Edits are overwritten. Source: src/exception/RuntimeException.ts. -->

# RuntimeException

<link-summary>Родитель всех исключений, которые может вызвать работающий скрипт.</link-summary>

<web-summary>RuntimeException — Родитель всех исключений, которые может вызвать работающий скрипт. apps-script-utils, Русский.</web-summary>

<tldr>
<p>Модуль: <code>exception</code> · Доступно с: 1.0.0</p>
</tldr>

```typescript
class RuntimeException extends Exception
```

Родитель всех исключений, которые может вызвать работающий скрипт. Почти все классы ниже наследуют его, а не `Exception` напрямую.

Любой класс этой иерархии принимает необязательное сообщение, другое исключение, чьё сообщение будет переиспользовано, или вообще ничего. `isException` распознаёт их все; обычный `Error` к ним не относится.

## Примеры

### Бросить и поймать

```javascript
throw new RuntimeException("something specific about this call");

try {
  doWork();
} catch (error) {
  if (error instanceof RuntimeException) {
    // handled
  }
}
```

## Смотрите также

- [`isException`](isException.md)
- [`requireException`](requireException.md)
- [](reference-exception.md)

## Исходный код

[`src/exception/RuntimeException.ts`](https://github.com/MaksymStoianov/apps-script-utils/blob/main/src/exception/RuntimeException.ts)
