<!-- Generated by scripts/docs/generate-topics.mjs. Edits are overwritten. Source: src/exception/InvalidEmailFormatException.ts. -->

# InvalidEmailFormatException

<link-summary>От строки ожидался адрес электронной почты, а её форма ему не соответствует.</link-summary>

<web-summary>InvalidEmailFormatException — От строки ожидался адрес электронной почты, а её форма ему не соответствует. apps-script-utils, Русский.</web-summary>

<tldr>
<p>Модуль: <code>exception</code> · Доступно с: 1.0.0</p>
</tldr>

```typescript
class InvalidEmailFormatException extends RuntimeException
```

От строки ожидался адрес электронной почты, а её форма ему не соответствует.

Бросается функциями [`requireValidEmail`](requireValidEmail.md).

Любой класс этой иерархии принимает необязательное сообщение, другое исключение, чьё сообщение будет переиспользовано, или вообще ничего. `isException` распознаёт их все; обычный `Error` к ним не относится.

## Примеры

### Бросить и поймать

```javascript
throw new InvalidEmailFormatException("something specific about this call");

try {
  doWork();
} catch (error) {
  if (error instanceof InvalidEmailFormatException) {
    // handled
  }
}
```

## Смотрите также

- [`isException`](isException.md)
- [`requireException`](requireException.md)
- [](reference-exception.md)

## Исходный код

[`src/exception/InvalidEmailFormatException.ts`](https://github.com/MaksymStoianov/apps-script-utils/blob/main/src/exception/InvalidEmailFormatException.ts)
