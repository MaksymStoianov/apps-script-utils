<!-- Generated by scripts/docs/generate-topics.mjs. Edits are overwritten. Source: src/appsscript/sheet/removeSchema.ts. -->

# removeSchema

<link-summary>Удаляет схему, сохранённую на листе.</link-summary>

<web-summary>removeSchema() — Удаляет схему, сохранённую на листе. apps-script-utils, Русский.</web-summary>

<tldr>
<p>Модуль: <code>appsscript/sheet</code> · Доступно с: 1.11.0</p>
</tldr>

```typescript
function removeSchema(
  sheet: GoogleAppsScript.Spreadsheet.Sheet,
  options: RemoveSchemaOptions | null | undefined = {}
): boolean;
```

Сохранённое описание удаляется; данные и строка заголовков остаются в точности как были. Лист без сохранённой схемы не трогается, и вызов возвращает `false`.

`validation: true` дополнительно снимает проверку данных со столбцов, которые описывала схема. По умолчанию выключено намеренно: Apps Script не позволяет отличить правило, созданное схемой, от добавленного человеком, поэтому снимаются оба.

## Параметры

| Параметр                            | Тип                                        | Описание                                                                  |
| :---------------------------------- | :----------------------------------------- | :------------------------------------------------------------------------ |
| `sheet`                             | `GoogleAppsScript.Spreadsheet.Sheet`       | Лист, с которым работаем.                                                 |
| `options` _(необязательный)_ `= {}` | `RemoveSchemaOptions \| null \| undefined` | `validation` дополнительно снимает проверку данных, следовавшую из схемы. |

## Возвращает

`boolean` — `true`, если схема была удалена.

## Исключения

| Исключение              | Условие                             |
| :---------------------- | :---------------------------------- |
| `InvalidSheetException` | первый аргумент не является листом. |

## Примеры

### Удаление схемы

```javascript
const sheet = SpreadsheetApp.getActiveSheet();

removeSchema(sheet); // => true when one was there
removeSchema(sheet, { validation: true }); // also clears the rules it implied
```

## Смотрите также

- [`getSchema`](getSchema.md)
- [`insertSchema`](insertSchema.md)
- [](reference-appsscript.md)

## Исходный код

[`src/appsscript/sheet/removeSchema.ts`](https://github.com/MaksymStoianov/apps-script-utils/blob/main/src/appsscript/sheet/removeSchema.ts)
