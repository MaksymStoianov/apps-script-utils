<!-- Generated by scripts/docs/generate-topics.mjs. Edits are overwritten. Source: src/net/path/types/ParsedPath.ts. -->

# ParsedPath

<link-summary>Части, на которые распадается путь.</link-summary>

<web-summary>ParsedPath — Части, на которые распадается путь. apps-script-utils, Русский.</web-summary>

<tldr>
<p>Модуль: <code>net/path/types</code> · Доступно с: 0.1.0</p>
</tldr>

```typescript
type ParsedPath =
```

`root` — ведущий разделитель или схема, `dir` — всё до последнего разделителя, `base` — имя файла с расширением, `name` — оно же без расширения, `ext` — расширение вместе с точкой.

Все части необязательны, и та, которой в пути нет, просто отсутствует в объекте, а не приходит пустой строкой: голое имя файла даёт только `base`, `name` и `ext`. Это форма, которую возвращает `path.parse` в Node, — благодаря ей код, работающий в обеих средах, не нужно переписывать.

## Примеры

### Форма

```javascript
parse("/a/b/c.txt");
// => { root: "/", dir: "/a/b", base: "c.txt", name: "c", ext: ".txt" }

parse("readme.md");
// => { base: "readme.md", name: "readme", ext: ".md" }
```

## Смотрите также

- [`parse`](parse.md)
- [`join`](join.md)
- [`normalize`](normalize.md)
- [](reference-path.md)

## Исходный код

[`src/net/path/types/ParsedPath.ts`](https://github.com/MaksymStoianov/apps-script-utils/blob/main/src/net/path/types/ParsedPath.ts)
