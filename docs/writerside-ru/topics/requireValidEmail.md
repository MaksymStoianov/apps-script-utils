<!-- Generated by scripts/docs/generate-topics.mjs. Edits are overwritten. Source: src/lang/string/requireValidEmail.ts. -->

# requireValidEmail

<link-summary>Возвращает значение как адрес электронной почты или выбрасывает исключение.</link-summary>

<web-summary>requireValidEmail() — Возвращает значение как адрес электронной почты или выбрасывает исключение. apps-script-utils, Русский.</web-summary>

<tldr>
<p>Модуль: <code>lang/string</code> · Доступно с: 1.0.0</p>
</tldr>

```typescript
function requireValidEmail(email: string | null | undefined, message?: string): string;
```

Адрес разбивается по единственному `@`: домен проверяется так же, как в `isValidDomain`, а локальная часть должна быть непустой и без пробелов. Это проверка формата, а не доказательство того, что ящик существует.

`isX` и `nonX` отвечают на вопрос и никогда не прерывают выполнение; `requireX` и `requireNonX` возвращают значение или выбрасывают исключение. Схема разобрана в [](validation-conventions.md).

## Параметры

| Параметр                     | Тип                           | Описание                                                                          |
| :--------------------------- | :---------------------------- | :-------------------------------------------------------------------------------- |
| `email`                      | `string \| null \| undefined` | The string value representing an email to validate. Can be `null` or `undefined`. |
| `message` _(необязательный)_ | `string`                      | Сообщение исключения. Если не задано, используется сообщение по умолчанию.        |

## Возвращает

`string` — то же значение, уже проверенное как адрес электронной почты.

## Исключения

| Исключение                    | Условие                                         |
| :---------------------------- | :---------------------------------------------- |
| `InvalidEmailFormatException` | значение не является адресом электронной почты. |

## Примеры

### Что проходит, а что нет

```javascript
requireValidEmail("user@example.com"); // => "user@example.com"
requireValidEmail("first.last@sub.example.co.uk"); // => "first.last@sub.example.co.uk"
requireValidEmail("user@@example.com"); // throws InvalidEmailFormatException
requireValidEmail("user@"); // throws InvalidEmailFormatException
```

## Смотрите также

- [`isEmail`](isEmail.md)
- [`nonValidEmail`](nonValidEmail.md)
- [](reference-base.md)

## Исходный код

[`src/lang/string/requireValidEmail.ts`](https://github.com/MaksymStoianov/apps-script-utils/blob/main/src/lang/string/requireValidEmail.ts)
