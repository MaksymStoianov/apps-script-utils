<!-- Generated by scripts/docs/generate-topics.mjs. Edits are overwritten. Source: src/net/path/join.ts. -->

# join

<link-summary>Склеивает сегменты пути, оставляя между ними один разделитель.</link-summary>

<web-summary>join() — Склеивает сегменты пути, оставляя между ними один разделитель. apps-script-utils, Русский.</web-summary>

<tldr>
<p>Модуль: <code>net/path</code> · Доступно с: 1.0.0</p>
</tldr>

```typescript
function join(...paths: string[]): string;
```

Пустые сегменты отбрасываются, сдвоенные разделители схлопываются в один, а результат затем нормализуется, поэтому `..` в середине пути разрешается, а не тащится дальше.

Это работа со строками, а не с файловой системой: существование ничего не проверяется.

## Параметры

| Параметр   | Тип        | Описание                         |
| :--------- | :--------- | :------------------------------- |
| `...paths` | `string[]` | Сегменты, которые нужно склеить. |

## Возвращает

`string` — склеенный и нормализованный путь.

## Примеры

### Склеивание

```javascript
join("a", "b/c"); // => "a/b/c"
join("a", "", "b"); // => "a/b"
join("/a", "../b"); // => "/b"
```

## Смотрите также

- [`normalize`](normalize.md)
- [`parse`](parse.md)
- [`isAbsolute`](isAbsolute.md)
- [](reference-path.md)

## Исходный код

[`src/net/path/join.ts`](https://github.com/MaksymStoianov/apps-script-utils/blob/main/src/net/path/join.ts)
