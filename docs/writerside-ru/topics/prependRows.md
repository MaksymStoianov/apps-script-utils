<!-- Generated by scripts/docs/generate-topics.mjs. Edits are overwritten. Source: src/appsscript/sheet/prependRows.ts. -->

# prependRows

<link-summary>Вставляет строки перед данными — на листе или у диапазона.</link-summary>

<web-summary>prependRows() — Вставляет строки перед данными — на листе или у диапазона. apps-script-utils, Русский.</web-summary>

<tldr>
<p>Модуль: <code>appsscript/sheet</code> · Доступно с: 1.0.0</p>
</tldr>

```typescript
function prependRows(
  target: GoogleAppsScript.Spreadsheet.Sheet | GoogleAppsScript.Spreadsheet.Range,
  values: unknown,
  options: PrependRowsOptions | null | undefined = {}
): GoogleAppsScript.Spreadsheet.Sheet;
```

Сначала освобождается место, затем в него пишутся значения, поэтому ничего из того, что уже на листе, не перезаписывается. Передан лист — строки идут наверх; передан диапазон — над его первой строкой, а значения пишутся по его столбцам.

Ячейка, текст которой начинается с `=`, записывается как формула, а не как текст — по тому же правилу, что и в редакторе.

## Параметры

| Параметр                            | Тип                                                                        | Описание                                                                                                                                 |
| :---------------------------------- | :------------------------------------------------------------------------- | :--------------------------------------------------------------------------------------------------------------------------------------- |
| `target`                            | `GoogleAppsScript.Spreadsheet.Sheet \| GoogleAppsScript.Spreadsheet.Range` | Лист, в который вставляем, или диапазон, у которого вставляем.                                                                           |
| `values`                            | `unknown`                                                                  | Матрица строк: по массиву на строку, все одной длины.                                                                                    |
| `options` _(необязательный)_ `= {}` | `PrependRowsOptions \| null \| undefined`                                  | `afterFrozenRows` вставляет ниже закреплённых строк, а не в самый верх. Диапазон задаёт позицию сам, поэтому там опция ничего не меняет. |

## Возвращает

`GoogleAppsScript.Spreadsheet.Sheet` — лист, чтобы вызовы можно было объединять в цепочку.

## Исключения

| Исключение                 | Условие                                                     |
| :------------------------- | :---------------------------------------------------------- |
| `IllegalArgumentException` | не передан ни один аргумент.                                |
| `InvalidSheetException`    | первый аргумент не является ни листом, ни диапазоном.       |
| `TypeError`                | значения не являются матрицей со строками одинаковой длины. |

## Примеры

### Вставка наверх листа

```javascript
const sheet = SpreadsheetApp.getActiveSheet();

prependRows(sheet, [
  ["Name", "Email"],
  ["Ada", "ada@example.com"]
]);
```

### Вставка у диапазона

```javascript
const sheet = SpreadsheetApp.getActiveSheet();

prependRows(sheet.getRange("B4:C13"), [["Name", "Email"]]);
```

## Смотрите также

- [`prependRow`](prependRow.md)
- [`appendRows`](appendRows.md)
- [](reference-appsscript.md)

## Исходный код

[`src/appsscript/sheet/prependRows.ts`](https://github.com/MaksymStoianov/apps-script-utils/blob/main/src/appsscript/sheet/prependRows.ts)
