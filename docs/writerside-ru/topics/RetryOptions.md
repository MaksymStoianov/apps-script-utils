<!-- Generated by scripts/docs/generate-topics.mjs. Edits are overwritten. Source: src/lang/function/retry.ts. -->

# RetryOptions

<link-summary>Как `retry` ждёт и когда сдаётся.</link-summary>

<web-summary>RetryOptions — Как `retry` ждёт и когда сдаётся. apps-script-utils, Русский.</web-summary>

<tldr>
<p>Модуль: <code>lang/function</code></p>
</tldr>

```typescript
interface RetryOptions
```

`attempts` ограничивает число попыток, `delay` — пауза перед второй, `multiplier` увеличивает каждую следующую, `maxDelay` ограничивает одну паузу.

`shouldRetry` получает ошибку и номер неудавшейся попытки и решает, стоит ли вообще повторять; `sleep` выполняет ожидание и по умолчанию берёт `Utilities.sleep`, где тот есть, — это и позволяет тесту не ждать.

## Примеры

### Форма

```javascript
const options = {
  attempts: 4,
  delay: 500,
  multiplier: 2,
  maxDelay: 10000
};
```

## Смотрите также

- [`retry`](retry.md)
- [](reference-base.md)

## Исходный код

[`src/lang/function/retry.ts`](https://github.com/MaksymStoianov/apps-script-utils/blob/main/src/lang/function/retry.ts)
