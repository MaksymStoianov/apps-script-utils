<!-- Generated by scripts/docs/generate-topics.mjs. Edits are overwritten. Source: src/exception/appsscript/sheet/InvalidSpreadsheetException.ts. -->

# InvalidSpreadsheetException

<link-summary>Ожидалась таблица, а значение ею не является.</link-summary>

<web-summary>InvalidSpreadsheetException — Ожидалась таблица, а значение ею не является. apps-script-utils, Русский.</web-summary>

<tldr>
<p>Модуль: <code>exception/appsscript/sheet</code> · Доступно с: 1.5.0</p>
</tldr>

```typescript
class InvalidSpreadsheetException extends RuntimeException
```

Ожидалась таблица, а значение ею не является.

Бросается функциями [`requireSpreadsheet`](requireSpreadsheet.md).

Любой класс этой иерархии принимает необязательное сообщение, другое исключение, чьё сообщение будет переиспользовано, или вообще ничего. `isException` распознаёт их все; обычный `Error` к ним не относится.

## Примеры

### Бросить и поймать

```javascript
throw new InvalidSpreadsheetException("something specific about this call");

try {
  doWork();
} catch (error) {
  if (error instanceof InvalidSpreadsheetException) {
    // handled
  }
}
```

## Смотрите также

- [`isException`](isException.md)
- [`requireException`](requireException.md)
- [](reference-exception.md)

## Исходный код

[`src/exception/appsscript/sheet/InvalidSpreadsheetException.ts`](https://github.com/MaksymStoianov/apps-script-utils/blob/main/src/exception/appsscript/sheet/InvalidSpreadsheetException.ts)
