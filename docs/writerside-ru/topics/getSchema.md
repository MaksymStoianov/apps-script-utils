<!-- Generated by scripts/docs/generate-topics.mjs. Edits are overwritten. Source: src/appsscript/sheet/getSchema.ts. -->

# getSchema

<link-summary>Возвращает схему листа — сохранённую или выведенную из его содержимого.</link-summary>

<web-summary>getSchema() — Возвращает схему листа — сохранённую или выведенную из его содержимого. apps-script-utils, Русский.</web-summary>

<tldr>
<p>Модуль: <code>appsscript/sheet</code> · Доступно с: 1.11.0</p>
</tldr>

```typescript
function getSchema(sheet: GoogleAppsScript.Spreadsheet.Sheet): SheetSchema | null;
```

Схема описывает столбцы: их имена, типы и строку, в которой лежат имена. Если `insertSchema` уже сохранил схему на листе, возвращается именно она.

Если ничего не сохранено, лист читается и схема выводится из строки заголовков и значений под ней. В этом случае в результате стоит `inferred: true`, и вызывающий код отличает объявленное описание от угаданного.

## Параметры

| Параметр | Тип                                  | Описание                  |
| :------- | :----------------------------------- | :------------------------ |
| `sheet`  | `GoogleAppsScript.Spreadsheet.Sheet` | Лист, с которым работаем. |

## Возвращает

`SheetSchema | null` — схема или `null`, если на листе нечего описывать.

## Исключения

| Исключение              | Условие                             |
| :---------------------- | :---------------------------------- |
| `InvalidSheetException` | первый аргумент не является листом. |

## Примеры

### Чтение схемы

```javascript
const sheet = SpreadsheetApp.getActiveSheet();

const schema = getSchema(sheet);

schema?.columns.map((column) => column.name);
```

## Смотрите также

- [`insertSchema`](insertSchema.md)
- [`removeSchema`](removeSchema.md)
- [`getValues`](getValues.md)
- [](reference-appsscript.md)

## Исходный код

[`src/appsscript/sheet/getSchema.ts`](https://github.com/MaksymStoianov/apps-script-utils/blob/main/src/appsscript/sheet/getSchema.ts)
