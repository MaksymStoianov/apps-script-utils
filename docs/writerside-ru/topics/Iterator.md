<!-- Generated by scripts/docs/generate-topics.mjs. Edits are overwritten. Source: src/lang/Iterator.ts. -->

# Iterator

<link-summary>Курсор по последовательности, который умеет идти и назад.</link-summary>

<web-summary>Iterator — Курсор по последовательности, который умеет идти и назад. apps-script-utils, Русский.</web-summary>

<tldr>
<p>Модуль: <code>lang</code> · Доступно с: 1.0.0</p>
</tldr>

```typescript
interface Iterator<T>
```

Обязательны только `next` и `hasNext`. Остальное — `previous`, `hasPrevious`, `index`, `moveTo`, `size`, `toStart`, `toEnd` — необязательно, поэтому источник, умеющий идти только вперёд, удовлетворяет интерфейсу, не притворяясь, что умеет больше.

Это форма постраничного чтения: строки, приходящие от сервиса порциями, для потребляющего кода выглядят одной последовательностью.

## Примеры

### Форма

```javascript
while (iterator.hasNext()) {
  const value = iterator.next();
}
```

## Смотрите также

- [`Class`](Class.md)
- [`first`](first.md)
- [`last`](last.md)
- [](reference-abstracts.md)

## Исходный код

[`src/lang/Iterator.ts`](https://github.com/MaksymStoianov/apps-script-utils/blob/main/src/lang/Iterator.ts)
