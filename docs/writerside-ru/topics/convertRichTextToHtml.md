<!-- Generated by scripts/docs/generate-topics.mjs. Edits are overwritten. Source: src/appsscript/sheet/convertRichTextToHtml.ts. -->

# convertRichTextToHtml

<link-summary>Преобразует форматированное значение ячейки в HTML.</link-summary>

<web-summary>convertRichTextToHtml() — Преобразует форматированное значение ячейки в HTML. apps-script-utils, Русский.</web-summary>

<tldr>
<p>Модуль: <code>appsscript/sheet</code> · Доступно с: 1.0.0</p>
</tldr>

```typescript
function convertRichTextToHtml(richText: GoogleAppsScript.Spreadsheet.RichTextValue): string;
```

Каждый фрагмент значения превращается в span со своими стилями — насыщенность, наклон, подчёркивание, зачёркивание, цвет, размер и семейство, — а ссылка становится тегом `a`. Сам текст экранируется, поэтому ячейка с `<b>` выводится как текст, а не как разметка.

## Параметры

| Параметр   | Тип                                          | Описание                                               |
| :--------- | :------------------------------------------- | :----------------------------------------------------- |
| `richText` | `GoogleAppsScript.Spreadsheet.RichTextValue` | Форматированное значение, которое нужно преобразовать. |

## Возвращает

`string` — получившийся HTML.

## Примеры

### Применение

```javascript
const range = SpreadsheetApp.getActiveRange();

const html = convertRichTextToHtml(range.getRichTextValue());
```

## Смотрите также

- [`escapeHtml`](escapeHtml.md)
- [`isRichTextValue`](isRichTextValue.md)
- [`highlightHtml`](highlightHtml.md)
- [](reference-appsscript.md)

## Исходный код

[`src/appsscript/sheet/convertRichTextToHtml.ts`](https://github.com/MaksymStoianov/apps-script-utils/blob/main/src/appsscript/sheet/convertRichTextToHtml.ts)
