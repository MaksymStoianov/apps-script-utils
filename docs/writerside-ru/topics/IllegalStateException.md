<!-- Generated by scripts/docs/generate-topics.mjs. Edits are overwritten. Source: src/exception/IllegalStateException.ts. -->

# IllegalStateException

<link-summary>Вызов пришёл не вовремя: аргументы могут быть совершенно правильными, но объект не в том состоянии, которое позволяет этот вызов.</link-summary>

<web-summary>IllegalStateException — Вызов пришёл не вовремя: аргументы могут быть совершенно правильными, но объект не в том состоянии, которое позволяет этот вызов. apps-script-utils, Русский.</web-summary>

<tldr>
<p>Модуль: <code>exception</code> · Доступно с: 1.11.0</p>
</tldr>

```typescript
class IllegalStateException extends RuntimeException
```

Вызов пришёл не вовремя: аргументы могут быть совершенно правильными, но объект не в том состоянии, которое позволяет этот вызов.

Бросается функциями [`requireState`](requireState.md).

Любой класс этой иерархии принимает необязательное сообщение, другое исключение, чьё сообщение будет переиспользовано, или вообще ничего. `isException` распознаёт их все; обычный `Error` к ним не относится.

## Примеры

### Бросить и поймать

```javascript
throw new IllegalStateException("something specific about this call");

try {
  doWork();
} catch (error) {
  if (error instanceof IllegalStateException) {
    // handled
  }
}
```

## Смотрите также

- [`isException`](isException.md)
- [`requireException`](requireException.md)
- [](reference-exception.md)

## Исходный код

[`src/exception/IllegalStateException.ts`](https://github.com/MaksymStoianov/apps-script-utils/blob/main/src/exception/IllegalStateException.ts)
