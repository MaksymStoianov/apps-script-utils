<!-- Generated by scripts/docs/generate-topics.mjs. Edits are overwritten. Source: src/lang/number/isDouble.ts. -->

# isDouble

<link-summary>Проверяет, является ли значение числом с дробной частью.</link-summary>

<web-summary>isDouble() — Проверяет, является ли значение числом с дробной частью. apps-script-utils, Русский.</web-summary>

<tldr>
<p>Модуль: <code>lang/number</code> · Доступно с: 1.11.0</p>
</tldr>

```typescript
function isDouble(value: unknown): value is number;
```

Синоним семейства `Float` под именем, к которому тянется рука первым делом. В JavaScript каждое число и так double, поэтому `double` не обозначает отдельного случая во время выполнения.

`isX` и `nonX` отвечают на вопрос и никогда не прерывают выполнение; `requireX` и `requireNonX` возвращают значение или выбрасывают исключение. Схема разобрана в [](validation-conventions.md).

## Параметры

| Параметр | Тип       | Описание              |
| :------- | :-------- | :-------------------- |
| `value`  | `unknown` | Проверяемое значение. |

## Возвращает

`boolean` — `true`, если значение является числом с дробной частью; иначе `false`.

В TypeScript внутри ветки значение `value` сужается до `number`.

## Примеры

### Что проходит, а что нет

```javascript
isDouble(1.5); // => true
isDouble(-0.1); // => true
isDouble(42); // => false
isDouble(1.0); // => false
isDouble(Infinity); // => false
isDouble("1.5"); // => false
```

## Смотрите также

- [`nonDouble`](nonDouble.md)
- [`requireDouble`](requireDouble.md)
- [`requireNonDouble`](requireNonDouble.md)
- [](reference-base.md)

## Исходный код

[`src/lang/number/isDouble.ts`](https://github.com/MaksymStoianov/apps-script-utils/blob/main/src/lang/number/isDouble.ts)
