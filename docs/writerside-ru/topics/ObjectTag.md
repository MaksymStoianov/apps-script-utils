<!-- Generated by scripts/docs/generate-topics.mjs. Edits are overwritten. Source: src/lang/object/ObjectTag.ts. -->

# ObjectTag

<link-summary>Внутренние теги, которые может сообщить значение, в виде именованных констант.</link-summary>

<web-summary>ObjectTag — Внутренние теги, которые может сообщить значение, в виде именованных констант. apps-script-utils, Русский.</web-summary>

<tldr>
<p>Модуль: <code>lang/object</code> · Доступно с: 1.0.0</p>
</tldr>

```typescript
enum ObjectTag
```

`objectToString` возвращает строки вроде `"[object Array]"`; это перечисление даёт им имена, поэтому сравнение читается как `ObjectTag.ARRAY`, а не как литерал, который тихо ломается от опечатки.

На нём построено несколько проверок библиотеки — именно так `isRegExp` и `isFunction` опознают значение, пришедшее из другого фрейма, где `instanceof` уже не работает.

## Примеры

### Форма

```javascript
objectToString([]) === ObjectTag.ARRAY; // => true
objectToString(null) === ObjectTag.NULL; // => true
```

## Смотрите также

- [`objectToString`](objectToString.md)
- [`isRegExp`](isRegExp.md)
- [`isFunction`](isFunction.md)
- [](reference-base.md)

## Исходный код

[`src/lang/object/ObjectTag.ts`](https://github.com/MaksymStoianov/apps-script-utils/blob/main/src/lang/object/ObjectTag.ts)
