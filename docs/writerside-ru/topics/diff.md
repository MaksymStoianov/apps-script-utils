<!-- Generated by scripts/docs/generate-topics.mjs. Edits are overwritten. Source: src/time/diff.ts. -->

# diff

<link-summary>Возвращает интервал между двумя датами в выбранных единицах.</link-summary>

<web-summary>diff() — Возвращает интервал между двумя датами в выбранных единицах. apps-script-utils, Русский.</web-summary>

<tldr>
<p>Модуль: <code>time</code> · Доступно с: 1.11.0</p>
</tldr>

```typescript
function diff(
  left: Date,
  right: Date,
  unit: TimeUnit = "millisecond",
  float: boolean = false
): number;
```

Результат — первая дата минус вторая, поэтому он отрицателен, если вторая позже. По умолчанию возвращаются целые единицы; передайте `float`, чтобы получить и дробную часть.

Миллисекунды, секунды, минуты и часы имеют фиксированную длину и делятся точно. Дни, месяцы и годы считаются целыми прошедшими единицами — поэтому два часа через полночь дают `0` дней, а не `1`.

## Параметры

| Параметр                                    | Тип        | Описание                                                                            |
| :------------------------------------------ | :--------- | :---------------------------------------------------------------------------------- |
| `left`                                      | `Date`     | Дата, от которой отмеряется интервал.                                               |
| `right`                                     | `Date`     | Дата, которая вычитается.                                                           |
| `unit` _(необязательный)_ `= "millisecond"` | `TimeUnit` | `"millisecond"`, `"second"`, `"minute"`, `"hour"`, `"day"`, `"month"` или `"year"`. |
| `float` _(необязательный)_ `= false`        | `boolean`  | Сохранить дробную часть вместо усечения. Только для единиц фиксированной длины.     |

## Возвращает

`number` — интервал, отрицательный, если вторая дата позже.

## Исключения

| Исключение                 | Условие                                                        |
| :------------------------- | :------------------------------------------------------------- |
| `IllegalArgumentException` | аргумент не является корректным `Date` или единица неизвестна. |

## Примеры

### Измерение интервала

```javascript
diff(new Date(2026, 0, 31), new Date(2026, 0, 1), "day"); // => 30
diff(new Date(2026, 0, 1), new Date(2026, 0, 1, 12), "hour"); // => -12
diff(new Date(2026, 0, 2, 1), new Date(2026, 0, 1, 23), "day"); // => 0
```

## Смотрите также

- [`offset`](offset.md)
- [`getDaysInMonth`](getDaysInMonth.md)
- [](reference-base.md)

## Исходный код

[`src/time/diff.ts`](https://github.com/MaksymStoianov/apps-script-utils/blob/main/src/time/diff.ts)
