<!-- Generated by scripts/docs/generate-topics.mjs. Edits are overwritten. Source: src/lang/number/isSafeInteger.ts. -->

# isSafeInteger

<link-summary>Проверяет, является ли значение безопасным целым числом.</link-summary>

<web-summary>isSafeInteger() — Проверяет, является ли значение безопасным целым числом. apps-script-utils, Русский.</web-summary>

<tldr>
<p>Модуль: <code>lang/number</code> · Доступно с: 1.11.0</p>
</tldr>

```typescript
function isSafeInteger(value: unknown): value is number;
```

Целое число, которое точно представимо в IEEE-754. За пределами этого диапазона разные математические целые схлопываются в одно значение, и арифметика незаметно перестаёт быть точной.

`isX` и `nonX` отвечают на вопрос и никогда не прерывают выполнение; `requireX` и `requireNonX` возвращают значение или выбрасывают исключение. Схема разобрана в [](validation-conventions.md).

## Параметры

| Параметр | Тип       | Описание              |
| :------- | :-------- | :-------------------- |
| `value`  | `unknown` | Проверяемое значение. |

## Возвращает

`boolean` — `true`, если значение является безопасным целым числом; иначе `false`.

В TypeScript внутри ветки значение `value` сужается до `number`.

## Примеры

### Что проходит, а что нет

```javascript
isSafeInteger(42); // => true
isSafeInteger(Number.MAX_SAFE_INTEGER); // => true
isSafeInteger(Number.MAX_SAFE_INTEGER + 2); // => false
isSafeInteger(1.5); // => false
isSafeInteger("42"); // => false
```

## Смотрите также

- [`nonSafeInteger`](nonSafeInteger.md)
- [`requireSafeInteger`](requireSafeInteger.md)
- [`requireNonSafeInteger`](requireNonSafeInteger.md)
- [](reference-base.md)

## Исходный код

[`src/lang/number/isSafeInteger.ts`](https://github.com/MaksymStoianov/apps-script-utils/blob/main/src/lang/number/isSafeInteger.ts)
