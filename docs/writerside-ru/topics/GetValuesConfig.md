<!-- Generated by scripts/docs/generate-topics.mjs. Edits are overwritten. Source: src/appsscript/sheet/getValues.ts. -->

# GetValuesConfig

<link-summary>Как `getValues` читает лист.</link-summary>

<web-summary>GetValuesConfig — Как `getValues` читает лист. apps-script-utils, Русский.</web-summary>

<tldr>
<p>Модуль: <code>appsscript/sheet</code> · Доступно с: 1.11.0</p>
</tldr>

```typescript
interface GetValuesConfig
```

`headerRow` указывает строку с именами столбцов и снабжает этими ключами каждую строку. `display` читает значения так, как их видит человек, а не так, как они хранятся. `filter` оставляет строки, `offset` и `limit` разбивают оставшееся на страницы, `mapper` решает, во что превращается каждая.

Порядок фиксирован: сначала фильтр, потом смещение, потом ограничение, потом преобразование, — поэтому лимит считает подходящие строки, а не прочитанные.

## Примеры

### Форма

```javascript
const config = {
  headerRow: 1,
  filter: (row) => row.record.status === "active",
  limit: 100
};
```

## Смотрите также

- [`getValues`](getValues.md)
- [`Row`](Row.md)
- [](reference-appsscript.md)

## Исходный код

[`src/appsscript/sheet/getValues.ts`](https://github.com/MaksymStoianov/apps-script-utils/blob/main/src/appsscript/sheet/getValues.ts)
