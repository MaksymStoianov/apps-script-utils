<!-- Generated by scripts/docs/generate-topics.mjs. Edits are overwritten. Source: src/lang/string/merge.ts. -->

# MergeOptions

<link-summary>Как `merge` заполняет шаблон.</link-summary>

<web-summary>MergeOptions — Как `merge` заполняет шаблон. apps-script-utils, Русский.</web-summary>

<tldr>
<p>Модуль: <code>lang/string</code> · Доступно с: 1.11.0</p>
</tldr>

```typescript
interface MergeOptions
```

`onMissing` решает судьбу плейсхолдера, ключа для которого нет: `"keep"` по умолчанию оставляет его в выводе, где он виден и прослеживается; `"empty"` подставляет пустоту, что аккуратнее в том, что увидит читатель.

`escape` — функция, применяемая к каждому подставляемому значению: `escapeHtml`, если результат попадёт на страницу.

## Примеры

### Форма

```javascript
const options = { onMissing: "empty", escape: escapeHtml };
```

## Смотрите также

- [`merge`](merge.md)
- [`escapeHtml`](escapeHtml.md)
- [](reference-base.md)

## Исходный код

[`src/lang/string/merge.ts`](https://github.com/MaksymStoianov/apps-script-utils/blob/main/src/lang/string/merge.ts)
