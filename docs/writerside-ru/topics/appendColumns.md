<!-- Generated by scripts/docs/generate-topics.mjs. Edits are overwritten. Source: src/appsscript/sheet/appendColumns.ts. -->

# appendColumns

<link-summary>Добавляет столбцы справа от уже имеющихся данных листа.</link-summary>

<web-summary>appendColumns() — Добавляет столбцы справа от уже имеющихся данных листа. apps-script-utils, Русский.</web-summary>

<tldr>
<p>Модуль: <code>appsscript/sheet</code> · Доступно с: 1.0.0</p>
</tldr>

```typescript
function appendColumns(
  sheet: GoogleAppsScript.Spreadsheet.Sheet,
  values: unknown,
  options: Options | null | undefined = {}
): GoogleAppsScript.Spreadsheet.Sheet;
```

Матрица читается как столбцы: по массиву на столбец, запись идёт с первой строки правее последнего столбца с данными. Привести строки к этой форме помогает `transpose`.

Ячейка, текст которой начинается с `=`, записывается как формула, а не как текст — по тому же правилу, что и в редакторе.

## Параметры

| Параметр                            | Тип                                  | Описание                                                                            |
| :---------------------------------- | :----------------------------------- | :---------------------------------------------------------------------------------- |
| `sheet`                             | `GoogleAppsScript.Spreadsheet.Sheet` | Лист, в который выполняется запись.                                                 |
| `values`                            | `unknown`                            | Матрица строк: по массиву на строку, все одной длины.                               |
| `options` _(необязательный)_ `= {}` | `Options \| null \| undefined`       | `afterFrozenRows` помещает новые строки сразу под закреплёнными, а не в самый верх. |

## Возвращает

`GoogleAppsScript.Spreadsheet.Sheet` — тот же лист, чтобы вызовы можно было составлять в цепочку.

## Исключения

| Исключение              | Условие                                                     |
| :---------------------- | :---------------------------------------------------------- |
| `InvalidSheetException` | первый аргумент не является листом.                         |
| `TypeError`             | значения не являются матрицей со строками одинаковой длины. |

## Примеры

### Запись

```javascript
const sheet = SpreadsheetApp.getActiveSheet();

appendColumns(sheet, [["total", "=SUM(A2:A)"]]);
```

## Смотрите также

- [`appendColumn`](appendColumn.md)
- [`prependColumns`](prependColumns.md)
- [`transpose`](transpose.md)
- [](reference-appsscript.md)

## Исходный код

[`src/appsscript/sheet/appendColumns.ts`](https://github.com/MaksymStoianov/apps-script-utils/blob/main/src/appsscript/sheet/appendColumns.ts)
