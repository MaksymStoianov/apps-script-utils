<!-- Generated by scripts/docs/generate-topics.mjs. Edits are overwritten. Source: src/appsscript/sheet/prependColumns.ts. -->

# prependColumns

<link-summary>Вставляет столбцы слева от уже имеющихся данных листа.</link-summary>

<web-summary>prependColumns() — Вставляет столбцы слева от уже имеющихся данных листа. apps-script-utils, Русский.</web-summary>

<tldr>
<p>Модуль: <code>appsscript/sheet</code> · Доступно с: 1.11.0</p>
</tldr>

```typescript
function prependColumns(
  sheet: GoogleAppsScript.Spreadsheet.Sheet,
  count: number,
  values?: unknown[][] | null,
  options: Options | null | undefined = {}
): GoogleAppsScript.Spreadsheet.Sheet;
```

Место освобождается перед первым столбцом области данных, и матрица пишется туда — по массиву на столбец, — поэтому имеющееся на листе не перезаписывается.

Ячейка, текст которой начинается с `=`, записывается как формула, а не как текст — по тому же правилу, что и в редакторе.

## Параметры

| Параметр                            | Тип                                  | Описание                                                                            |
| :---------------------------------- | :----------------------------------- | :---------------------------------------------------------------------------------- |
| `sheet`                             | `GoogleAppsScript.Spreadsheet.Sheet` | Лист, в который выполняется запись.                                                 |
| `count`                             | `number`                             | How many columns to insert. `0` does nothing.                                       |
| `values` _(необязательный)_         | `unknown[][] \| null`                | Матрица строк: по массиву на строку, все одной длины.                               |
| `options` _(необязательный)_ `= {}` | `Options \| null \| undefined`       | `afterFrozenRows` помещает новые строки сразу под закреплёнными, а не в самый верх. |

## Возвращает

`GoogleAppsScript.Spreadsheet.Sheet` — тот же лист, чтобы вызовы можно было составлять в цепочку.

## Исключения

| Исключение              | Условие                                                     |
| :---------------------- | :---------------------------------------------------------- |
| `InvalidSheetException` | первый аргумент не является листом.                         |
| `TypeError`             | значения не являются матрицей со строками одинаковой длины. |

## Примеры

### Запись

```javascript
const sheet = SpreadsheetApp.getActiveSheet();

prependColumns(sheet, [["#", "1", "2"]]);
```

## Смотрите также

- [`prependColumn`](prependColumn.md)
- [`appendColumns`](appendColumns.md)
- [](reference-appsscript.md)

## Исходный код

[`src/appsscript/sheet/prependColumns.ts`](https://github.com/MaksymStoianov/apps-script-utils/blob/main/src/appsscript/sheet/prependColumns.ts)
