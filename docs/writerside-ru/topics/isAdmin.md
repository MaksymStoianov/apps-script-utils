<!-- Generated by scripts/docs/generate-topics.mjs. Edits are overwritten. Source: src/appsscript/admin/isAdmin.ts. -->

# isAdmin

<link-summary>Сообщает, является ли текущий пользователь администратором Workspace.</link-summary>

<web-summary>isAdmin() — Сообщает, является ли текущий пользователь администратором Workspace. apps-script-utils, Русский.</web-summary>

<tldr>
<p>Модуль: <code>appsscript/admin</code> · Доступно с: 1.5.0</p>
</tldr>

```typescript
function isAdmin(): boolean;
```

Активный пользователь ищется через сервис Admin SDK Directory, который должен быть включён для скрипта и авторизован. Любая неудача — сервиса нет, запрос отклонён — записывается в журнал и даёт `false`, поэтому вызов безопасен даже при построении меню.

Это вопрос о человеке, запустившем скрипт, а не о правах на документ.

## Возвращает

`boolean` — `true`, если активный пользователь — администратор.

## Примеры

### Ограничение возможности

```javascript
if (isAdmin()) {
  showAdminMenu();
}
```

## Смотрите также

- [`nonAdmin`](nonAdmin.md)
- [`requireAdmin`](requireAdmin.md)
- [`AdminDirectoryException`](AdminDirectoryException.md)
- [](reference-appsscript.md)

## Исходный код

[`src/appsscript/admin/isAdmin.ts`](https://github.com/MaksymStoianov/apps-script-utils/blob/main/src/appsscript/admin/isAdmin.ts)
