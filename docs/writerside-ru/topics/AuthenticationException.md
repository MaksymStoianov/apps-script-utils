<!-- Generated by scripts/docs/generate-topics.mjs. Edits are overwritten. Source: src/exception/net/AuthenticationException.ts. -->

# AuthenticationException

<link-summary>Аутентификация не удалась: установить, кто вызывает, не получилось.</link-summary>

<web-summary>AuthenticationException — Аутентификация не удалась: установить, кто вызывает, не получилось. apps-script-utils, Русский.</web-summary>

<tldr>
<p>Модуль: <code>exception/net</code> · Доступно с: 1.5.0</p>
</tldr>

```typescript
class AuthenticationException extends RuntimeException
```

Аутентификация не удалась: установить, кто вызывает, не получилось.

Любой класс этой иерархии принимает необязательное сообщение, другое исключение, чьё сообщение будет переиспользовано, или вообще ничего. `isException` распознаёт их все; обычный `Error` к ним не относится.

## Примеры

### Бросить и поймать

```javascript
throw new AuthenticationException("something specific about this call");

try {
  doWork();
} catch (error) {
  if (error instanceof AuthenticationException) {
    // handled
  }
}
```

## Смотрите также

- [`isException`](isException.md)
- [`requireException`](requireException.md)
- [](reference-exception.md)

## Исходный код

[`src/exception/net/AuthenticationException.ts`](https://github.com/MaksymStoianov/apps-script-utils/blob/main/src/exception/net/AuthenticationException.ts)
