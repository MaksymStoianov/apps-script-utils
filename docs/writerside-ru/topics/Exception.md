<!-- Generated by scripts/docs/generate-topics.mjs. Edits are overwritten. Source: src/exception/Exception.ts. -->

# Exception

<link-summary>Основание иерархии, наследник Error.</link-summary>

<web-summary>Exception — Основание иерархии, наследник Error. apps-script-utils, Русский.</web-summary>

<tldr>
<p>Модуль: <code>exception</code> · Доступно с: 1.0.0</p>
</tldr>

```typescript
class Exception extends Error
```

Основание иерархии, наследник `Error`. Ловите его, чтобы поймать всё, что бросает библиотека, и ничего сверх того.

Любой класс этой иерархии принимает необязательное сообщение, другое исключение, чьё сообщение будет переиспользовано, или вообще ничего. `isException` распознаёт их все; обычный `Error` к ним не относится.

## Примеры

### Бросить и поймать

```javascript
throw new Exception("something specific about this call");

try {
  doWork();
} catch (error) {
  if (error instanceof Exception) {
    // handled
  }
}
```

## Смотрите также

- [`isException`](isException.md)
- [`requireException`](requireException.md)
- [](reference-exception.md)

## Исходный код

[`src/exception/Exception.ts`](https://github.com/MaksymStoianov/apps-script-utils/blob/main/src/exception/Exception.ts)
