<!-- Generated by scripts/docs/generate-topics.mjs. Edits are overwritten. Source: src/lang/string/nonValidSlug.ts. -->

# nonValidSlug

<link-summary>Проверяет, что значение **не** является слагом.</link-summary>

<web-summary>nonValidSlug() — Проверяет, что значение **не** является слагом. apps-script-utils, Русский.</web-summary>

<tldr>
<p>Модуль: <code>lang/string</code> · Доступно с: 1.11.0</p>
</tldr>

```typescript
function nonValidSlug(value: string): boolean;
```

`nonValidSlug(value)` — та же проверка, что и `!isValidSlug(value)`, записанная так, чтобы охранное условие читалось как утверждение, а не как отрицание.

Начинается с буквы, дальше буквы, цифры, дефисы и подчёркивания — форма, годная для сегмента URL или ключа. Пробелы, цифра в начале и пустая строка не проходят.

`isX` и `nonX` отвечают на вопрос и никогда не прерывают выполнение; `requireX` и `requireNonX` возвращают значение или выбрасывают исключение. Схема разобрана в [](validation-conventions.md).

## Параметры

| Параметр | Тип      | Описание              |
| :------- | :------- | :-------------------- |
| `value`  | `string` | Проверяемое значение. |

## Возвращает

`boolean` — `true`, если значение не является слагом; иначе `false`.

## Примеры

### Что проходит, а что нет

```javascript
nonValidSlug("1-page"); // => true
nonValidSlug("my page"); // => true
nonValidSlug(""); // => true
nonValidSlug("my-page"); // => false
nonValidSlug("a_1"); // => false
```

## Смотрите также

- [`isValidSlug`](isValidSlug.md)
- [`requireValidSlug`](requireValidSlug.md)
- [](reference-base.md)

## Исходный код

[`src/lang/string/nonValidSlug.ts`](https://github.com/MaksymStoianov/apps-script-utils/blob/main/src/lang/string/nonValidSlug.ts)
