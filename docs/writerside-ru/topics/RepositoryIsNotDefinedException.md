<!-- Generated by scripts/docs/generate-topics.mjs. Edits are overwritten. Source: src/exception/RepositoryIsNotDefinedException.ts. -->

# RepositoryIsNotDefinedException

<link-summary>Репозиторий, на доступность которого рассчитывал скрипт, недоступен.</link-summary>

<web-summary>RepositoryIsNotDefinedException — Репозиторий, на доступность которого рассчитывал скрипт, недоступен. apps-script-utils, Русский.</web-summary>

<tldr>
<p>Модуль: <code>exception</code> · Доступно с: 1.5.0</p>
</tldr>

```typescript
class RepositoryIsNotDefinedException extends RuntimeException
```

Репозиторий, на доступность которого рассчитывал скрипт, недоступен.

Бросается функциями [`requireRepository`](requireRepository.md).

Любой класс этой иерархии принимает необязательное сообщение, другое исключение, чьё сообщение будет переиспользовано, или вообще ничего. `isException` распознаёт их все; обычный `Error` к ним не относится.

## Примеры

### Бросить и поймать

```javascript
throw new RepositoryIsNotDefinedException("something specific about this call");

try {
  doWork();
} catch (error) {
  if (error instanceof RepositoryIsNotDefinedException) {
    // handled
  }
}
```

## Смотрите также

- [`isException`](isException.md)
- [`requireException`](requireException.md)
- [](reference-exception.md)

## Исходный код

[`src/exception/RepositoryIsNotDefinedException.ts`](https://github.com/MaksymStoianov/apps-script-utils/blob/main/src/exception/RepositoryIsNotDefinedException.ts)
