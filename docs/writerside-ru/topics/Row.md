<!-- Generated by scripts/docs/generate-topics.mjs. Edits are overwritten. Source: src/appsscript/sheet/getValues.ts. -->

# Row

<link-summary>Одна строка в том виде, в каком `getValues` передаёт её фильтру или преобразователю.</link-summary>

<web-summary>Row — Одна строка в том виде, в каком `getValues` передаёт её фильтру или преобразователю. apps-script-utils, Русский.</web-summary>

<tldr>
<p>Модуль: <code>appsscript/sheet</code> · Доступно с: 1.11.0</p>
</tldr>

```typescript
interface Row
```

`values` — ячейки слева направо, `position` — номер строки на листе, считая от единицы, тот самый, что человек видит на полях, а также строка с ключами по именам столбцов, если строка заголовков была указана.

Наличие позиции и позволяет фильтру потом сослаться на конкретную строку или что-то с ней сделать.

## Примеры

### Форма

```javascript
// What a filter receives when `headerRow` is set.
const row = {
  values: ["7", "ada@example.com"],
  position: 2,
  record: { id: "7", email: "ada@example.com" }
};
```

## Смотрите также

- [`getValues`](getValues.md)
- [`RowPredicate`](RowPredicate.md)
- [](reference-appsscript.md)

## Исходный код

[`src/appsscript/sheet/getValues.ts`](https://github.com/MaksymStoianov/apps-script-utils/blob/main/src/appsscript/sheet/getValues.ts)
