<!-- Generated by scripts/docs/generate-topics.mjs. Edits are overwritten. Source: src/appsscript/sheet/getColumnLetterByIndex.ts. -->

# getColumnLetterByIndex

<link-summary>Convertit un indice de colonne à base zéro en sa lettre.</link-summary>

<web-summary>getColumnLetterByIndex() — Convertit un indice de colonne à base zéro en sa lettre. apps-script-utils, Français.</web-summary>

<tldr>
<p>Module: <code>appsscript/sheet</code> · Disponible depuis: 1.0.0</p>
</tldr>

```typescript
function getColumnLetterByIndex(columnIndex: number): string;
```

Les indices comptent à partir de zéro, comme un `GridRange` ; les positions à partir de un, comme `getRange(row, column)`. Les confondre est la source habituelle d'un décalage d'une unité dans un script de feuille de calcul, d'où l'existence des deux paires sous des noms distincts.

## Paramètres

| Paramètre     | Type     | Description                                      |
| :------------ | :------- | :----------------------------------------------- |
| `columnIndex` | `number` | The zero-based column index (e.g., `0` for 'A'). |

## Valeur de retour

`string` — la lettre de la colonne.

## Exemples

### Conversion

```javascript
getColumnLetterByIndex(0); // => "A"
getColumnLetterByIndex(26); // => "AA"
```

## Voir aussi

- [`getColumnIndexByLetter`](getColumnIndexByLetter.md)
- [`getColumnLetterByPosition`](getColumnLetterByPosition.md)
- [Class Sheet](https://developers.google.com/apps-script/reference/spreadsheet/sheet)
- [](reference-appsscript.md)

## Code source

[`src/appsscript/sheet/getColumnLetterByIndex.ts`](https://github.com/MaksymStoianov/apps-script-utils/blob/main/src/appsscript/sheet/getColumnLetterByIndex.ts)
