<!-- Generated by scripts/docs/generate-topics.mjs. Edits are overwritten. Source: src/appsscript/sheet/getSchema.ts. -->

# getSchema

<link-summary>Renvoie le schéma d'une feuille, stocké ou déduit de son contenu.</link-summary>

<web-summary>getSchema() — Renvoie le schéma d'une feuille, stocké ou déduit de son contenu. apps-script-utils, Français.</web-summary>

<tldr>
<p>Module: <code>appsscript/sheet</code> · Disponible depuis: 1.11.0</p>
</tldr>

```typescript
function getSchema(sheet: GoogleAppsScript.Spreadsheet.Sheet): SheetSchema | null;
```

Un schéma décrit les colonnes : leurs noms, leurs types et la ligne où figurent les noms. Si `insertSchema` en a stocké un sur la feuille, c'est celui-là qui revient.

Si rien n'a été stocké, la feuille est lue et un schéma est déduit de la ligne d'en-tête et des valeurs en dessous. Le résultat porte alors `inferred: true`, ce qui permet de distinguer une description déclarée d'une description devinée.

## Paramètres

| Paramètre | Type                                 | Description                         |
| :-------- | :----------------------------------- | :---------------------------------- |
| `sheet`   | `GoogleAppsScript.Spreadsheet.Sheet` | La feuille sur laquelle travailler. |

## Valeur de retour

`SheetSchema | null` — le schéma, ou `null` si la feuille n'a rien à décrire.

## Exceptions

| Exception               | Condition                                  |
| :---------------------- | :----------------------------------------- |
| `InvalidSheetException` | le premier argument n'est pas une feuille. |

## Exemples

### Lire un schéma

```javascript
const sheet = SpreadsheetApp.getActiveSheet();

const schema = getSchema(sheet);

schema?.columns.map((column) => column.name);
```

## Voir aussi

- [`insertSchema`](insertSchema.md)
- [`removeSchema`](removeSchema.md)
- [`getValues`](getValues.md)
- [](reference-appsscript.md)

## Code source

[`src/appsscript/sheet/getSchema.ts`](https://github.com/MaksymStoianov/apps-script-utils/blob/main/src/appsscript/sheet/getSchema.ts)
