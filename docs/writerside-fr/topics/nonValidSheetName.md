<!-- Generated by scripts/docs/generate-topics.mjs. Edits are overwritten. Source: src/appsscript/sheet/nonValidSheetName.ts. -->

# nonValidSheetName

<link-summary>Vérifie qu'une valeur n'est **pas** un nom de feuille.</link-summary>

<web-summary>nonValidSheetName() — Vérifie qu'une valeur n'est **pas** un nom de feuille. apps-script-utils, Français.</web-summary>

<tldr>
<p>Module: <code>appsscript/sheet</code> · Disponible depuis: 1.11.0</p>
</tldr>

```typescript
function nonValidSheetName(value: unknown): boolean;
```

`nonValidSheetName(value)` est le même test que `!isValidSheetName(value)`, écrit pour qu'une clause de garde se lise comme une phrase plutôt que comme une négation.

Ce que Google Sheets accepte : au plus 100 caractères, non vide, aucun des caractères `\ / ? * [ ]`, et pas le nom réservé `history`, quelle que soit la casse.

`isX` et `nonX` répondent à une question sans jamais interrompre le flot ; `requireX` et `requireNonX` rendent la valeur ou lèvent. Le schéma est expliqué dans [](validation-conventions.md).

## Paramètres

| Paramètre | Type      | Description           |
| :-------- | :-------- | :-------------------- |
| `value`   | `unknown` | La valeur à vérifier. |

## Valeur de retour

`boolean` — `true` si la valeur n'est pas un nom de feuille ; sinon `false`.

## Exemples

### Ce qui passe et ce qui ne passe pas

```javascript
nonValidSheetName("history"); // => true
nonValidSheetName("Data/2026"); // => true
nonValidSheetName(""); // => true
nonValidSheetName("Data"); // => false
nonValidSheetName("Report 2026"); // => false
```

## Voir aussi

- [`isValidSheetName`](isValidSheetName.md)
- [`requireValidSheetName`](requireValidSheetName.md)
- [](reference-appsscript.md)

## Code source

[`src/appsscript/sheet/nonValidSheetName.ts`](https://github.com/MaksymStoianov/apps-script-utils/blob/main/src/appsscript/sheet/nonValidSheetName.ts)
