<!-- Generated by scripts/docs/generate-topics.mjs. Edits are overwritten. Translated topic. -->

# Utilitaires de base

<link-summary>Toutes les fonctions indépendantes de l'environnement : gardes, tableaux, nombres, chaînes, objets, JSON et HTML.</link-summary>

<web-summary>Référence de toutes les fonctions indépendantes de l'environnement d'apps-script-utils : gardes isX/nonX/requireX, aides pour tableaux, nombres, chaînes, objets, JSON, HTML et temps.</web-summary>

Des fonctions qui ne dépendent d'aucun framework : vérification des types et des valeurs (`isX`/`nonX`/`requireX`),
manipulation de chaînes, de tableaux et d'objets, JSON et HTML. L'environnement Google Apps Script ne leur est pas
nécessaire.

La mention **New** signale une fonction ajoutée dans la dernière version ; **Deprecated** une fonction dont le
retrait est prévu dans une version future.

| Fonction                                                          | Type de retour   | Description brève                                                                                     |
| :---------------------------------------------------------------- | :--------------- | :---------------------------------------------------------------------------------------------------- | :-------------------------------------------------------------------------- |
| [`chunk`](chunk.md)                                               | `Array`          | Découpe un tableau en groupes consécutifs de taille fixe, en validant les deux arguments.             |
| [`compact`](compact.md) **New**                                   | `Array`          | Retire toutes les valeurs falsy d'un tableau et affine le type des éléments sans cast.                |
| [`decodeHtml`](decodeHtml.md)                                     | `String`         | Retransforme les références numériques en caractères.                                                 |
| [`diff`](diff.md) **New**                                         | `Number`         | Renvoie l'intervalle entre deux dates dans l'unité choisie.                                           |
| [`encodeHtml`](encodeHtml.md)                                     | `String`         | Encode chaque caractère en référence numérique.                                                       |
| [`equals`](equals.md) **New**                                     | `Boolean`        | Compare deux valeurs en profondeur et sans risque avec null.                                          |
| [`escapeHtml`](escapeHtml.md)                                     | `String`         | Remplace les cinq caractères qui seraient autrement lus comme du balisage.                            |
| [`escapeRegExp`](escapeRegExp.md)                                 | `String`         | Échappe les caractères qui ont un sens dans une expression régulière.                                 |
| [`escapeXml`](escapeXml.md)                                       | `String`         | Échappe les caractères réservés par XML.                                                              |
| [`first`](first.md) **New**                                       | `Any`            | Prend le premier élément d'un tableau, ou ses n premiers éléments, sans test de vacuité.              |
| [`flat`](flat.md) **New**                                         | `Object`         | Aplatit un objet imbriqué en un seul niveau de clés pointées.                                         |
| [`getDaysInMonth`](getDaysInMonth.md) **New**                     | `Number`         | Renvoie le nombre de jours du mois d'une date.                                                        |
| [`getDaysLeftInMonth`](getDaysLeftInMonth.md) **New**             | `Number`         | Renvoie combien de jours séparent une date de la fin de son mois.                                     |
| [`getPath`](getPath.md) **New**                                   | `Any`            | Lit une propriété imbriquée par chemin, avec valeur de repli.                                         |
| [`hashCode`](hashCode.md)                                         | `Number`         | Produit un condensé numérique pour n'importe quelle valeur.                                           |
| [`intersect`](intersect.md) **New**                               | `Array`          | Trouve les valeurs présentes dans chacun des tableaux fournis.                                        |
| [`is2DArray`](is2DArray.md)                                       | `Boolean`        | Vérifie qu'une valeur est un tableau non vide dont chaque élément est lui-même un tableau.            |
| [`isArray`](isArray.md)                                           | `Boolean`        | Vérifie si une valeur est un tableau.                                                                 |
| [`isBoolean`](isBoolean.md)                                       | `Boolean`        | Vérifie si une valeur est un booléen.                                                                 |
| [`isConsistent2DArray`](isConsistent2DArray.md)                   | `Boolean`        | Vérifie qu'une valeur est une matrice non vide dont toutes les lignes ont la même longueur.           |
| [`isCountable`](isCountable.md)                                   | `Boolean`        | Vérifie si une valeur est un nombre dénombrable.                                                      |
| [`isDouble`](isDouble.md) **New**                                 | `Boolean`        | Vérifie si une valeur est un nombre avec une partie fractionnaire.                                    |
| [`isEmail`](isEmail.md)                                           | `Boolean`        | Vérifie si une valeur est une adresse e-mail.                                                         |
| [`isEmpty`](isEmpty.md)                                           | `Boolean`        | Vérifie si une valeur est vide.                                                                       |
| [`isException`](isException.md)                                   | `Boolean`        | Vérifie si une valeur est une `Exception`.                                                            |
| [`isFloat`](isFloat.md) **New**                                   | `Boolean`        | Vérifie si une valeur est un nombre avec une partie fractionnaire.                                    |
| [`isFunction`](isFunction.md)                                     | `Boolean`        | Vérifie si une valeur est une fonction.                                                               |
| [`isFunctionLike`](isFunctionLike.md)                             | `Boolean`        | Vérifie si une valeur est appelable.                                                                  |
| [`isInteger`](isInteger.md)                                       | `Boolean`        | Vérifie si une valeur est un entier.                                                                  |
| [`isLength`](isLength.md)                                         | `Boolean`        | Vérifie si une valeur est une longueur de tableau valide.                                             |
| [`isNaN`](isNaN.md) **New**                                       | `Boolean`        | Vérifie si une valeur est le nombre `NaN`.                                                            |
| [`isNil`](isNil.md)                                               | `Boolean`        | Vérifie si une valeur est `null` ou `undefined`.                                                      |
| [`isNonNegative`](isNonNegative.md) **New**                       | `Boolean`        | Vérifie si une valeur est un nombre non négatif.                                                      |
| [`isNull`](isNull.md)                                             | `Boolean`        | Vérifie si une valeur est `null`.                                                                     |
| [`isNumber`](isNumber.md)                                         | `Boolean`        | Vérifie si une valeur est un nombre.                                                                  |
| [`isNumberLike`](isNumberLike.md)                                 | `Boolean`        | Vérifie si une valeur est convertible en nombre.                                                      |
| [`isObject`](isObject.md)                                         | `Boolean`        | Vérifie si une valeur est un objet.                                                                   |
| [`isObjectLike`](isObjectLike.md)                                 | `Boolean`        | Vérifie si une valeur est de type objet.                                                              |
| [`isRegExp`](isRegExp.md)                                         | `Boolean`        | Vérifie si une valeur est une expression régulière.                                                   |
| [`isSafeInteger`](isSafeInteger.md) **New**                       | `Boolean`        | Vérifie si une valeur est un entier sûr.                                                              |
| [`isScalar`](isScalar.md)                                         | `Boolean`        | Vérifie si une valeur est un scalaire.                                                                |
| [`isString`](isString.md)                                         | `Boolean`        | Vérifie si une valeur est une chaîne de caractères.                                                   |
| [`isSymbol`](isSymbol.md)                                         | `Boolean`        | Vérifie si une valeur est un symbole.                                                                 |
| [`last`](last.md) **New**                                         | `Any`            | Prend le dernier élément d'un tableau, ou ses n derniers éléments dans leur ordre d'origine.          |
| [`merge`](merge.md) **New**                                       | `String`         | Remplit les emplacements `{{ key }}` d'un gabarit depuis un objet.                                    |
| [`nonDouble`](nonDouble.md) **New**                               | `Boolean`        | Vérifie qu'une valeur n'est **pas** un nombre avec une partie fractionnaire.                          |
| [`nonEmptyString`](nonEmptyString.md) **Deprecated**              | `String`         | Renvoie la valeur comme chaîne non vide, en nommant l'argument en cas d'échec.                        |
| [`isUndefined`](isUndefined.md)                                   | `Boolean`        | Vérifie si une valeur est `undefined`.                                                                |
| [`isUrl`](isUrl.md)                                               | `Boolean`        | Vérifie si une valeur est une URL.                                                                    |
| [`isValidLocale`](isValidLocale.md)                               | `Boolean`        | Vérifie si une valeur est un code de locale.                                                          |
| [`isValidSlug`](isValidSlug.md)                                   | `Boolean`        | Vérifie si une valeur est un slug.                                                                    |
| [`isValidVersion`](isValidVersion.md)                             | `Boolean`        | Vérifie si une valeur est une chaîne de version.                                                      |
| [`isVersionCompatible`](isVersionCompatible.md)                   | `Boolean`        | Vérifie qu'une version atteint au moins celle requise.                                                |
| [`non2DArray`](non2DArray.md) **New**                             | `Boolean`        | Vérifie qu'une valeur n'est **pas** un tableau bidimensionnel non vide.                               |
| [`nonArray`](nonArray.md)                                         | `Boolean`        | Vérifie qu'une valeur n'est **pas** un tableau.                                                       |
| [`nonBoolean`](nonBoolean.md)                                     | `Boolean`        | Vérifie qu'une valeur n'est **pas** un booléen.                                                       |
| [`nonConsistent2DArray`](nonConsistent2DArray.md) **New**         | `Boolean`        | Vérifie qu'une valeur n'est **pas** une matrice dont les lignes ont toutes la même longueur.          |
| [`nonEmpty`](nonEmpty.md)                                         | `Boolean`        | Vérifie qu'une valeur n'est **pas** vide.                                                             |
| [`nonException`](nonException.md) **New**                         | `Boolean`        | Vérifie qu'une valeur n'est **pas** une `Exception`.                                                  |
| [`nonFloat`](nonFloat.md) **New**                                 | `Boolean`        | Vérifie qu'une valeur n'est **pas** un nombre avec une partie fractionnaire.                          |
| [`nonFunction`](nonFunction.md) **New**                           | `Boolean`        | Vérifie qu'une valeur n'est **pas** une fonction.                                                     |
| [`nonCountable`](nonCountable.md) **New**                         | `Boolean`        | Vérifie qu'une valeur n'est **pas** un nombre dénombrable.                                            |
| [`nonFunctionLike`](nonFunctionLike.md) **New**                   | `Boolean`        | Vérifie qu'une valeur n'est **pas** appelable.                                                        |
| [`nonInteger`](nonInteger.md) **New**                             | `Boolean`        | Vérifie qu'une valeur n'est **pas** un entier.                                                        |
| [`nonLength`](nonLength.md) **New**                               | `Boolean`        | Vérifie qu'une valeur n'est **pas** une longueur de tableau valide.                                   |
| [`nonNaN`](nonNaN.md) **New**                                     | `Boolean`        | Vérifie qu'une valeur n'est **pas** le nombre `NaN`.                                                  |
| [`nonNil`](nonNil.md)                                             | `Boolean`        | Vérifie qu'une valeur n'est **pas** `null` ou `undefined`.                                            |
| [`nonNull`](nonNull.md)                                           | `Boolean`        | Vérifie qu'une valeur n'est **pas** `null`.                                                           |
| [`nonNegative`](nonNegative.md) **Deprecated**                    | `Boolean`        | Vérifie qu'une valeur est un nombre non négatif.                                                      |
| [`nonNumber`](nonNumber.md)                                       | `Boolean`        | Vérifie qu'une valeur n'est **pas** un nombre.                                                        |
| [`nonNumberLike`](nonNumberLike.md) **New**                       | `Boolean`        | Vérifie qu'une valeur n'est **pas** convertible en nombre.                                            |
| [`nonObject`](nonObject.md) **New**                               | `Boolean`        | Vérifie qu'une valeur n'est **pas** un objet.                                                         |
| [`nonObjectLike`](nonObjectLike.md) **New**                       | `Boolean`        | Vérifie qu'une valeur n'est **pas** de type objet.                                                    |
| [`nonRegExp`](nonRegExp.md) **New**                               | `Boolean`        | Vérifie qu'une valeur n'est **pas** une expression régulière.                                         |
| [`nonSafeInteger`](nonSafeInteger.md) **New**                     | `Boolean`        | Vérifie qu'une valeur n'est **pas** un entier sûr.                                                    |
| [`nonScalar`](nonScalar.md)                                       | `Boolean`        | Vérifie qu'une valeur n'est **pas** un scalaire.                                                      |
| [`nonString`](nonString.md)                                       | `Boolean`        | Vérifie qu'une valeur n'est **pas** une chaîne de caractères.                                         |
| [`nonSymbol`](nonSymbol.md)                                       | `Boolean`        | Vérifie qu'une valeur n'est **pas** un symbole.                                                       |
| [`nonUndefined`](nonUndefined.md)                                 | `Boolean`        | Vérifie qu'une valeur n'est **pas** `undefined`.                                                      |
| [`nonUrl`](nonUrl.md) **New**                                     | `Boolean`        | Vérifie qu'une valeur n'est **pas** une URL.                                                          |
| [`nonValidEmail`](nonValidEmail.md) **New**                       | `Boolean`        | Vérifie qu'une valeur n'est **pas** une adresse e-mail.                                               |
| [`nonValidLocale`](nonValidLocale.md) **New**                     | `Boolean`        | Vérifie qu'une valeur n'est **pas** un code de locale.                                                |
| [`nonValidSlug`](nonValidSlug.md) **New**                         | `Boolean`        | Vérifie qu'une valeur n'est **pas** un slug.                                                          |
| [`nonValidVersion`](nonValidVersion.md) **New**                   | `Boolean`        | Vérifie qu'une valeur n'est **pas** une chaîne de version.                                            |
| [`nonVersionCompatible`](nonVersionCompatible.md) **New**         | `Boolean`        | Vérifie qu'une version reste en deçà de celle requise.                                                |
| [`now`](now.md)                                                   | `Number`         | Renvoie l'heure courante en millisecondes depuis l'époque.                                            |
| [`ObjectTag`](ObjectTag.md)                                       | `Enum`           | Les tags internes qu'une valeur peut déclarer, sous forme de constantes nommées.                      |
| [`objectToString`](objectToString.md)                             | `String`         | Renvoie le tag interne d'une valeur.                                                                  |
| [`offset`](offset.md) **New**                                     | `Date`           | Décale une date d'un certain nombre d'unités.                                                         |
| [`parseJson`](parseJson.md) **Deprecated**                        | `Object`         | Analyse du JSON, puis réessaie avec un lecteur permissif si l'analyse stricte échoue.                 |
| [`require2DArray`](require2DArray.md) **New**                     | `Array`          | Renvoie la valeur en tant que tableau bidimensionnel non vide, ou lève une exception.                 |
| [`requireArray`](requireArray.md) **New**                         | `Array`          | Renvoie la valeur en tant que un tableau, ou lève une exception.                                      |
| [`requireBoolean`](requireBoolean.md) **New**                     | `Boolean`        | Renvoie la valeur en tant que un booléen, ou lève une exception.                                      |
| [`requireConsistent2DArray`](requireConsistent2DArray.md) **New** | `Array`          | Renvoie la valeur en tant que matrice à lignes de même longueur, ou lève en nommant la ligne fautive. |
| [`requireCountable`](requireCountable.md) **New**                 | `Number`         | Renvoie la valeur en tant que un nombre dénombrable, ou lève une exception.                           |
| [`requireDouble`](requireDouble.md) **New**                       | `Number`         | Renvoie la valeur en tant que un nombre avec une partie fractionnaire, ou lève une exception.         |
| [`requireEmpty`](requireEmpty.md) **New**                         | `Any`            | Renvoie la valeur en tant que une valeur vide, ou lève une exception.                                 |
| [`requireException`](requireException.md) **New**                 | `Exception`      | Renvoie la valeur en tant que une `Exception`, ou lève une exception.                                 |
| [`requireFloat`](requireFloat.md) **New**                         | `Number`         | Renvoie la valeur en tant que un nombre avec une partie fractionnaire, ou lève une exception.         |
| [`requireFunction`](requireFunction.md) **New**                   | `Function`       | Renvoie la valeur en tant que une fonction, ou lève une exception.                                    |
| [`requireFunctionLike`](requireFunctionLike.md) **New**           | `Function`       | Renvoie la valeur en tant que une valeur appelable, ou lève une exception.                            |
| [`requireInteger`](requireInteger.md) **New**                     | `Number`         | Renvoie la valeur en tant que un entier, ou lève une exception.                                       |
| [`requireLength`](requireLength.md) **New**                       | `Number`         | Renvoie la valeur en tant que une longueur de tableau valide, ou lève une exception.                  |
| [`requireNaN`](requireNaN.md) **New**                             | `Number`         | Renvoie la valeur en tant que `NaN`, ou lève une exception.                                           |
| [`requireNil`](requireNil.md) **New**                             | `Null            | Undefined`                                                                                            | Renvoie la valeur en tant que `null` ou `undefined`, ou lève une exception. |
| [`requireNonArray`](requireNonArray.md) **New**                   | `Any`            | Renvoie la valeur après avoir vérifié qu'elle n'est pas un tableau.                                   |
| [`requireNonBoolean`](requireNonBoolean.md) **New**               | `Any`            | Renvoie la valeur après avoir vérifié qu'elle n'est pas un booléen.                                   |
| [`requireNonDouble`](requireNonDouble.md) **New**                 | `Any`            | Renvoie la valeur après avoir vérifié qu'elle n'est pas un nombre avec une partie fractionnaire.      |
| [`requireNonEmpty`](requireNonEmpty.md) **New**                   | `Any`            | Renvoie la valeur après avoir vérifié qu'elle n'est pas vide.                                         |
| [`requireNonFloat`](requireNonFloat.md) **New**                   | `Any`            | Renvoie la valeur après avoir vérifié qu'elle n'est pas un nombre avec une partie fractionnaire.      |
| [`requireNonInteger`](requireNonInteger.md) **New**               | `Any`            | Renvoie la valeur après avoir vérifié qu'elle n'est pas un entier.                                    |
| [`requireNonNaN`](requireNonNaN.md) **New**                       | `Any`            | Renvoie la valeur après avoir vérifié qu'elle n'est pas le nombre `NaN`.                              |
| [`requireNonSafeInteger`](requireNonSafeInteger.md) **New**       | `Any`            | Renvoie la valeur après avoir vérifié qu'elle n'est pas un entier sûr.                                |
| [`requireSafeInteger`](requireSafeInteger.md) **New**             | `Number`         | Renvoie la valeur en tant que un entier sûr, ou lève une exception.                                   |
| [`requireSymbol`](requireSymbol.md) **New**                       | `Symbol`         | Renvoie la valeur en tant que un symbole, ou lève une exception.                                      |
| [`requireNonEmptyString`](requireNonEmptyString.md)               | `String`         | Renvoie la valeur comme une chaîne non vide, ou lève.                                                 |
| [`requireNonException`](requireNonException.md) **New**           | `Any`            | Renvoie la valeur après avoir vérifié qu'elle n'est pas une `Exception`.                              |
| [`requireNonFunction`](requireNonFunction.md) **New**             | `Any`            | Renvoie la valeur après avoir vérifié qu'elle n'est pas une fonction.                                 |
| [`requireNonFunctionLike`](requireNonFunctionLike.md) **New**     | `Any`            | Renvoie la valeur après avoir vérifié qu'elle n'est pas appelable.                                    |
| [`requireNoNilElements`](requireNoNilElements.md) **New**         | `Array`          | Renvoie la collection inchangée après avoir vérifié qu'aucun élément n'est `null` ni `undefined`.     |
| [`requireNonLength`](requireNonLength.md) **New**                 | `Any`            | Renvoie la valeur après avoir vérifié qu'elle n'est pas une longueur de tableau valide.               |
| [`requireNonNil`](requireNonNil.md) **New**                       | `Any`            | Renvoie la valeur après avoir vérifié qu'elle n'est pas `null` ou `undefined`.                        |
| [`requireNonNull`](requireNonNull.md)                             | `any`            | Renvoie la valeur après avoir vérifié qu'elle n'est pas `null` ou `undefined`.                        |
| [`requireNonNumber`](requireNonNumber.md) **New**                 | `Any`            | Renvoie la valeur après avoir vérifié qu'elle n'est pas un nombre.                                    |
| [`requireNonNumberLike`](requireNonNumberLike.md) **New**         | `Any`            | Renvoie la valeur après avoir vérifié qu'elle n'est pas convertible en nombre.                        |
| [`requireNonObject`](requireNonObject.md) **New**                 | `Any`            | Renvoie la valeur après avoir vérifié qu'elle n'est pas un objet.                                     |
| [`requireNonObjectLike`](requireNonObjectLike.md) **New**         | `Any`            | Renvoie la valeur après avoir vérifié qu'elle n'est pas de type objet.                                |
| [`requireNonRegExp`](requireNonRegExp.md) **New**                 | `Any`            | Renvoie la valeur après avoir vérifié qu'elle n'est pas une expression régulière.                     |
| [`requireNonScalar`](requireNonScalar.md) **New**                 | `Any`            | Renvoie la valeur après avoir vérifié qu'elle n'est pas un scalaire.                                  |
| [`requireNonString`](requireNonString.md) **New**                 | `Any`            | Renvoie la valeur après avoir vérifié qu'elle n'est pas une chaîne de caractères.                     |
| [`requireNonSymbol`](requireNonSymbol.md) **New**                 | `Any`            | Renvoie la valeur après avoir vérifié qu'elle n'est pas un symbole.                                   |
| [`requireNonUndefined`](requireNonUndefined.md) **New**           | `Any`            | Renvoie la valeur après avoir vérifié qu'elle n'est pas `undefined`.                                  |
| [`requireNull`](requireNull.md) **New**                           | `Null`           | Renvoie la valeur en tant que `null`, ou lève une exception.                                          |
| [`requireNumber`](requireNumber.md) **New**                       | `Number`         | Renvoie la valeur en tant que un nombre, ou lève une exception.                                       |
| [`requireNumberLike`](requireNumberLike.md) **New**               | `Number          | String`                                                                                               | Renvoie la valeur en tant que une valeur numérique, ou lève une exception.  |
| [`requireObject`](requireObject.md) **New**                       | `Object`         | Renvoie la valeur en tant que un objet, ou lève une exception.                                        |
| [`requireObjectLike`](requireObjectLike.md) **New**               | `Object`         | Renvoie la valeur en tant que une valeur de type objet, ou lève une exception.                        |
| [`requireRegExp`](requireRegExp.md) **New**                       | `RegExp`         | Renvoie la valeur en tant que une expression régulière, ou lève une exception.                        |
| [`requireScalar`](requireScalar.md) **New**                       | `Scalar`         | Renvoie la valeur en tant que un scalaire, ou lève une exception.                                     |
| [`requireState`](requireState.md) **New**                         | `void`           | Affirme qu'une condition tient, et lève dans le cas contraire.                                        |
| [`requireString`](requireString.md)                               | `String`         | Renvoie la valeur en tant que une chaîne de caractères, ou lève une exception.                        |
| [`requireUndefined`](requireUndefined.md) **New**                 | `Undefined`      | Renvoie la valeur en tant que `undefined`, ou lève une exception.                                     |
| [`requireUrl`](requireUrl.md) **New**                             | `String`         | Renvoie la valeur en tant que une URL, ou lève une exception.                                         |
| [`requireValidEmail`](requireValidEmail.md)                       | `String`         | Renvoie la valeur en tant que une adresse e-mail, ou lève une exception.                              |
| [`requireValidLocale`](requireValidLocale.md) **New**             | `String`         | Renvoie la valeur en tant que un code de locale, ou lève une exception.                               |
| [`requireValidSlug`](requireValidSlug.md) **New**                 | `String`         | Renvoie la valeur en tant que un slug, ou lève une exception.                                         |
| [`requireValidVersion`](requireValidVersion.md) **New**           | `String`         | Renvoie la valeur en tant que une chaîne de version, ou lève une exception.                           |
| [`requireVersionCompatible`](requireVersionCompatible.md) **New** | `String`         | Renvoie la version une fois l'exigence satisfaite, ou lève.                                           |
| [`retry`](retry.md) **New**                                       | `T`              | Rappelle une fonction quand elle lève, avec une attente croissante entre les tentatives.              |
| [`setPath`](setPath.md) **New**                                   | `Object`         | Écrit une propriété imbriquée par chemin, en créant ce qui manque.                                    |
| [`StopWatch`](StopWatch.md) **New**                               | `StopWatch`      | Chronomètre une suite de tâches nommées.                                                              |
| [`stringifyJson`](stringifyJson.md)                               | `String`         | Sérialise une valeur en JSON avec un ordre de clés fixe.                                              |
| [`toCamelCase`](toCamelCase.md)                                   | `String`         | Assemble les mots d'une chaîne en camelCase.                                                          |
| [`toInteger`](toInteger.md)                                       | `Number \| null` | Extrait un nombre d'une valeur, ou signale qu'il n'y en a pas.                                        |
| [`toKebabCase`](toKebabCase.md)                                   | `String`         | Réécrit une chaîne en kebab-case.                                                                     |
| [`toLowerCase`](toLowerCase.md)                                   | `String`         | Met une chaîne en minuscules, en nettoyant au besoin ses espaces.                                     |
| [`toProperCase`](toProperCase.md)                                 | `String`         | Met la première lettre de chaque mot en majuscule et le reste en minuscules.                          |
| [`toSnakeCase`](toSnakeCase.md)                                   | `String`         | Réécrit une chaîne en snake_case.                                                                     |
| [`toUpperCase`](toUpperCase.md)                                   | `String`         | Met une chaîne en majuscules, en nettoyant au besoin ses espaces.                                     |
| [`transpose`](transpose.md)                                       | `Array`          | Échange les lignes et les colonnes d'une matrice.                                                     |
| [`unique`](unique.md) **New**                                     | `Array`          | Retire les doublons d'un tableau en gardant la première occurrence de chaque valeur.                  |
| [`versionCompare`](versionCompare.md)                             | `Number`         | Compare deux chaînes de version numériquement.                                                        |
| [`without`](without.md) **New**                                   | `Array`          | Retire d'un tableau toutes les occurrences des valeurs indiquées.                                     |
