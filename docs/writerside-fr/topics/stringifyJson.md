<!-- Generated by scripts/docs/generate-topics.mjs. Edits are overwritten. Source: src/json/stringifyJson.ts. -->

# stringifyJson

<link-summary>Sérialise une valeur en JSON avec un ordre de clés fixe.</link-summary>

<web-summary>stringifyJson() — Sérialise une valeur en JSON avec un ordre de clés fixe. apps-script-utils, Français.</web-summary>

<tldr>
<p>Module: <code>json</code> · Disponible depuis: 1.0.0</p>
</tldr>

```typescript
function stringifyJson(value: unknown): string;
```

`JSON.stringify` écrit les clés dans l'ordre d'insertion : deux objets portant les mêmes données peuvent donc produire des textes différents. Ici, les clés de chaque objet sont triées, ce qui fait de la sortie une clé stable — pour un cache, une somme de contrôle ou une comparaison.

C'est ce que condense `hashCode`, et la raison pour laquelle deux objets égaux donnent le même condensé.

## Paramètres

| Paramètre | Type      | Description             |
| :-------- | :-------- | :---------------------- |
| `value`   | `unknown` | La valeur à sérialiser. |

## Valeur de retour

`string` — le texte JSON, clés d'objet triées.

## Exemples

### Sérialisation

```javascript
stringifyJson({ b: 1, a: 2 }); // => "{\"a\":2,\"b\":1}"
stringifyJson([1, "a", null]); // => "[1,\"a\",null]"
```

## Voir aussi

- [`parseJson`](parseJson.md)
- [`hashCode`](hashCode.md)
- [`equals`](equals.md)
- [](reference-base.md)

## Code source

[`src/json/stringifyJson.ts`](https://github.com/MaksymStoianov/apps-script-utils/blob/main/src/json/stringifyJson.ts)
