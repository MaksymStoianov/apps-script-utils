<!-- Generated by scripts/docs/generate-topics.mjs. Edits are overwritten. Source: src/lang/number/isNaN.ts. -->

# isNaN

<link-summary>Vérifie si une valeur est le nombre `NaN`.</link-summary>

<web-summary>isNaN() — Vérifie si une valeur est le nombre `NaN`. apps-script-utils, Français.</web-summary>

<tldr>
<p>Module: <code>lang/number</code> · Disponible depuis: 1.11.0</p>
</tldr>

```typescript
function isNaN(value: unknown): boolean;
```

Rien n'est converti au préalable, et c'est là que ce test diffère du `isNaN` global : celui-ci convertit son argument, si bien que `isNaN("abc")` et `isNaN(undefined)` renvoient `true`.

`isX` et `nonX` répondent à une question sans jamais interrompre le flot ; `requireX` et `requireNonX` rendent la valeur ou lèvent. Le schéma est expliqué dans [](validation-conventions.md).

## Paramètres

| Paramètre | Type      | Description           |
| :-------- | :-------- | :-------------------- |
| `value`   | `unknown` | La valeur à vérifier. |

## Valeur de retour

`boolean` — `true` si la valeur est le nombre `NaN` ; sinon `false`.

## Exemples

### Ce qui passe et ce qui ne passe pas

```javascript
isNaN(NaN); // => true
isNaN(0 / 0); // => true
isNaN(0); // => false
isNaN("abc"); // => false
isNaN(undefined); // => false
```

## Voir aussi

- [`nonNaN`](nonNaN.md)
- [`requireNaN`](requireNaN.md)
- [`requireNonNaN`](requireNonNaN.md)
- [](reference-base.md)

## Code source

[`src/lang/number/isNaN.ts`](https://github.com/MaksymStoianov/apps-script-utils/blob/main/src/lang/number/isNaN.ts)
