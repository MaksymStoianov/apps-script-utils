<!-- Generated by scripts/docs/generate-topics.mjs. Edits are overwritten. Source: src/appsscript/sheet/getColumnPositionByLetter.ts. -->

# getColumnPositionByLetter

<link-summary>Convertit une lettre de colonne en sa position à base un.</link-summary>

<web-summary>getColumnPositionByLetter() — Convertit une lettre de colonne en sa position à base un. apps-script-utils, Français.</web-summary>

<tldr>
<p>Module: <code>appsscript/sheet</code> · Disponible depuis: 1.0.0</p>
</tldr>

```typescript
function getColumnPositionByLetter(letter: string): number | null;
```

Les indices comptent à partir de zéro, comme un `GridRange` ; les positions à partir de un, comme `getRange(row, column)`. Les confondre est la source habituelle d'un décalage d'une unité dans un script de feuille de calcul, d'où l'existence des deux paires sous des noms distincts.

## Paramètres

| Paramètre | Type     | Description                                        |
| :-------- | :------- | :------------------------------------------------- |
| `letter`  | `string` | La lettre de colonne, dans n'importe quelle casse. |

## Valeur de retour

`number | null` — la position à base un.

## Exemples

### Conversion

```javascript
getColumnPositionByLetter("A"); // => 1
getColumnPositionByLetter("AA"); // => 27
```

## Voir aussi

- [`getColumnLetterByPosition`](getColumnLetterByPosition.md)
- [`getColumnIndexByLetter`](getColumnIndexByLetter.md)
- [](reference-appsscript.md)

## Code source

[`src/appsscript/sheet/getColumnPositionByLetter.ts`](https://github.com/MaksymStoianov/apps-script-utils/blob/main/src/appsscript/sheet/getColumnPositionByLetter.ts)
