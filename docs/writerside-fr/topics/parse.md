<!-- Generated by scripts/docs/generate-topics.mjs. Edits are overwritten. Source: src/net/path/parse.ts. -->

# parse

<link-summary>Décompose un chemin en ses parties.</link-summary>

<web-summary>parse() — Décompose un chemin en ses parties. apps-script-utils, Français.</web-summary>

<tldr>
<p>Module: <code>net/path</code></p>
</tldr>

```typescript
function parse(path: string): ParsedPath;
```

Le résultat porte `root`, `dir`, `base`, `name` et `ext`, les cinq mêmes parties que rapporte `path.parse` de Node. Une partie que le chemin n'a pas est simplement absente de l'objet : un simple nom de fichier revient avec `base`, `name` et `ext` seulement.

`ext` inclut le point, et un nom qui commence par un point — `.gitignore` — est un nom, pas une extension.

## Paramètres

| Paramètre | Type     | Description             |
| :-------- | :------- | :---------------------- |
| `path`    | `string` | Le chemin à décomposer. |

## Valeur de retour

`ParsedPath` — un objet portant les parties présentes dans le chemin.

## Exemples

### Décomposer un chemin

```javascript
parse("/a/b/c.txt");
// => { root: "/", dir: "/a/b", base: "c.txt", name: "c", ext: ".txt" }

parse("readme.md");
// => { base: "readme.md", name: "readme", ext: ".md" }
```

## Voir aussi

- [`join`](join.md)
- [`normalize`](normalize.md)
- [](reference-path.md)

## Code source

[`src/net/path/parse.ts`](https://github.com/MaksymStoianov/apps-script-utils/blob/main/src/net/path/parse.ts)
