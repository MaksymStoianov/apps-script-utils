<!-- Generated by scripts/docs/generate-topics.mjs. Edits are overwritten. Source: src/lang/base/nonFunctionLike.ts. -->

# nonFunctionLike

<link-summary>Vérifie qu'une valeur n'est **pas** appelable.</link-summary>

<web-summary>nonFunctionLike() — Vérifie qu'une valeur n'est **pas** appelable. apps-script-utils, Français.</web-summary>

<tldr>
<p>Module: <code>lang/base</code> · Disponible depuis: 1.11.0</p>
</tldr>

```typescript
function nonFunctionLike<T>(value: T | Function): value is T;
```

`nonFunctionLike(value)` est le même test que `!isFunctionLike(value)`, écrit pour qu'une clause de garde se lise comme une phrase plutôt que comme une négation.

Le test bon marché — `typeof value === "function"` — celui qu'on veut avant d'appeler quelque chose. `isFunction` lit la même question plus strictement.

`isX` et `nonX` répondent à une question sans jamais interrompre le flot ; `requireX` et `requireNonX` rendent la valeur ou lèvent. Le schéma est expliqué dans [](validation-conventions.md).

## Paramètres

| Paramètre | Type            | Description           |
| :-------- | :-------------- | :-------------------- |
| `value`   | `T \| Function` | La valeur à vérifier. |

## Valeur de retour

`boolean` — `true` si la valeur n'est pas appelable ; sinon `false`.

En TypeScript, le garde restreint `value` à `T` dans la branche.

## Exemples

### Ce qui passe et ce qui ne passe pas

```javascript
nonFunctionLike(null); // => true
nonFunctionLike({}); // => true
nonFunctionLike("fn"); // => true
nonFunctionLike(function () {}); // => false
nonFunctionLike(Math.max); // => false
```

## Voir aussi

- [`isFunctionLike`](isFunctionLike.md)
- [`requireFunctionLike`](requireFunctionLike.md)
- [`requireNonFunctionLike`](requireNonFunctionLike.md)
- [](reference-base.md)

## Code source

[`src/lang/base/nonFunctionLike.ts`](https://github.com/MaksymStoianov/apps-script-utils/blob/main/src/lang/base/nonFunctionLike.ts)
