<!-- Generated by scripts/docs/generate-topics.mjs. Edits are overwritten. Source: src/lang/string/merge.ts. -->

# merge

<link-summary>Remplit les emplacements `{{ key }}` d'un gabarit depuis un objet.</link-summary>

<web-summary>merge() — Remplit les emplacements `{{ key }}` d'un gabarit depuis un objet. apps-script-utils, Français.</web-summary>

<tldr>
<p>Module: <code>lang/string</code> · Disponible depuis: 1.11.0</p>
</tldr>

```typescript
function merge(template: string, data: object, options: MergeOptions = {}): string;
```

Les espaces dans les accolades sont ignorés : `{{name}}` et `{{ name }}` désignent le même emplacement. Une clé absente de l'objet est laissée telle quelle, ce qui rend le manque visible plutôt que silencieusement vide ; `onMissing: "empty"` la supprime.

`escape` prend une fonction appliquée à chaque valeur insérée — passez `escapeHtml` si le résultat part dans une page.

## Paramètres

| Paramètre                       | Type           | Description                                                                                   |
| :------------------------------ | :------------- | :-------------------------------------------------------------------------------------------- |
| `template`                      | `string`       | Le gabarit contenant des emplacements `{{ key }}`.                                            |
| `data`                          | `object`       | L'objet d'où proviennent les valeurs.                                                         |
| `options` _(facultatif)_ `= {}` | `MergeOptions` | `onMissing` vaut `"keep"` ou `"empty"` ; `escape` est une fonction appliquée à chaque valeur. |

## Valeur de retour

`string` — le gabarit, emplacements remplis.

## Exceptions

| Exception                  | Condition                                                             |
| :------------------------- | :-------------------------------------------------------------------- |
| `IllegalArgumentException` | le gabarit n'est pas une chaîne, ou les données ne sont pas un objet. |

## Exemples

### Remplir un gabarit

```javascript
merge("Hi {{ name }}!", { name: "Ada" }); // => "Hi Ada!"
merge("Hi {{ name }}!", {}); // => "Hi {{ name }}!"
merge("Hi {{ name }}!", {}, { onMissing: "empty" }); // => "Hi !"
```

## Voir aussi

- [`escapeHtml`](escapeHtml.md)
- [`getPath`](getPath.md)
- [](reference-base.md)

## Code source

[`src/lang/string/merge.ts`](https://github.com/MaksymStoianov/apps-script-utils/blob/main/src/lang/string/merge.ts)
