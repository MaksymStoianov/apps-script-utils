<!-- Generated by scripts/docs/generate-topics.mjs. Edits are overwritten. Source: src/lang/string/nonValidEmail.ts. -->

# nonValidEmail

<link-summary>Vérifie qu'une valeur n'est **pas** une adresse e-mail.</link-summary>

<web-summary>nonValidEmail() — Vérifie qu'une valeur n'est **pas** une adresse e-mail. apps-script-utils, Français.</web-summary>

<tldr>
<p>Module: <code>lang/string</code> · Disponible depuis: 1.11.0</p>
</tldr>

```typescript
function nonValidEmail<T>(value: T | string): value is T;
```

`nonValidEmail(value)` est le même test que `!isValidEmail(value)`, écrit pour qu'une clause de garde se lise comme une phrase plutôt que comme une négation.

L'adresse est coupée sur un unique `@` : le domaine est vérifié comme le fait `isValidDomain`, et la partie locale doit être non vide et sans espaces. C'est un contrôle de format, pas la preuve que la boîte existe.

`isX` et `nonX` répondent à une question sans jamais interrompre le flot ; `requireX` et `requireNonX` rendent la valeur ou lèvent. Le schéma est expliqué dans [](validation-conventions.md).

## Paramètres

| Paramètre | Type          | Description           |
| :-------- | :------------ | :-------------------- |
| `value`   | `T \| string` | La valeur à vérifier. |

## Valeur de retour

`boolean` — `true` si la valeur n'est pas une adresse e-mail ; sinon `false`.

En TypeScript, le garde restreint `value` à `T` dans la branche.

## Exemples

### Ce qui passe et ce qui ne passe pas

```javascript
nonValidEmail("user@@example.com"); // => true
nonValidEmail("user@"); // => true
nonValidEmail("example.com"); // => true
nonValidEmail(""); // => true
nonValidEmail("user@example.com"); // => false
nonValidEmail("first.last@sub.example.co.uk"); // => false
```

## Voir aussi

- [`isEmail`](isEmail.md)
- [`requireValidEmail`](requireValidEmail.md)
- [](reference-base.md)

## Code source

[`src/lang/string/nonValidEmail.ts`](https://github.com/MaksymStoianov/apps-script-utils/blob/main/src/lang/string/nonValidEmail.ts)
