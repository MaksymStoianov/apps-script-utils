<!-- Generated by scripts/docs/generate-topics.mjs. Edits are overwritten. Source: src/lang/string/requireVersionCompatible.ts. -->

# requireVersionCompatible

<link-summary>Renvoie la version une fois l'exigence satisfaite, ou lève.</link-summary>

<web-summary>requireVersionCompatible() — Renvoie la version une fois l'exigence satisfaite, ou lève. apps-script-utils, Français.</web-summary>

<tldr>
<p>Module: <code>lang/string</code> · Disponible depuis: 1.11.0</p>
</tldr>

```typescript
function requireVersionCompatible(
  currentVersion: string,
  requiredVersion: string,
  message?: string
): string;
```

La forme assertive. Le message par défaut nomme les deux versions, ce qui rend une dépendance non satisfaite lisible dans le journal d'exécution.

## Paramètres

| Paramètre                | Type     | Description                                                                            |
| :----------------------- | :------- | :------------------------------------------------------------------------------------- |
| `currentVersion`         | `string` | La version dont on dispose.                                                            |
| `requiredVersion`        | `string` | Le minimum acceptable.                                                                 |
| `message` _(facultatif)_ | `string` | Le message de l'exception. À défaut, un message nommant les deux versions est utilisé. |

## Valeur de retour

`string` — la version disponible, inchangée.

## Exceptions

| Exception                  | Condition                                             |
| :------------------------- | :---------------------------------------------------- |
| `IllegalArgumentException` | la version disponible est inférieure à celle requise. |

## Exemples

### Garder une dépendance

```javascript
requireVersionCompatible("1.3.0", "1.2.0"); // => "1.3.0"
```

## Voir aussi

- [`isVersionCompatible`](isVersionCompatible.md)
- [`nonVersionCompatible`](nonVersionCompatible.md)
- [](reference-base.md)

## Code source

[`src/lang/string/requireVersionCompatible.ts`](https://github.com/MaksymStoianov/apps-script-utils/blob/main/src/lang/string/requireVersionCompatible.ts)
