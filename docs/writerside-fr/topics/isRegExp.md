<!-- Generated by scripts/docs/generate-topics.mjs. Edits are overwritten. Source: src/lang/base/isRegExp.ts. -->

# isRegExp

<link-summary>Vérifie si une valeur est une expression régulière.</link-summary>

<web-summary>isRegExp() — Vérifie si une valeur est une expression régulière. apps-script-utils, Français.</web-summary>

<tldr>
<p>Module: <code>lang/base</code> · Disponible depuis: 1.0.0</p>
</tldr>

```typescript
function isRegExp(value: unknown): value is RegExp;
```

Identifiée par son tag interne plutôt que par `instanceof`, si bien qu'une expression régulière venue d'un autre frame est reconnue aussi.

`isX` et `nonX` répondent à une question sans jamais interrompre le flot ; `requireX` et `requireNonX` rendent la valeur ou lèvent. Le schéma est expliqué dans [](validation-conventions.md).

## Paramètres

| Paramètre | Type      | Description           |
| :-------- | :-------- | :-------------------- |
| `value`   | `unknown` | La valeur à vérifier. |

## Valeur de retour

`boolean` — `true` si la valeur est une expression régulière ; sinon `false`.

En TypeScript, le garde restreint `value` à `RegExp` dans la branche.

## Exemples

### Ce qui passe et ce qui ne passe pas

```javascript
isRegExp(/a/); // => true
isRegExp(new RegExp("a")); // => true
isRegExp("/a/"); // => false
isRegExp({}); // => false
isRegExp(null); // => false
```

## Voir aussi

- [`nonRegExp`](nonRegExp.md)
- [`requireRegExp`](requireRegExp.md)
- [`requireNonRegExp`](requireNonRegExp.md)
- [](reference-base.md)

## Code source

[`src/lang/base/isRegExp.ts`](https://github.com/MaksymStoianov/apps-script-utils/blob/main/src/lang/base/isRegExp.ts)
