<!-- Generated by scripts/docs/generate-topics.mjs. Edits are overwritten. Source: src/exception/InvalidEmailFormatException.ts. -->

# InvalidEmailFormatException

<link-summary>Une chaîne devait être une adresse e-mail et n'en a pas la forme.</link-summary>

<web-summary>InvalidEmailFormatException — Une chaîne devait être une adresse e-mail et n'en a pas la forme. apps-script-utils, Français.</web-summary>

<tldr>
<p>Module: <code>exception</code> · Disponible depuis: 1.0.0</p>
</tldr>

```typescript
class InvalidEmailFormatException extends RuntimeException
```

Une chaîne devait être une adresse e-mail et n'en a pas la forme.

Levée par [`requireValidEmail`](requireValidEmail.md).

Toute classe de cette hiérarchie accepte un message facultatif, une autre exception dont le message est repris, ou rien du tout. `isException` les reconnaît toutes ; un `Error` ordinaire n'en fait pas partie.

## Exemples

### Lever et intercepter

```javascript
throw new InvalidEmailFormatException("something specific about this call");

try {
  doWork();
} catch (error) {
  if (error instanceof InvalidEmailFormatException) {
    // handled
  }
}
```

## Voir aussi

- [`isException`](isException.md)
- [`requireException`](requireException.md)
- [](reference-exception.md)

## Code source

[`src/exception/InvalidEmailFormatException.ts`](https://github.com/MaksymStoianov/apps-script-utils/blob/main/src/exception/InvalidEmailFormatException.ts)
