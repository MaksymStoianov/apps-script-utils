<!-- Generated by scripts/docs/generate-topics.mjs. Edits are overwritten. Source: src/appsscript/ui/nonTextOutput.ts. -->

# nonTextOutput

<link-summary>Vérifie qu'une valeur n'est **pas** un TextOutput.</link-summary>

<web-summary>nonTextOutput() — Vérifie qu'une valeur n'est **pas** un TextOutput. apps-script-utils, Français.</web-summary>

<tldr>
<p>Module: <code>appsscript/ui</code> · Disponible depuis: 1.11.0</p>
</tldr>

```typescript
function nonTextOutput<T>(value: T | TextOutput): value is T;
```

`nonTextOutput(value)` est le même test que `!isTextOutput(value)`, écrit pour qu'une clause de garde se lise comme une phrase plutôt que comme une négation.

Un objet de service Apps Script n'a pas de constructeur contre lequel un script pourrait tester, alors le garde cherche les méthodes qui définissent le type — `getMimeType`, `getContent`. Un bouchon qui les implémente passe, et c'est ce qui rend ce code testable hors de l'environnement d'exécution.

`isX` et `nonX` répondent à une question sans jamais interrompre le flot ; `requireX` et `requireNonX` rendent la valeur ou lèvent. Le schéma est expliqué dans [](validation-conventions.md).

## Paramètres

| Paramètre | Type              | Description           |
| :-------- | :---------------- | :-------------------- |
| `value`   | `T \| TextOutput` | La valeur à vérifier. |

## Valeur de retour

`boolean` — `true` si la valeur n'est pas un TextOutput ; sinon `false`.

En TypeScript, le garde restreint `value` à `T` dans la branche.

## Exemples

### Ce qui passe et ce qui ne passe pas

```javascript
const output = ContentService.createTextOutput("hi");

nonTextOutput({}); // => true
nonTextOutput(null); // => true
nonTextOutput(output); // => false
```

## Voir aussi

- [`isTextOutput`](isTextOutput.md)
- [`requireTextOutput`](requireTextOutput.md)
- [`TextOutput`](https://developers.google.com/apps-script/reference/content/text-output)
- [](reference-appsscript.md)

## Code source

[`src/appsscript/ui/nonTextOutput.ts`](https://github.com/MaksymStoianov/apps-script-utils/blob/main/src/appsscript/ui/nonTextOutput.ts)
