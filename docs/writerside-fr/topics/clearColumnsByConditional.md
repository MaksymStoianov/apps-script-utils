<!-- Generated by scripts/docs/generate-topics.mjs. Edits are overwritten. Source: src/appsscript/sheet/clearColumnsByConditional.ts. -->

# clearColumnsByConditional

<link-summary>Vide les colonnes que le prédicat sélectionne, sans supprimer les colonnes.</link-summary>

<web-summary>clearColumnsByConditional() — Vide les colonnes que le prédicat sélectionne, sans supprimer les colonnes. apps-script-utils, Français.</web-summary>

<tldr>
<p>Module: <code>appsscript/sheet</code> · Disponible depuis: 1.11.0</p>
</tldr>

```typescript
function clearColumnsByConditional(
  target: GoogleAppsScript.Spreadsheet.Sheet | GoogleAppsScript.Spreadsheet.Range,
  predicate: ColumnPredicate,
  options: ColumnConditionalOptions | null | undefined = {}
): number;
```

`clearRowsByConditional` pivoté d'un quart de tour : le contenu part, les colonnes restent, et les lettres des colonnes suivantes ne changent pas.

La feuille est lue une fois et l'effacement regroupé en blocs consécutifs.

Une feuille désigne toute sa plage de données ; une plage ne désigne que les cellules qu'elle contient, le reste de la feuille n'étant ni lu ni touché. Dans les deux cas, le prédicat reçoit la position dans la feuille : une colonne garde son vrai numéro.

## Paramètres

| Paramètre                       | Type                                                                       | Description                                                                                                                                                                          |
| :------------------------------ | :------------------------------------------------------------------------- | :----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `target`                        | `GoogleAppsScript.Spreadsheet.Sheet \| GoogleAppsScript.Spreadsheet.Range` | La feuille sur laquelle travailler, ou la plage dans laquelle travailler : seules ses cellules sont lues, et seules elles sont vidées.                                               |
| `predicate`                     | `ColumnPredicate`                                                          | Reçoit les cellules de la colonne, sa position à base un et — si `headerColumn` est défini — la colonne indexée par les noms de lignes. Renvoyez `true` pour les colonnes à traiter. |
| `options` _(facultatif)_ `= {}` | `ColumnConditionalOptions \| null \| undefined`                            | `headerColumn` désigne la colonne portant les noms de lignes et l'exclut des candidates.                                                                                             |

## Valeur de retour

`number` — combien ont été traitées.

## Exceptions

| Exception                  | Condition                                                                                 |
| :------------------------- | :---------------------------------------------------------------------------------------- |
| `InvalidSheetException`    | le premier argument n'est ni une feuille ni une plage.                                    |
| `IllegalArgumentException` | le prédicat n'est pas une fonction, ou la position d'en-tête n'est pas un entier positif. |

## Exemples

### Utilisation

```javascript
const sheet = SpreadsheetApp.getActiveSheet();

clearColumnsByConditional(sheet, (values) => values.every((cell) => cell === ""));
```

### Dans un seul bloc

```javascript
const sheet = SpreadsheetApp.getActiveSheet();

// Only B2:Z100 is read, and only those cells are cleared.
clearColumnsByConditional(sheet.getRange("B2:Z100"), (values) =>
  values.every((cell) => cell === "")
);
```

## Voir aussi

- [`deleteColumnsByConditional`](deleteColumnsByConditional.md)
- [`clearRowsByConditional`](clearRowsByConditional.md)
- [](reference-appsscript.md)

## Code source

[`src/appsscript/sheet/clearColumnsByConditional.ts`](https://github.com/MaksymStoianov/apps-script-utils/blob/main/src/appsscript/sheet/clearColumnsByConditional.ts)
