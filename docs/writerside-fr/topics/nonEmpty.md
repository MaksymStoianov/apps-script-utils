<!-- Generated by scripts/docs/generate-topics.mjs. Edits are overwritten. Source: src/lang/base/nonEmpty.ts. -->

# nonEmpty

<link-summary>Vérifie qu'une valeur n'est **pas** vide.</link-summary>

<web-summary>nonEmpty() — Vérifie qu'une valeur n'est **pas** vide. apps-script-utils, Français.</web-summary>

<tldr>
<p>Module: <code>lang/base</code> · Disponible depuis: 1.1.0</p>
</tldr>

```typescript
function nonEmpty(value: unknown): boolean;
```

`nonEmpty(value)` est le même test que `!isEmpty(value)`, écrit pour qu'une clause de garde se lise comme une phrase plutôt que comme une négation.

La vacuité se lit selon le type : `null` et `undefined`, une chaîne faite d'espaces, un tableau, `Set` ou `Map` sans entrée, un objet sans clé propre. `0` et `false` sont des valeurs, pas du vide.

`isX` et `nonX` répondent à une question sans jamais interrompre le flot ; `requireX` et `requireNonX` rendent la valeur ou lèvent. Le schéma est expliqué dans [](validation-conventions.md).

## Paramètres

| Paramètre | Type      | Description           |
| :-------- | :-------- | :-------------------- |
| `value`   | `unknown` | La valeur à vérifier. |

## Valeur de retour

`boolean` — `true` si la valeur n'est pas vide ; sinon `false`.

## Exemples

### Ce qui passe et ce qui ne passe pas

```javascript
nonEmpty(0); // => true
nonEmpty(false); // => true
nonEmpty("a"); // => true
nonEmpty([0]); // => true
nonEmpty(null); // => false
nonEmpty(""); // => false
nonEmpty("   "); // => false
nonEmpty([]); // => false
nonEmpty({}); // => false
nonEmpty(new Map()); // => false
```

## Voir aussi

- [`isEmpty`](isEmpty.md)
- [`requireEmpty`](requireEmpty.md)
- [`requireNonEmpty`](requireNonEmpty.md)
- [](reference-base.md)

## Code source

[`src/lang/base/nonEmpty.ts`](https://github.com/MaksymStoianov/apps-script-utils/blob/main/src/lang/base/nonEmpty.ts)
