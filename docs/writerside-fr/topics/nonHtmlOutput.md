<!-- Generated by scripts/docs/generate-topics.mjs. Edits are overwritten. Source: src/appsscript/ui/nonHtmlOutput.ts. -->

# nonHtmlOutput

<link-summary>Vérifie qu'une valeur n'est **pas** un HtmlOutput.</link-summary>

<web-summary>nonHtmlOutput() — Vérifie qu'une valeur n'est **pas** un HtmlOutput. apps-script-utils, Français.</web-summary>

<tldr>
<p>Module: <code>appsscript/ui</code> · Disponible depuis: 1.11.0</p>
</tldr>

```typescript
function nonHtmlOutput<T>(value: T | HtmlOutput): value is T;
```

`nonHtmlOutput(value)` est le même test que `!isHtmlOutput(value)`, écrit pour qu'une clause de garde se lise comme une phrase plutôt que comme une négation.

Un objet de service Apps Script n'a pas de constructeur contre lequel un script pourrait tester, alors le garde cherche les méthodes qui définissent le type — `getContent`, `setTitle`, `setXFrameOptionsMode`. Un bouchon qui les implémente passe, et c'est ce qui rend ce code testable hors de l'environnement d'exécution.

`isX` et `nonX` répondent à une question sans jamais interrompre le flot ; `requireX` et `requireNonX` rendent la valeur ou lèvent. Le schéma est expliqué dans [](validation-conventions.md).

## Paramètres

| Paramètre | Type              | Description           |
| :-------- | :---------------- | :-------------------- |
| `value`   | `T \| HtmlOutput` | La valeur à vérifier. |

## Valeur de retour

`boolean` — `true` si la valeur n'est pas un HtmlOutput ; sinon `false`.

En TypeScript, le garde restreint `value` à `T` dans la branche.

## Exemples

### Ce qui passe et ce qui ne passe pas

```javascript
const output = HtmlService.createHtmlOutput("<p>hi</p>");

nonHtmlOutput({}); // => true
nonHtmlOutput(null); // => true
nonHtmlOutput(output); // => false
```

## Voir aussi

- [`isHtmlOutput`](isHtmlOutput.md)
- [`requireHtmlOutput`](requireHtmlOutput.md)
- [`HtmlOutput`](https://developers.google.com/apps-script/reference/html/html-output)
- [](reference-appsscript.md)

## Code source

[`src/appsscript/ui/nonHtmlOutput.ts`](https://github.com/MaksymStoianov/apps-script-utils/blob/main/src/appsscript/ui/nonHtmlOutput.ts)
