<!-- Generated by scripts/docs/generate-topics.mjs. Edits are overwritten. Source: src/time/types/TimeUnit.ts. -->

# TimeUnit

<link-summary>Une unité dans laquelle exprimer un intervalle.</link-summary>

<web-summary>TimeUnit — Une unité dans laquelle exprimer un intervalle. apps-script-utils, Français.</web-summary>

<tldr>
<p>Module: <code>time/types</code> · Disponible depuis: 1.11.0</p>
</tldr>

```typescript
type TimeUnit = "millisecond" | "second" | "minute" | "hour" | "day" | "month" | "year";
```

`"millisecond"`, `"second"`, `"minute"` et `"hour"` sont des multiples fixes de la milliseconde. `"day"`, `"month"` et `"year"` sont des unités calendaires sans longueur fixe : un jour fait 23 ou 25 heures lors d'un changement d'heure, et un mois de 28 à 31 jours.

Laquelle des deux lectures s'applique est indiquée sur la page de chaque fonction : `diff` compte des unités calendaires entières, `offset` décale de ces unités et ramène le quantième en fin de mois lorsqu'il le faut.

## Exemples

### Forme

```javascript
diff(a, b, "day");
offset(date, 1, "month");
```

## Voir aussi

- [`diff`](diff.md)
- [`offset`](offset.md)
- [](reference-base.md)

## Code source

[`src/time/types/TimeUnit.ts`](https://github.com/MaksymStoianov/apps-script-utils/blob/main/src/time/types/TimeUnit.ts)
