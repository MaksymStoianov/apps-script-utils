<!-- Generated by scripts/docs/generate-topics.mjs. Edits are overwritten. Source: src/exception/RuntimeException.ts. -->

# RuntimeException

<link-summary>Le parent de toute exception qu'un script en cours peut provoquer.</link-summary>

<web-summary>RuntimeException — Le parent de toute exception qu'un script en cours peut provoquer. apps-script-utils, Français.</web-summary>

<tldr>
<p>Module: <code>exception</code> · Disponible depuis: 1.0.0</p>
</tldr>

```typescript
class RuntimeException extends Exception
```

Le parent de toute exception qu'un script en cours peut provoquer. Presque toutes les classes ci-dessous en dérivent plutôt que d'`Exception` directement.

Toute classe de cette hiérarchie accepte un message facultatif, une autre exception dont le message est repris, ou rien du tout. `isException` les reconnaît toutes ; un `Error` ordinaire n'en fait pas partie.

## Exemples

### Lever et intercepter

```javascript
throw new RuntimeException("something specific about this call");

try {
  doWork();
} catch (error) {
  if (error instanceof RuntimeException) {
    // handled
  }
}
```

## Voir aussi

- [`isException`](isException.md)
- [`requireException`](requireException.md)
- [](reference-exception.md)

## Code source

[`src/exception/RuntimeException.ts`](https://github.com/MaksymStoianov/apps-script-utils/blob/main/src/exception/RuntimeException.ts)
