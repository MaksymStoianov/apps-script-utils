<!-- Generated by scripts/docs/generate-topics.mjs. Edits are overwritten. Source: src/appsscript/sheet/requireValidSpreadsheetId.ts. -->

# requireValidSpreadsheetId

<link-summary>Renvoie la valeur en tant que un identifiant de classeur, ou lève une exception.</link-summary>

<web-summary>requireValidSpreadsheetId() — Renvoie la valeur en tant que un identifiant de classeur, ou lève une exception. apps-script-utils, Français.</web-summary>

<tldr>
<p>Module: <code>appsscript/sheet</code> · Disponible depuis: 1.11.0</p>
</tldr>

```typescript
function requireValidSpreadsheetId(value: unknown, message?: string): string;
```

Au moins 25 caractères parmi lettres, chiffres, tirets et tirets bas — l'identifiant qui figure dans l'URL du classeur entre `/d/` et `/edit`. Rien n'est récupéré : seul le format est vérifié.

`isX` et `nonX` répondent à une question sans jamais interrompre le flot ; `requireX` et `requireNonX` rendent la valeur ou lèvent. Le schéma est expliqué dans [](validation-conventions.md).

## Paramètres

| Paramètre                | Type      | Description                                                             |
| :----------------------- | :-------- | :---------------------------------------------------------------------- |
| `value`                  | `unknown` | La valeur à valider.                                                    |
| `message` _(facultatif)_ | `string`  | Le message de l'exception. À défaut, un message par défaut est utilisé. |

## Valeur de retour

`string` — la même valeur, typée comme un identifiant de classeur.

## Exceptions

| Exception                  | Condition                                       |
| :------------------------- | :---------------------------------------------- |
| `IllegalArgumentException` | la valeur n'est pas un identifiant de classeur. |

## Exemples

### Ce qui passe et ce qui ne passe pas

```javascript
requireValidSpreadsheetId("1BxiMVs0XRA5nFMdKvBdBZjgmUUqptlbs74OgvE2upms"); // => "1BxiMVs0XRA5nFMdKvBdBZjgmUUqptlbs74OgvE2upms"
requireValidSpreadsheetId("short-id"); // throws IllegalArgumentException
requireValidSpreadsheetId("has space in it and is long enough to pass"); // throws IllegalArgumentException
```

## Voir aussi

- [`isValidSpreadsheetId`](isValidSpreadsheetId.md)
- [`nonValidSpreadsheetId`](nonValidSpreadsheetId.md)
- [](reference-appsscript.md)

## Code source

[`src/appsscript/sheet/requireValidSpreadsheetId.ts`](https://github.com/MaksymStoianov/apps-script-utils/blob/main/src/appsscript/sheet/requireValidSpreadsheetId.ts)
