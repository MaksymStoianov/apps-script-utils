<!-- Generated by scripts/docs/generate-topics.mjs. Edits are overwritten. Source: src/lang/number/requireDouble.ts. -->

# requireDouble

<link-summary>Renvoie la valeur en tant que un nombre avec une partie fractionnaire, ou lève une exception.</link-summary>

<web-summary>requireDouble() — Renvoie la valeur en tant que un nombre avec une partie fractionnaire, ou lève une exception. apps-script-utils, Français.</web-summary>

<tldr>
<p>Module: <code>lang/number</code> · Disponible depuis: 1.11.0</p>
</tldr>

```typescript
function requireDouble(
  value: unknown,
  message: string = "Expected a number with a fractional part."
): number;
```

Un synonyme de la famille `Float`, sous le nom auquel on pense d'abord. En JavaScript, tout nombre est déjà un double : `double` ne désigne aucun cas particulier à l'exécution.

`isX` et `nonX` répondent à une question sans jamais interrompre le flot ; `requireX` et `requireNonX` rendent la valeur ou lèvent. Le schéma est expliqué dans [](validation-conventions.md).

## Paramètres

| Paramètre                                                                | Type      | Description                                                             |
| :----------------------------------------------------------------------- | :-------- | :---------------------------------------------------------------------- |
| `value`                                                                  | `unknown` | La valeur à valider.                                                    |
| `message` _(facultatif)_ `= "Expected a number with a fractional part."` | `string`  | Le message de l'exception. À défaut, un message par défaut est utilisé. |

## Valeur de retour

`number` — la même valeur, typée comme un nombre avec une partie fractionnaire.

## Exceptions

| Exception                  | Condition                                                    |
| :------------------------- | :----------------------------------------------------------- |
| `IllegalArgumentException` | la valeur n'est pas un nombre avec une partie fractionnaire. |

## Exemples

### Ce qui passe et ce qui ne passe pas

```javascript
requireDouble(1.5); // => 1.5
requireDouble(-0.1); // => -0.1
requireDouble(42); // throws IllegalArgumentException
requireDouble(1.0); // throws IllegalArgumentException
```

## Voir aussi

- [`isDouble`](isDouble.md)
- [`nonDouble`](nonDouble.md)
- [`requireNonDouble`](requireNonDouble.md)
- [](reference-base.md)

## Code source

[`src/lang/number/requireDouble.ts`](https://github.com/MaksymStoianov/apps-script-utils/blob/main/src/lang/number/requireDouble.ts)
