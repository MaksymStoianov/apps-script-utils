<!-- Generated by scripts/docs/generate-topics.mjs. Edits are overwritten. Source: src/appsscript/sheet/getValues.ts. -->

# GetValuesConfig

<link-summary>Comment `getValues` lit une feuille.</link-summary>

<web-summary>GetValuesConfig — Comment `getValues` lit une feuille. apps-script-utils, Français.</web-summary>

<tldr>
<p>Module: <code>appsscript/sheet</code> · Disponible depuis: 1.11.0</p>
</tldr>

```typescript
interface GetValuesConfig
```

`headerRow` désigne la ligne des noms de colonnes et indexe chaque ligne par eux. `display` lit les valeurs telles qu'une personne les voit plutôt que telles qu'elles sont stockées. `filter` retient des lignes, `offset` et `limit` paginent ce qui reste, `mapper` décide de ce que devient chacune.

L'ordre est fixe : filtre, puis décalage, puis limite, puis transformation — une limite compte donc les lignes retenues, pas les lignes lues.

## Exemples

### Forme

```javascript
const config = {
  headerRow: 1,
  filter: (row) => row.record.status === "active",
  limit: 100
};
```

## Voir aussi

- [`getValues`](getValues.md)
- [`Row`](Row.md)
- [](reference-appsscript.md)

## Code source

[`src/appsscript/sheet/getValues.ts`](https://github.com/MaksymStoianov/apps-script-utils/blob/main/src/appsscript/sheet/getValues.ts)
