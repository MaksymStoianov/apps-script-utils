<!-- Generated by scripts/docs/generate-topics.mjs. Edits are overwritten. Source: src/appsscript/sheet/isValidSheetId.ts. -->

# isValidSheetId

<link-summary>Vérifie si une valeur est un identifiant de feuille.</link-summary>

<web-summary>isValidSheetId() — Vérifie si une valeur est un identifiant de feuille. apps-script-utils, Français.</web-summary>

<tldr>
<p>Module: <code>appsscript/sheet</code> · Disponible depuis: 1.5.0</p>
</tldr>

```typescript
function isValidSheetId(value: unknown): value is number;
```

Apps Script numérote les feuilles à partir de zéro : un identifiant de feuille est un entier sûr non négatif — la règle qu'applique `isCountable`. La première feuille d'un classeur porte l'identifiant `0`.

`isX` et `nonX` répondent à une question sans jamais interrompre le flot ; `requireX` et `requireNonX` rendent la valeur ou lèvent. Le schéma est expliqué dans [](validation-conventions.md).

## Paramètres

| Paramètre | Type      | Description           |
| :-------- | :-------- | :-------------------- |
| `value`   | `unknown` | La valeur à vérifier. |

## Valeur de retour

`boolean` — `true` si la valeur est un identifiant de feuille ; sinon `false`.

En TypeScript, le garde restreint `value` à `number` dans la branche.

## Exemples

### Ce qui passe et ce qui ne passe pas

```javascript
isValidSheetId(0); // => true
isValidSheetId(1234567890); // => true
isValidSheetId(-1); // => false
isValidSheetId(1.5); // => false
isValidSheetId("0"); // => false
```

## Voir aussi

- [`nonValidSheetId`](nonValidSheetId.md)
- [`requireValidSheetId`](requireValidSheetId.md)
- [](reference-appsscript.md)

## Code source

[`src/appsscript/sheet/isValidSheetId.ts`](https://github.com/MaksymStoianov/apps-script-utils/blob/main/src/appsscript/sheet/isValidSheetId.ts)
