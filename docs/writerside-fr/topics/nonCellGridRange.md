<!-- Generated by scripts/docs/generate-topics.mjs. Edits are overwritten. Source: src/appsscript/sheet/nonCellGridRange.ts. -->

# nonCellGridRange

<link-summary>Vérifie qu'une valeur n'est **pas** une plage d'une seule cellule.</link-summary>

<web-summary>nonCellGridRange() — Vérifie qu'une valeur n'est **pas** une plage d'une seule cellule. apps-script-utils, Français.</web-summary>

<tldr>
<p>Module: <code>appsscript/sheet</code> · Disponible depuis: 1.11.0</p>
</tldr>

```typescript
function nonCellGridRange(gridRange: GridRange): boolean;
```

`nonCellGridRange(value)` est le même test que `!isCellGridRange(value)`, écrit pour qu'une clause de garde se lise comme une phrase plutôt que comme une négation.

Une ligne et une colonne, mesurées d'après les bornes et non d'après l'écriture de la plage : les indices de fin sont exclus, une cellule unique vaut donc `endRowIndex - startRowIndex === 1`, de même pour les colonnes. Une plage dont une borne est omise est ouverte de ce côté et n'est jamais une cellule unique.

`isX` et `nonX` répondent à une question sans jamais interrompre le flot ; `requireX` et `requireNonX` rendent la valeur ou lèvent. Le schéma est expliqué dans [](validation-conventions.md).

## Paramètres

| Paramètre   | Type        | Description              |
| :---------- | :---------- | :----------------------- |
| `gridRange` | `GridRange` | The grid range to check. |

## Valeur de retour

`boolean` — `true` si la valeur n'est pas une plage d'une seule cellule ; sinon `false`.

## Exemples

### Ce qui passe et ce qui ne passe pas

```javascript
nonCellGridRange({ startRowIndex: 0, endRowIndex: 2, startColumnIndex: 0, endColumnIndex: 1 }); // => true
nonCellGridRange({}); // => true
nonCellGridRange(null); // => true
nonCellGridRange({ startRowIndex: 0, endRowIndex: 1, startColumnIndex: 0, endColumnIndex: 1 }); // => false
```

## Voir aussi

- [`isCellGridRange`](isCellGridRange.md)
- [`requireCellGridRange`](requireCellGridRange.md)
- [](reference-appsscript.md)

## Code source

[`src/appsscript/sheet/nonCellGridRange.ts`](https://github.com/MaksymStoianov/apps-script-utils/blob/main/src/appsscript/sheet/nonCellGridRange.ts)
