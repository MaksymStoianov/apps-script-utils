<!-- Generated by scripts/docs/generate-topics.mjs. Edits are overwritten. Source: src/appsscript/sheet/isCellGridRange.ts. -->

# isCellGridRange

<link-summary>Vérifie si une valeur est une plage d'une seule cellule.</link-summary>

<web-summary>isCellGridRange() — Vérifie si une valeur est une plage d'une seule cellule. apps-script-utils, Français.</web-summary>

<tldr>
<p>Module: <code>appsscript/sheet</code> · Disponible depuis: 1.0.0</p>
</tldr>

```typescript
function isCellGridRange(gridRange: GridRange): boolean;
```

Une ligne et une colonne, mesurées d'après les bornes et non d'après l'écriture de la plage : les indices de fin sont exclus, une cellule unique vaut donc `endRowIndex - startRowIndex === 1`, de même pour les colonnes. Une plage dont une borne est omise est ouverte de ce côté et n'est jamais une cellule unique.

`isX` et `nonX` répondent à une question sans jamais interrompre le flot ; `requireX` et `requireNonX` rendent la valeur ou lèvent. Le schéma est expliqué dans [](validation-conventions.md).

## Paramètres

| Paramètre   | Type        | Description                                                                    |
| :---------- | :---------- | :----------------------------------------------------------------------------- |
| `gridRange` | `GridRange` | The <a href="./types/GridRange.ts"><code>GridRange</code></a> object to check. |

## Valeur de retour

`boolean` — `true` si la valeur est une plage d'une seule cellule ; sinon `false`.

## Exemples

### Ce qui passe et ce qui ne passe pas

```javascript
isCellGridRange({ startRowIndex: 0, endRowIndex: 1, startColumnIndex: 0, endColumnIndex: 1 }); // => true
isCellGridRange({ startRowIndex: 0, endRowIndex: 2, startColumnIndex: 0, endColumnIndex: 1 }); // => false
isCellGridRange({}); // => false
isCellGridRange(null); // => false
```

## Voir aussi

- [`nonCellGridRange`](nonCellGridRange.md)
- [`requireCellGridRange`](requireCellGridRange.md)
- [](reference-appsscript.md)

## Code source

[`src/appsscript/sheet/isCellGridRange.ts`](https://github.com/MaksymStoianov/apps-script-utils/blob/main/src/appsscript/sheet/isCellGridRange.ts)
