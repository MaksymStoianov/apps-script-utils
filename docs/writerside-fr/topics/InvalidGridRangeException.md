<!-- Generated by scripts/docs/generate-topics.mjs. Edits are overwritten. Source: src/exception/appsscript/sheet/InvalidGridRangeException.ts. -->

# InvalidGridRangeException

<link-summary>Un GridRange était attendu et l'objet n'en est pas un valide : bornes absentes, inversées ou négatives.</link-summary>

<web-summary>InvalidGridRangeException — Un GridRange était attendu et l'objet n'en est pas un valide : bornes absentes, inversées ou négatives. apps-script-utils, Français.</web-summary>

<tldr>
<p>Module: <code>exception/appsscript/sheet</code> · Disponible depuis: 1.5.0</p>
</tldr>

```typescript
class InvalidGridRangeException extends RuntimeException
```

Un `GridRange` était attendu et l'objet n'en est pas un valide : bornes absentes, inversées ou négatives.

Toute classe de cette hiérarchie accepte un message facultatif, une autre exception dont le message est repris, ou rien du tout. `isException` les reconnaît toutes ; un `Error` ordinaire n'en fait pas partie.

## Exemples

### Lever et intercepter

```javascript
throw new InvalidGridRangeException("something specific about this call");

try {
  doWork();
} catch (error) {
  if (error instanceof InvalidGridRangeException) {
    // handled
  }
}
```

## Voir aussi

- [`isException`](isException.md)
- [`requireException`](requireException.md)
- [](reference-exception.md)

## Code source

[`src/exception/appsscript/sheet/InvalidGridRangeException.ts`](https://github.com/MaksymStoianov/apps-script-utils/blob/main/src/exception/appsscript/sheet/InvalidGridRangeException.ts)
