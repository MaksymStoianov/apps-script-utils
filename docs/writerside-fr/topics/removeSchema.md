<!-- Generated by scripts/docs/generate-topics.mjs. Edits are overwritten. Source: src/appsscript/sheet/removeSchema.ts. -->

# removeSchema

<link-summary>Supprime le schéma stocké sur une feuille.</link-summary>

<web-summary>removeSchema() — Supprime le schéma stocké sur une feuille. apps-script-utils, Français.</web-summary>

<tldr>
<p>Module: <code>appsscript/sheet</code> · Disponible depuis: 1.11.0</p>
</tldr>

```typescript
function removeSchema(
  sheet: GoogleAppsScript.Spreadsheet.Sheet,
  options: RemoveSchemaOptions | null | undefined = {}
): boolean;
```

La description stockée disparaît ; les données et la ligne d'en-tête restent exactement telles quelles. Une feuille sans schéma stocké n'est pas touchée et l'appel renvoie `false`.

`validation: true` efface en plus la validation des données sur les colonnes décrites. C'est désactivé par défaut à dessein : Apps Script ne permet pas de distinguer une règle créée par le schéma d'une règle ajoutée à la main, et l'effacement emporterait les deux.

## Paramètres

| Paramètre                       | Type                                       | Description                                                                  |
| :------------------------------ | :----------------------------------------- | :--------------------------------------------------------------------------- |
| `sheet`                         | `GoogleAppsScript.Spreadsheet.Sheet`       | La feuille sur laquelle travailler.                                          |
| `options` _(facultatif)_ `= {}` | `RemoveSchemaOptions \| null \| undefined` | `validation` efface aussi la validation des données qu'impliquait le schéma. |

## Valeur de retour

`boolean` — `true` si un schéma a été supprimé.

## Exceptions

| Exception               | Condition                                  |
| :---------------------- | :----------------------------------------- |
| `InvalidSheetException` | le premier argument n'est pas une feuille. |

## Exemples

### Supprimer un schéma

```javascript
const sheet = SpreadsheetApp.getActiveSheet();

removeSchema(sheet); // => true when one was there
removeSchema(sheet, { validation: true }); // also clears the rules it implied
```

## Voir aussi

- [`getSchema`](getSchema.md)
- [`insertSchema`](insertSchema.md)
- [](reference-appsscript.md)

## Code source

[`src/appsscript/sheet/removeSchema.ts`](https://github.com/MaksymStoianov/apps-script-utils/blob/main/src/appsscript/sheet/removeSchema.ts)
