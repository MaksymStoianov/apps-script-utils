<!-- Generated by scripts/docs/generate-topics.mjs. Edits are overwritten. Source: src/appsscript/sheet/nonRange.ts. -->

# nonRange

<link-summary>Vérifie qu'une valeur n'est **pas** une plage.</link-summary>

<web-summary>nonRange() — Vérifie qu'une valeur n'est **pas** une plage. apps-script-utils, Français.</web-summary>

<tldr>
<p>Module: <code>appsscript/sheet</code> · Disponible depuis: 1.3.0</p>
</tldr>

```typescript
function nonRange<T>(value: T | GoogleAppsScript.Spreadsheet.Range): value is T;
```

`nonRange(value)` est le même test que `!isRange(value)`, écrit pour qu'une clause de garde se lise comme une phrase plutôt que comme une négation.

Un objet de service Apps Script n'a pas de constructeur contre lequel un script pourrait tester, alors le garde lit le tag que l'objet déclare pour lui-même : `value.toString() === "Range"`. Un objet ordinaire qui déclare le même tag passe aussi, ce qui en pratique désigne un double de test.

`isX` et `nonX` répondent à une question sans jamais interrompre le flot ; `requireX` et `requireNonX` rendent la valeur ou lèvent. Le schéma est expliqué dans [](validation-conventions.md).

## Paramètres

| Paramètre | Type                                      | Description           |
| :-------- | :---------------------------------------- | :-------------------- |
| `value`   | `T \| GoogleAppsScript.Spreadsheet.Range` | La valeur à vérifier. |

## Valeur de retour

`boolean` — `true` si la valeur n'est pas une plage ; sinon `false`.

En TypeScript, le garde restreint `value` à `T` dans la branche.

## Exemples

### Ce qui passe et ce qui ne passe pas

```javascript
const range = SpreadsheetApp.getActiveRange();

nonRange({}); // => true
nonRange(null); // => true
nonRange(range); // => false
```

## Voir aussi

- [`isRange`](isRange.md)
- [`requireRange`](requireRange.md)
- [`Range`](https://developers.google.com/apps-script/reference/spreadsheet/range)
- [](reference-appsscript.md)

## Code source

[`src/appsscript/sheet/nonRange.ts`](https://github.com/MaksymStoianov/apps-script-utils/blob/main/src/appsscript/sheet/nonRange.ts)
