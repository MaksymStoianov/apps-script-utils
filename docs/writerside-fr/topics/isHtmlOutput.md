<!-- Generated by scripts/docs/generate-topics.mjs. Edits are overwritten. Source: src/appsscript/ui/isHtmlOutput.ts. -->

# isHtmlOutput

<link-summary>Vérifie si une valeur est un HtmlOutput.</link-summary>

<web-summary>isHtmlOutput() — Vérifie si une valeur est un HtmlOutput. apps-script-utils, Français.</web-summary>

<tldr>
<p>Module: <code>appsscript/ui</code> · Disponible depuis: 1.0.0</p>
</tldr>

```typescript
function isHtmlOutput(value: unknown): value is HtmlOutput;
```

Un objet de service Apps Script n'a pas de constructeur contre lequel un script pourrait tester, alors le garde cherche les méthodes qui définissent le type — `getContent`, `setTitle`, `setXFrameOptionsMode`. Un bouchon qui les implémente passe, et c'est ce qui rend ce code testable hors de l'environnement d'exécution.

`isX` et `nonX` répondent à une question sans jamais interrompre le flot ; `requireX` et `requireNonX` rendent la valeur ou lèvent. Le schéma est expliqué dans [](validation-conventions.md).

## Paramètres

| Paramètre | Type      | Description           |
| :-------- | :-------- | :-------------------- |
| `value`   | `unknown` | La valeur à vérifier. |

## Valeur de retour

`boolean` — `true` si la valeur est un HtmlOutput ; sinon `false`.

En TypeScript, le garde restreint `value` à `HtmlOutput` dans la branche.

## Exemples

### Ce qui passe et ce qui ne passe pas

```javascript
const output = HtmlService.createHtmlOutput("<p>hi</p>");

isHtmlOutput(output); // => true
isHtmlOutput({}); // => false
isHtmlOutput(null); // => false
```

## Voir aussi

- [`nonHtmlOutput`](nonHtmlOutput.md)
- [`requireHtmlOutput`](requireHtmlOutput.md)
- [`HtmlOutput`](https://developers.google.com/apps-script/reference/html/html-output)
- [](reference-appsscript.md)

## Code source

[`src/appsscript/ui/isHtmlOutput.ts`](https://github.com/MaksymStoianov/apps-script-utils/blob/main/src/appsscript/ui/isHtmlOutput.ts)
