<!-- Generated by scripts/docs/generate-topics.mjs. Edits are overwritten. Source: src/appsscript/sheet/nonValidSpreadsheetId.ts. -->

# nonValidSpreadsheetId

<link-summary>Vérifie qu'une valeur n'est **pas** un identifiant de classeur.</link-summary>

<web-summary>nonValidSpreadsheetId() — Vérifie qu'une valeur n'est **pas** un identifiant de classeur. apps-script-utils, Français.</web-summary>

<tldr>
<p>Module: <code>appsscript/sheet</code> · Disponible depuis: 1.11.0</p>
</tldr>

```typescript
function nonValidSpreadsheetId(value: unknown): boolean;
```

`nonValidSpreadsheetId(value)` est le même test que `!isValidSpreadsheetId(value)`, écrit pour qu'une clause de garde se lise comme une phrase plutôt que comme une négation.

Au moins 25 caractères parmi lettres, chiffres, tirets et tirets bas — l'identifiant qui figure dans l'URL du classeur entre `/d/` et `/edit`. Rien n'est récupéré : seul le format est vérifié.

`isX` et `nonX` répondent à une question sans jamais interrompre le flot ; `requireX` et `requireNonX` rendent la valeur ou lèvent. Le schéma est expliqué dans [](validation-conventions.md).

## Paramètres

| Paramètre | Type      | Description           |
| :-------- | :-------- | :-------------------- |
| `value`   | `unknown` | La valeur à vérifier. |

## Valeur de retour

`boolean` — `true` si la valeur n'est pas un identifiant de classeur ; sinon `false`.

## Exemples

### Ce qui passe et ce qui ne passe pas

```javascript
nonValidSpreadsheetId("short-id"); // => true
nonValidSpreadsheetId("has space in it and is long enough to pass"); // => true
nonValidSpreadsheetId("1BxiMVs0XRA5nFMdKvBdBZjgmUUqptlbs74OgvE2upms"); // => false
```

## Voir aussi

- [`isValidSpreadsheetId`](isValidSpreadsheetId.md)
- [`requireValidSpreadsheetId`](requireValidSpreadsheetId.md)
- [](reference-appsscript.md)

## Code source

[`src/appsscript/sheet/nonValidSpreadsheetId.ts`](https://github.com/MaksymStoianov/apps-script-utils/blob/main/src/appsscript/sheet/nonValidSpreadsheetId.ts)
