<!-- Generated by scripts/docs/generate-topics.mjs. Edits are overwritten. Source: src/exception/RepositoryIsNotDefinedException.ts. -->

# RepositoryIsNotDefinedException

<link-summary>Un dépôt que le script attendait n'est pas là.</link-summary>

<web-summary>RepositoryIsNotDefinedException — Un dépôt que le script attendait n'est pas là. apps-script-utils, Français.</web-summary>

<tldr>
<p>Module: <code>exception</code> · Disponible depuis: 1.5.0</p>
</tldr>

```typescript
class RepositoryIsNotDefinedException extends RuntimeException
```

Un dépôt que le script attendait n'est pas là.

Levée par [`requireRepository`](requireRepository.md).

Toute classe de cette hiérarchie accepte un message facultatif, une autre exception dont le message est repris, ou rien du tout. `isException` les reconnaît toutes ; un `Error` ordinaire n'en fait pas partie.

## Exemples

### Lever et intercepter

```javascript
throw new RepositoryIsNotDefinedException("something specific about this call");

try {
  doWork();
} catch (error) {
  if (error instanceof RepositoryIsNotDefinedException) {
    // handled
  }
}
```

## Voir aussi

- [`isException`](isException.md)
- [`requireException`](requireException.md)
- [](reference-exception.md)

## Code source

[`src/exception/RepositoryIsNotDefinedException.ts`](https://github.com/MaksymStoianov/apps-script-utils/blob/main/src/exception/RepositoryIsNotDefinedException.ts)
