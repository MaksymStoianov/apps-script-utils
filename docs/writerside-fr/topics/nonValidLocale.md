<!-- Generated by scripts/docs/generate-topics.mjs. Edits are overwritten. Source: src/lang/string/nonValidLocale.ts. -->

# nonValidLocale

<link-summary>Vérifie qu'une valeur n'est **pas** un code de locale.</link-summary>

<web-summary>nonValidLocale() — Vérifie qu'une valeur n'est **pas** un code de locale. apps-script-utils, Français.</web-summary>

<tldr>
<p>Module: <code>lang/string</code> · Disponible depuis: 1.11.0</p>
</tldr>

```typescript
function nonValidLocale(value: string): boolean;
```

`nonValidLocale(value)` est le même test que `!isValidLocale(value)`, écrit pour qu'une clause de garde se lise comme une phrase plutôt que comme une négation.

Exactement deux lettres ASCII, dans n'importe quelle casse — `"en"`, `"UK"`. Un sous-tag de région ou d'écriture, comme dans `"en-US"`, est refusé : il s'agit du code de langue court, pas d'une étiquette BCP 47 complète.

`isX` et `nonX` répondent à une question sans jamais interrompre le flot ; `requireX` et `requireNonX` rendent la valeur ou lèvent. Le schéma est expliqué dans [](validation-conventions.md).

## Paramètres

| Paramètre | Type     | Description           |
| :-------- | :------- | :-------------------- |
| `value`   | `string` | La valeur à vérifier. |

## Valeur de retour

`boolean` — `true` si la valeur n'est pas un code de locale ; sinon `false`.

## Exemples

### Ce qui passe et ce qui ne passe pas

```javascript
nonValidLocale("en-US"); // => true
nonValidLocale("eng"); // => true
nonValidLocale(""); // => true
nonValidLocale("en"); // => false
nonValidLocale("UK"); // => false
```

## Voir aussi

- [`isValidLocale`](isValidLocale.md)
- [`requireValidLocale`](requireValidLocale.md)
- [](reference-base.md)

## Code source

[`src/lang/string/nonValidLocale.ts`](https://github.com/MaksymStoianov/apps-script-utils/blob/main/src/lang/string/nonValidLocale.ts)
