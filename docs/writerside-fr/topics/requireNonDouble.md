<!-- Generated by scripts/docs/generate-topics.mjs. Edits are overwritten. Source: src/lang/number/requireNonDouble.ts. -->

# requireNonDouble

<link-summary>Renvoie la valeur après avoir vérifié qu'elle n'est pas un nombre avec une partie fractionnaire.</link-summary>

<web-summary>requireNonDouble() — Renvoie la valeur après avoir vérifié qu'elle n'est pas un nombre avec une partie fractionnaire. apps-script-utils, Français.</web-summary>

<tldr>
<p>Module: <code>lang/number</code> · Disponible depuis: 1.11.0</p>
</tldr>

```typescript
function requireNonDouble<T>(
  value: T | number,
  message: string = "Expected a value without a fractional part."
): T;
```

Un synonyme de la famille `Float`, sous le nom auquel on pense d'abord. En JavaScript, tout nombre est déjà un double : `double` ne désigne aucun cas particulier à l'exécution.

`isX` et `nonX` répondent à une question sans jamais interrompre le flot ; `requireX` et `requireNonX` rendent la valeur ou lèvent. Le schéma est expliqué dans [](validation-conventions.md).

## Paramètres

| Paramètre                                                                  | Type          | Description                                                             |
| :------------------------------------------------------------------------- | :------------ | :---------------------------------------------------------------------- |
| `value`                                                                    | `T \| number` | La valeur à valider.                                                    |
| `message` _(facultatif)_ `= "Expected a value without a fractional part."` | `string`      | Le message de l'exception. À défaut, un message par défaut est utilisé. |

## Valeur de retour

`T` — la même valeur ; après le test, elle n'est pas un nombre avec une partie fractionnaire.

## Exceptions

| Exception                  | Condition                                              |
| :------------------------- | :----------------------------------------------------- |
| `IllegalArgumentException` | la valeur est un nombre avec une partie fractionnaire. |

## Exemples

### Ce qui passe et ce qui ne passe pas

```javascript
requireNonDouble(42); // => 42
requireNonDouble(1.0); // => 1.0
requireNonDouble(1.5); // throws IllegalArgumentException
requireNonDouble(-0.1); // throws IllegalArgumentException
```

## Voir aussi

- [`isDouble`](isDouble.md)
- [`nonDouble`](nonDouble.md)
- [`requireDouble`](requireDouble.md)
- [](reference-base.md)

## Code source

[`src/lang/number/requireNonDouble.ts`](https://github.com/MaksymStoianov/apps-script-utils/blob/main/src/lang/number/requireNonDouble.ts)
