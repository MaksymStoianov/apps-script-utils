<!-- Generated by scripts/docs/generate-topics.mjs. Edits are overwritten. Source: src/appsscript/sheet/nonValidSheetId.ts. -->

# nonValidSheetId

<link-summary>Vérifie qu'une valeur n'est **pas** un identifiant de feuille.</link-summary>

<web-summary>nonValidSheetId() — Vérifie qu'une valeur n'est **pas** un identifiant de feuille. apps-script-utils, Français.</web-summary>

<tldr>
<p>Module: <code>appsscript/sheet</code> · Disponible depuis: 1.11.0</p>
</tldr>

```typescript
function nonValidSheetId(value: unknown): boolean;
```

`nonValidSheetId(value)` est le même test que `!isValidSheetId(value)`, écrit pour qu'une clause de garde se lise comme une phrase plutôt que comme une négation.

Apps Script numérote les feuilles à partir de zéro : un identifiant de feuille est un entier sûr non négatif — la règle qu'applique `isCountable`. La première feuille d'un classeur porte l'identifiant `0`.

`isX` et `nonX` répondent à une question sans jamais interrompre le flot ; `requireX` et `requireNonX` rendent la valeur ou lèvent. Le schéma est expliqué dans [](validation-conventions.md).

## Paramètres

| Paramètre | Type      | Description           |
| :-------- | :-------- | :-------------------- |
| `value`   | `unknown` | La valeur à vérifier. |

## Valeur de retour

`boolean` — `true` si la valeur n'est pas un identifiant de feuille ; sinon `false`.

## Exemples

### Ce qui passe et ce qui ne passe pas

```javascript
nonValidSheetId(-1); // => true
nonValidSheetId(1.5); // => true
nonValidSheetId("0"); // => true
nonValidSheetId(0); // => false
nonValidSheetId(1234567890); // => false
```

## Voir aussi

- [`isValidSheetId`](isValidSheetId.md)
- [`requireValidSheetId`](requireValidSheetId.md)
- [](reference-appsscript.md)

## Code source

[`src/appsscript/sheet/nonValidSheetId.ts`](https://github.com/MaksymStoianov/apps-script-utils/blob/main/src/appsscript/sheet/nonValidSheetId.ts)
