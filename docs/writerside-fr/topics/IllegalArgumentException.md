<!-- Generated by scripts/docs/generate-topics.mjs. Edits are overwritten. Source: src/exception/IllegalArgumentException.ts. -->

# IllegalArgumentException

<link-summary>Un argument est d'un type inadapté ou hors de l'intervalle accepté.</link-summary>

<web-summary>IllegalArgumentException — Un argument est d'un type inadapté ou hors de l'intervalle accepté. apps-script-utils, Français.</web-summary>

<tldr>
<p>Module: <code>exception</code> · Disponible depuis: 1.0.0</p>
</tldr>

```typescript
class IllegalArgumentException extends RuntimeException
```

Un argument est d'un type inadapté ou hors de l'intervalle accepté. C'est ce que lèvent la plupart des gardes `requireX`.

Levée par [`requireArray`](requireArray.md), [`requireInteger`](requireInteger.md), [`requireNonEmpty`](requireNonEmpty.md).

Toute classe de cette hiérarchie accepte un message facultatif, une autre exception dont le message est repris, ou rien du tout. `isException` les reconnaît toutes ; un `Error` ordinaire n'en fait pas partie.

## Exemples

### Lever et intercepter

```javascript
throw new IllegalArgumentException("something specific about this call");

try {
  doWork();
} catch (error) {
  if (error instanceof IllegalArgumentException) {
    // handled
  }
}
```

## Voir aussi

- [`isException`](isException.md)
- [`requireException`](requireException.md)
- [](reference-exception.md)

## Code source

[`src/exception/IllegalArgumentException.ts`](https://github.com/MaksymStoianov/apps-script-utils/blob/main/src/exception/IllegalArgumentException.ts)
