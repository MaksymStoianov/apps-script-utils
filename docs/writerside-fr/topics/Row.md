<!-- Generated by scripts/docs/generate-topics.mjs. Edits are overwritten. Source: src/appsscript/sheet/getValues.ts. -->

# Row

<link-summary>Une ligne, telle que `getValues` la remet à un filtre ou à un mappeur.</link-summary>

<web-summary>Row — Une ligne, telle que `getValues` la remet à un filtre ou à un mappeur. apps-script-utils, Français.</web-summary>

<tldr>
<p>Module: <code>appsscript/sheet</code> · Disponible depuis: 1.11.0</p>
</tldr>

```typescript
interface Row
```

`values` ce sont les cellules de gauche à droite, `position` la ligne à base un sur la feuille — le numéro que l'on voit dans la marge — et la ligne indexée par les noms de colonnes lorsqu'une ligne d'en-tête a été désignée.

Disposer de la position est ce qui permet ensuite à un filtre de désigner une ligne précise ou d'agir sur elle.

## Exemples

### Forme

```javascript
// What a filter receives when `headerRow` is set.
const row = {
  values: ["7", "ada@example.com"],
  position: 2,
  record: { id: "7", email: "ada@example.com" }
};
```

## Voir aussi

- [`getValues`](getValues.md)
- [`RowPredicate`](RowPredicate.md)
- [](reference-appsscript.md)

## Code source

[`src/appsscript/sheet/getValues.ts`](https://github.com/MaksymStoianov/apps-script-utils/blob/main/src/appsscript/sheet/getValues.ts)
