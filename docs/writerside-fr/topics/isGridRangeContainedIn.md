<!-- Generated by scripts/docs/generate-topics.mjs. Edits are overwritten. Source: src/appsscript/sheet/isGridRangeContainedIn.ts. -->

# isGridRangeContainedIn

<link-summary>Vérifie si une plage de grille est entièrement contenue dans une autre.</link-summary>

<web-summary>isGridRangeContainedIn() — Vérifie si une plage de grille est entièrement contenue dans une autre. apps-script-utils, Français.</web-summary>

<tldr>
<p>Module: <code>appsscript/sheet</code> · Disponible depuis: 1.0.0</p>
</tldr>

```typescript
function isGridRangeContainedIn(gridRange: GridRange, containerGridRange: GridRange): boolean;
```

L'inclusion est large : une plage se contient elle-même. Une borne absente sur la plage extérieure signifie qu'elle est ouverte de ce côté et contient donc tout sur cet axe — c'est ainsi que s'expriment une colonne entière ou une feuille entière.

C'est la vérification à faire avant d'écrire dans une plage choisie par l'utilisateur, pour qu'une écriture ne déborde pas de la zone dont le script a la charge.

## Paramètres

| Paramètre            | Type        | Description                                                                                                         |
| :------------------- | :---------- | :------------------------------------------------------------------------------------------------------------------ |
| `gridRange`          | `GridRange` | The <a href="./types/GridRange.ts"><code>GridRange</code></a> object that is potentially a subset (child range).    |
| `containerGridRange` | `GridRange` | The <a href="./types/GridRange.ts"><code>GridRange</code></a> object that is potentially a superset (parent range). |

## Valeur de retour

`boolean` — `true` si la première plage est à l'intérieur de la seconde.

## Exemples

### Vérifier l'inclusion

```javascript
const inner = { startRowIndex: 1, endRowIndex: 2, startColumnIndex: 1, endColumnIndex: 2 };
const outer = { startRowIndex: 0, endRowIndex: 5, startColumnIndex: 0, endColumnIndex: 5 };

isGridRangeContainedIn(inner, outer); // => true
```

## Voir aussi

- [`nonGridRangeContainedIn`](nonGridRangeContainedIn.md)
- [`requireGridRangeContainedIn`](requireGridRangeContainedIn.md)
- [`doGridRangesIntersect`](doGridRangesIntersect.md)
- [](reference-appsscript.md)

## Code source

[`src/appsscript/sheet/isGridRangeContainedIn.ts`](https://github.com/MaksymStoianov/apps-script-utils/blob/main/src/appsscript/sheet/isGridRangeContainedIn.ts)
