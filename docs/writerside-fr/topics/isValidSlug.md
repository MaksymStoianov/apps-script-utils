<!-- Generated by scripts/docs/generate-topics.mjs. Edits are overwritten. Source: src/lang/string/isValidSlug.ts. -->

# isValidSlug

<link-summary>Vérifie si une valeur est un slug.</link-summary>

<web-summary>isValidSlug() — Vérifie si une valeur est un slug. apps-script-utils, Français.</web-summary>

<tldr>
<p>Module: <code>lang/string</code> · Disponible depuis: 1.0.0</p>
</tldr>

```typescript
function isValidSlug(value: string): boolean;
```

Commence par une lettre, puis lettres, chiffres, tirets et tirets bas — la forme que peut prendre un segment d'URL ou une clé. Les espaces, un chiffre initial et la chaîne vide sont refusés.

`isX` et `nonX` répondent à une question sans jamais interrompre le flot ; `requireX` et `requireNonX` rendent la valeur ou lèvent. Le schéma est expliqué dans [](validation-conventions.md).

## Paramètres

| Paramètre | Type     | Description           |
| :-------- | :------- | :-------------------- |
| `value`   | `string` | La valeur à vérifier. |

## Valeur de retour

`boolean` — `true` si la valeur est un slug ; sinon `false`.

## Exemples

### Ce qui passe et ce qui ne passe pas

```javascript
isValidSlug("my-page"); // => true
isValidSlug("a_1"); // => true
isValidSlug("1-page"); // => false
isValidSlug("my page"); // => false
isValidSlug(""); // => false
```

## Voir aussi

- [`nonValidSlug`](nonValidSlug.md)
- [`requireValidSlug`](requireValidSlug.md)
- [](reference-base.md)

## Code source

[`src/lang/string/isValidSlug.ts`](https://github.com/MaksymStoianov/apps-script-utils/blob/main/src/lang/string/isValidSlug.ts)
