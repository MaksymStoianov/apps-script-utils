<!-- Generated by scripts/docs/generate-topics.mjs. Edits are overwritten. Source: src/appsscript/base/getTriggerById.ts. -->

# getTriggerById

<link-summary>Знаходить тригер проєкту за ідентифікатором.</link-summary>

<web-summary>getTriggerById() — Знаходить тригер проєкту за ідентифікатором. apps-script-utils, Українська.</web-summary>

<tldr>
<p>Модуль: <code>appsscript/base</code> · Доступно з: 1.11.0</p>
</tldr>

```typescript
function getTriggerById(id: string): GoogleAppsScript.Script.Trigger | null;
```

Apps Script видає ідентифікатор під час створення тригера, але не дає способу знайти його потім, тому тригери проєкту перебираються та порівнюються. Уже видалений тригер дає `null` — саме цей випадок і має обробляти збережений ідентифікатор.

## Параметри

| Параметр | Тип      | Опис                                         |
| :------- | :------- | :------------------------------------------- |
| `id`     | `string` | Ідентифікатор тригера. Не має бути порожнім. |

## Повертає

`GoogleAppsScript.Script.Trigger | null` — тригер або `null`, якщо такого ідентифікатора в проєкту немає.

## Винятки

| Виняток                | Умова                                      |
| :--------------------- | :----------------------------------------- |
| `EmptyStringException` | ідентифікатор не рядок або рядок порожній. |

## Приклади

### Пошук

```javascript
const id = PropertiesService.getScriptProperties().getProperty("TRIGGER_ID");

const trigger = getTriggerById(id);

if (trigger !== null) {
  ScriptApp.deleteTrigger(trigger);
}
```

## Дивіться також

- [`requireNonEmptyString`](requireNonEmptyString.md)
- [](reference-appsscript.md)

## Вихідний код

[`src/appsscript/base/getTriggerById.ts`](https://github.com/MaksymStoianov/apps-script-utils/blob/main/src/appsscript/base/getTriggerById.ts)
