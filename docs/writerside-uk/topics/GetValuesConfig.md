<!-- Generated by scripts/docs/generate-topics.mjs. Edits are overwritten. Source: src/appsscript/sheet/getValues.ts. -->

# GetValuesConfig

<link-summary>Як `getValues` читає аркуш.</link-summary>

<web-summary>GetValuesConfig — Як `getValues` читає аркуш. apps-script-utils, Українська.</web-summary>

<tldr>
<p>Модуль: <code>appsscript/sheet</code> · Доступно з: 1.11.0</p>
</tldr>

```typescript
interface GetValuesConfig
```

`headerRow` вказує рядок з іменами стовпців і дає ці ключі кожному рядку. `display` читає значення так, як їх бачить людина, а не так, як вони зберігаються. `filter` лишає рядки, `offset` і `limit` розбивають решту на сторінки, `mapper` вирішує, на що перетворюється кожен.

Порядок фіксований: спершу фільтр, потім зсув, потім обмеження, потім перетворення, — тому ліміт рахує відповідні рядки, а не прочитані.

## Приклади

### Форма

```javascript
const config = {
  headerRow: 1,
  filter: (row) => row.record.status === "active",
  limit: 100
};
```

## Дивіться також

- [`getValues`](getValues.md)
- [`Row`](Row.md)
- [](reference-appsscript.md)

## Вихідний код

[`src/appsscript/sheet/getValues.ts`](https://github.com/MaksymStoianov/apps-script-utils/blob/main/src/appsscript/sheet/getValues.ts)
