<!-- Generated by scripts/docs/generate-topics.mjs. Edits are overwritten. Source: src/appsscript/sheet/deleteColumnsByConditional.ts. -->

# deleteColumnsByConditional

<link-summary>Видаляє стовпці, вибрані предикатом.</link-summary>

<web-summary>deleteColumnsByConditional() — Видаляє стовпці, вибрані предикатом. apps-script-utils, Українська.</web-summary>

<tldr>
<p>Модуль: <code>appsscript/sheet</code> · Доступно з: 1.11.0</p>
</tldr>

```typescript
function deleteColumnsByConditional(
  target: GoogleAppsScript.Spreadsheet.Sheet | GoogleAppsScript.Spreadsheet.Range,
  predicate: ColumnPredicate,
  options: ColumnConditionalOptions | null | undefined = {}
): number;
```

Оцінка йде за прочитаним аркушем, а видалення — від правого краю всередину суміжними блоками, тому зсув позицій не потягне за собою чужий стовпець.

Стовпці зникають назавжди: лише очищає їх `clearColumnsByConditional`.

Аркуш означає весь його діапазон даних, і стовпці видаляються цілком. Діапазон означає лише його клітинки: вони видаляються, клітинки праворуч зсуваються ліворуч, а рядки вище й нижче лишаються на місці. В обох випадках предикат отримує позицію на аркуші, а відмова спорожнити аркуш стосується форми з аркушем, де має лишитися хоча б один стовпець.

## Параметри

| Параметр                            | Тип                                                                        | Опис                                                                                                                                                                                  |
| :---------------------------------- | :------------------------------------------------------------------------- | :------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `target`                            | `GoogleAppsScript.Spreadsheet.Sheet \| GoogleAppsScript.Spreadsheet.Range` | Аркуш, з яким працюємо, або діапазон, усередині якого працюємо: читаються лише його клітинки, і видаляються лише вони.                                                                |
| `predicate`                         | `ColumnPredicate`                                                          | Отримує комірки стовпця, його позицію від одиниці та — якщо задано `headerColumn` — стовпець із ключами за іменами рядків. Поверніть `true` для стовпців, з якими треба щось зробити. |
| `options` _(необов'язковий)_ `= {}` | `ColumnConditionalOptions \| null \| undefined`                            | `headerColumn` вказує стовпець з іменами рядків і виводить цей стовпець з числа кандидатів.                                                                                           |

## Повертає

`number` — скількох це торкнулося.

## Винятки

| Виняток                    | Умова                                                      |
| :------------------------- | :--------------------------------------------------------- |
| `InvalidSheetException`    | перший аргумент не є ні аркушем, ні діапазоном.            |
| `IllegalArgumentException` | предикат не функція або позиція заголовка не додатне ціле. |

## Приклади

### Застосування

```javascript
const sheet = SpreadsheetApp.getActiveSheet();

deleteColumnsByConditional(sheet, (values, position, column) => column.internal === true, {
  headerColumn: 1
});
```

### Усередині одного блоку

```javascript
const sheet = SpreadsheetApp.getActiveSheet();

// Only B2:Z100 is read, and only those cells move left.
deleteColumnsByConditional(sheet.getRange("B2:Z100"), (values) =>
  values.every((cell) => cell === "")
);
```

## Дивіться також

- [`clearColumnsByConditional`](clearColumnsByConditional.md)
- [`deleteRowsByConditional`](deleteRowsByConditional.md)
- [Class Sheet](https://developers.google.com/apps-script/reference/spreadsheet/sheet)
- [Class Range](https://developers.google.com/apps-script/reference/spreadsheet/range)
- [](reference-appsscript.md)

## Вихідний код

[`src/appsscript/sheet/deleteColumnsByConditional.ts`](https://github.com/MaksymStoianov/apps-script-utils/blob/main/src/appsscript/sheet/deleteColumnsByConditional.ts)
