<!-- Generated by scripts/docs/generate-topics.mjs. Edits are overwritten. Source: src/exception/appsscript/sheet/InvalidRangeException.ts. -->

# InvalidRangeException

<link-summary>Очікувався діапазон, а значення ним не є.</link-summary>

<web-summary>InvalidRangeException — Очікувався діапазон, а значення ним не є. apps-script-utils, Українська.</web-summary>

<tldr>
<p>Модуль: <code>exception/appsscript/sheet</code> · Доступно з: 1.5.0</p>
</tldr>

```typescript
class InvalidRangeException extends RuntimeException
```

Очікувався діапазон, а значення ним не є.

Кидається функціями [`requireRange`](requireRange.md).

Будь-який клас цієї ієрархії приймає необов'язкове повідомлення, інший виняток, чиє повідомлення буде перевикористано, або взагалі нічого. `isException` розпізнає їх усі; звичайний `Error` до них не належить.

## Приклади

### Кинути й упіймати

```javascript
throw new InvalidRangeException("something specific about this call");

try {
  doWork();
} catch (error) {
  if (error instanceof InvalidRangeException) {
    // handled
  }
}
```

## Дивіться також

- [`isException`](isException.md)
- [`requireException`](requireException.md)
- [](reference-exception.md)

## Вихідний код

[`src/exception/appsscript/sheet/InvalidRangeException.ts`](https://github.com/MaksymStoianov/apps-script-utils/blob/main/src/exception/appsscript/sheet/InvalidRangeException.ts)
