<!-- Generated by scripts/docs/generate-topics.mjs. Edits are overwritten. Source: src/appsscript/sheet/getSchema.ts. -->

# getSchema

<link-summary>Повертає схему аркуша — збережену або виведену з його вмісту.</link-summary>

<web-summary>getSchema() — Повертає схему аркуша — збережену або виведену з його вмісту. apps-script-utils, Українська.</web-summary>

<tldr>
<p>Модуль: <code>appsscript/sheet</code> · Доступно з: 1.11.0</p>
</tldr>

```typescript
function getSchema(sheet: GoogleAppsScript.Spreadsheet.Sheet): SheetSchema | null;
```

Схема описує стовпці: їхні імена, типи та рядок, у якому лежать імена. Якщо `insertSchema` вже зберіг схему на аркуші, повертається саме вона.

Якщо нічого не збережено, аркуш читається і схема виводиться з рядка заголовків та значень під ним. У цьому разі в результаті стоїть `inferred: true`, і код, що викликає, відрізняє оголошений опис від вгаданого.

## Параметри

| Параметр | Тип                                  | Опис                    |
| :------- | :----------------------------------- | :---------------------- |
| `sheet`  | `GoogleAppsScript.Spreadsheet.Sheet` | Аркуш, з яким працюємо. |

## Повертає

`SheetSchema | null` — схема або `null`, якщо на аркуші нема чого описувати.

## Винятки

| Виняток                 | Умова                         |
| :---------------------- | :---------------------------- |
| `InvalidSheetException` | перший аргумент не є аркушем. |

## Приклади

### Читання схеми

```javascript
const sheet = SpreadsheetApp.getActiveSheet();

const schema = getSchema(sheet);

schema?.columns.map((column) => column.name);
```

## Дивіться також

- [`insertSchema`](insertSchema.md)
- [`removeSchema`](removeSchema.md)
- [`getValues`](getValues.md)
- [](reference-appsscript.md)

## Вихідний код

[`src/appsscript/sheet/getSchema.ts`](https://github.com/MaksymStoianov/apps-script-utils/blob/main/src/appsscript/sheet/getSchema.ts)
