<!-- Generated by scripts/docs/generate-topics.mjs. Edits are overwritten. Source: src/lang/base/nonRegExp.ts. -->

# nonRegExp

<link-summary>Перевіряє, що значення **не** є регулярним виразом.</link-summary>

<web-summary>nonRegExp() — Перевіряє, що значення **не** є регулярним виразом. apps-script-utils, Українська.</web-summary>

<tldr>
<p>Модуль: <code>lang/base</code> · Доступно з: 1.11.0</p>
</tldr>

```typescript
function nonRegExp<T>(value: T | RegExp): value is T;
```

`nonRegExp(value)` — та сама перевірка, що й `!isRegExp(value)`, записана так, щоб охоронна умова читалася як твердження, а не як заперечення.

Визначається за внутрішнім тегом, а не через `instanceof`, тому розпізнається й регулярний вираз з іншого фрейму.

`isX` та `nonX` відповідають на питання й ніколи не переривають виконання; `requireX` і `requireNonX` повертають значення або викидають виняток. Схему розібрано в [](validation-conventions.md).

## Параметри

| Параметр | Тип           | Опис                        |
| :------- | :------------ | :-------------------------- |
| `value`  | `T \| RegExp` | Значення, що перевіряється. |

## Повертає

`boolean` — `true`, якщо значення не є регулярним виразом; інакше `false`.

У TypeScript усередині гілки значення `value` звужується до `T`.

## Приклади

### Що проходить, а що ні

```javascript
nonRegExp("/a/"); // => true
nonRegExp({}); // => true
nonRegExp(null); // => true
nonRegExp(/a/); // => false
nonRegExp(new RegExp("a")); // => false
```

## Дивіться також

- [`isRegExp`](isRegExp.md)
- [`requireRegExp`](requireRegExp.md)
- [`requireNonRegExp`](requireNonRegExp.md)
- [](reference-base.md)

## Вихідний код

[`src/lang/base/nonRegExp.ts`](https://github.com/MaksymStoianov/apps-script-utils/blob/main/src/lang/base/nonRegExp.ts)
