<!-- Generated by scripts/docs/generate-topics.mjs. Edits are overwritten. Source: src/appsscript/sheet/appendRows.ts. -->

# appendRows

<link-summary>Додає рядки під уже наявними даними аркуша.</link-summary>

<web-summary>appendRows() — Додає рядки під уже наявними даними аркуша. apps-script-utils, Українська.</web-summary>

<tldr>
<p>Модуль: <code>appsscript/sheet</code> · Доступно з: 1.0.0</p>
</tldr>

```typescript
function appendRows(
  sheet: GoogleAppsScript.Spreadsheet.Sheet,
  values: unknown,
  options: AppendRowsOptions | null | undefined = {}
): GoogleAppsScript.Spreadsheet.Sheet;
```

Запис починається зі стовпця 1 першого нового рядка, і аркуш зростає рівно на стільки рядків, скільки їх у матриці. На відміну від `Sheet#appendRow`, який приймає по одному рядку, увесь блок пишеться одним викликом.

Комірка, текст якої починається з `=`, записується як формула, а не як текст — за тим самим правилом, що й у редакторі.

## Параметри

| Параметр                            | Тип                                      | Опис                                                                              |
| :---------------------------------- | :--------------------------------------- | :-------------------------------------------------------------------------------- |
| `sheet`                             | `GoogleAppsScript.Spreadsheet.Sheet`     | Аркуш, у який виконується запис.                                                  |
| `values`                            | `unknown`                                | Матриця рядків: по масиву на рядок, усі однієї довжини.                           |
| `options` _(необов'язковий)_ `= {}` | `AppendRowsOptions \| null \| undefined` | `afterFrozenRows` розміщує нові рядки одразу під закріпленими, а не в самий верх. |

## Повертає

`GoogleAppsScript.Spreadsheet.Sheet` — той самий аркуш, щоб виклики можна було складати в ланцюжок.

## Винятки

| Виняток                 | Умова                                               |
| :---------------------- | :-------------------------------------------------- |
| `InvalidSheetException` | перший аргумент не є аркушем.                       |
| `TypeError`             | значення не є матрицею з рядками однакової довжини. |

## Приклади

### Запис

```javascript
const sheet = SpreadsheetApp.getActiveSheet();

appendRows(sheet, [
  ["Ada", "ada@example.com"],
  ["Grace", "grace@example.com"]
]);
```

## Дивіться також

- [`appendRow`](appendRow.md)
- [`prependRows`](prependRows.md)
- [`requireConsistent2DArray`](requireConsistent2DArray.md)
- [](reference-appsscript.md)

## Вихідний код

[`src/appsscript/sheet/appendRows.ts`](https://github.com/MaksymStoianov/apps-script-utils/blob/main/src/appsscript/sheet/appendRows.ts)
