<!-- Generated by scripts/docs/generate-topics.mjs. Edits are overwritten. Source: src/exception/RuntimeException.ts. -->

# RuntimeException

<link-summary>Батько всіх винятків, які може спричинити робочий скрипт.</link-summary>

<web-summary>RuntimeException — Батько всіх винятків, які може спричинити робочий скрипт. apps-script-utils, Українська.</web-summary>

<tldr>
<p>Модуль: <code>exception</code> · Доступно з: 1.0.0</p>
</tldr>

```typescript
class RuntimeException extends Exception
```

Батько всіх винятків, які може спричинити робочий скрипт. Майже всі класи нижче успадковують його, а не `Exception` напряму.

Будь-який клас цієї ієрархії приймає необов'язкове повідомлення, інший виняток, чиє повідомлення буде перевикористано, або взагалі нічого. `isException` розпізнає їх усі; звичайний `Error` до них не належить.

## Приклади

### Кинути й упіймати

```javascript
throw new RuntimeException("something specific about this call");

try {
  doWork();
} catch (error) {
  if (error instanceof RuntimeException) {
    // handled
  }
}
```

## Дивіться також

- [`isException`](isException.md)
- [`requireException`](requireException.md)
- [](reference-exception.md)

## Вихідний код

[`src/exception/RuntimeException.ts`](https://github.com/MaksymStoianov/apps-script-utils/blob/main/src/exception/RuntimeException.ts)
