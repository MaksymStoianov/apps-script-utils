<!-- Generated by scripts/docs/generate-topics.mjs. Edits are overwritten. Source: src/lang/base/isNumber.ts. -->

# isNumber

<link-summary>Перевіряє, чи є значення числом.</link-summary>

<web-summary>isNumber() — Перевіряє, чи є значення числом. apps-script-utils, Українська.</web-summary>

<tldr>
<p>Модуль: <code>lang/base</code> · Доступно з: 1.0.0</p>
</tldr>

```typescript
function isNumber(value: unknown): value is number;
```

Перевіряється `typeof value === "number"`, чому задовольняють і `NaN`, і обидві нескінченності. Коли ці випадки треба розрізняти, беріть `isFloat`, `isInteger` або `isNaN`.

`isX` та `nonX` відповідають на питання й ніколи не переривають виконання; `requireX` і `requireNonX` повертають значення або викидають виняток. Схему розібрано в [](validation-conventions.md).

## Параметри

| Параметр | Тип       | Опис                        |
| :------- | :-------- | :-------------------------- |
| `value`  | `unknown` | Значення, що перевіряється. |

## Повертає

`boolean` — `true`, якщо значення є числом; інакше `false`.

У TypeScript усередині гілки значення `value` звужується до `number`.

## Приклади

### Що проходить, а що ні

```javascript
isNumber(0); // => true
isNumber(1.5); // => true
isNumber(NaN); // => true
isNumber(Infinity); // => true
isNumber("1"); // => false
isNumber(null); // => false
```

## Дивіться також

- [`nonNumber`](nonNumber.md)
- [`requireNumber`](requireNumber.md)
- [`requireNonNumber`](requireNonNumber.md)
- [](reference-base.md)

## Вихідний код

[`src/lang/base/isNumber.ts`](https://github.com/MaksymStoianov/apps-script-utils/blob/main/src/lang/base/isNumber.ts)
