<!-- Generated by scripts/docs/generate-topics.mjs. Edits are overwritten. Source: src/lang/base/isLength.ts. -->

# isLength

<link-summary>Перевіряє, чи є значення припустимою довжиною масиву.</link-summary>

<web-summary>isLength() — Перевіряє, чи є значення припустимою довжиною масиву. apps-script-utils, Українська.</web-summary>

<tldr>
<p>Модуль: <code>lang/base</code> · Доступно з: 1.0.0</p>
</tldr>

```typescript
function isLength(value: unknown): boolean;
```

Ціле число від `0` до `Number.MAX_SAFE_INTEGER` — діапазон, який може містити властивість `length`.

`isX` та `nonX` відповідають на питання й ніколи не переривають виконання; `requireX` і `requireNonX` повертають значення або викидають виняток. Схему розібрано в [](validation-conventions.md).

## Параметри

| Параметр | Тип       | Опис                        |
| :------- | :-------- | :-------------------------- |
| `value`  | `unknown` | Значення, що перевіряється. |

## Повертає

`boolean` — `true`, якщо значення є припустимою довжиною масиву; інакше `false`.

## Приклади

### Що проходить, а що ні

```javascript
isLength(0); // => true
isLength(42); // => true
isLength(Number.MAX_SAFE_INTEGER); // => true
isLength(-1); // => false
isLength(1.5); // => false
isLength("3"); // => false
```

## Дивіться також

- [`nonLength`](nonLength.md)
- [`requireLength`](requireLength.md)
- [`requireNonLength`](requireNonLength.md)
- [](reference-base.md)

## Вихідний код

[`src/lang/base/isLength.ts`](https://github.com/MaksymStoianov/apps-script-utils/blob/main/src/lang/base/isLength.ts)
