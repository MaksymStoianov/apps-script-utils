<!-- Generated by scripts/docs/generate-topics.mjs. Edits are overwritten. Source: src/lang/object/ObjectTag.ts. -->

# ObjectTag

<link-summary>Внутрішні теги, які може повідомити значення, у вигляді іменованих констант.</link-summary>

<web-summary>ObjectTag — Внутрішні теги, які може повідомити значення, у вигляді іменованих констант. apps-script-utils, Українська.</web-summary>

<tldr>
<p>Модуль: <code>lang/object</code> · Доступно з: 1.0.0</p>
</tldr>

```typescript
enum ObjectTag
```

`objectToString` повертає рядки на кшталт `"[object Array]"`; це перелічення дає їм імена, тому порівняння читається як `ObjectTag.ARRAY`, а не як літерал, що тихо ламається від одруківки.

На ньому побудовано кілька перевірок бібліотеки — саме так `isRegExp` і `isFunction` розпізнають значення, що прийшло з іншого фрейму, де `instanceof` уже не працює.

## Приклади

### Форма

```javascript
objectToString([]) === ObjectTag.ARRAY; // => true
objectToString(null) === ObjectTag.NULL; // => true
```

## Дивіться також

- [`objectToString`](objectToString.md)
- [`isRegExp`](isRegExp.md)
- [`isFunction`](isFunction.md)
- [](reference-base.md)

## Вихідний код

[`src/lang/object/ObjectTag.ts`](https://github.com/MaksymStoianov/apps-script-utils/blob/main/src/lang/object/ObjectTag.ts)
