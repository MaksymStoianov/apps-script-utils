<!-- Generated by scripts/docs/generate-topics.mjs. Edits are overwritten. Source: src/lang/number/nonSafeInteger.ts. -->

# nonSafeInteger

<link-summary>Перевіряє, що значення **не** є безпечним цілим числом.</link-summary>

<web-summary>nonSafeInteger() — Перевіряє, що значення **не** є безпечним цілим числом. apps-script-utils, Українська.</web-summary>

<tldr>
<p>Модуль: <code>lang/number</code> · Доступно з: 1.11.0</p>
</tldr>

```typescript
function nonSafeInteger<T>(value: T | number): value is T;
```

`nonSafeInteger(value)` — та сама перевірка, що й `!isSafeInteger(value)`, записана так, щоб охоронна умова читалася як твердження, а не як заперечення.

Ціле число, яке точно представне в IEEE-754. За межами цього діапазону різні математичні цілі схлопуються в одне значення, і арифметика непомітно перестає бути точною.

`isX` та `nonX` відповідають на питання й ніколи не переривають виконання; `requireX` і `requireNonX` повертають значення або викидають виняток. Схему розібрано в [](validation-conventions.md).

## Параметри

| Параметр | Тип           | Опис                        |
| :------- | :------------ | :-------------------------- |
| `value`  | `T \| number` | Значення, що перевіряється. |

## Повертає

`boolean` — `true`, якщо значення не є безпечним цілим числом; інакше `false`.

У TypeScript усередині гілки значення `value` звужується до `T`.

## Приклади

### Що проходить, а що ні

```javascript
nonSafeInteger(Number.MAX_SAFE_INTEGER + 2); // => true
nonSafeInteger(1.5); // => true
nonSafeInteger("42"); // => true
nonSafeInteger(42); // => false
nonSafeInteger(Number.MAX_SAFE_INTEGER); // => false
```

## Дивіться також

- [`isSafeInteger`](isSafeInteger.md)
- [`requireSafeInteger`](requireSafeInteger.md)
- [`requireNonSafeInteger`](requireNonSafeInteger.md)
- [](reference-base.md)

## Вихідний код

[`src/lang/number/nonSafeInteger.ts`](https://github.com/MaksymStoianov/apps-script-utils/blob/main/src/lang/number/nonSafeInteger.ts)
