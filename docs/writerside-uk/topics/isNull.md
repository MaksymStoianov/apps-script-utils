<!-- Generated by scripts/docs/generate-topics.mjs. Edits are overwritten. Source: src/lang/base/isNull.ts. -->

# isNull

<link-summary>Перевіряє, чи є значення значенням `null`.</link-summary>

<web-summary>isNull() — Перевіряє, чи є значення значенням `null`. apps-script-utils, Українська.</web-summary>

<tldr>
<p>Модуль: <code>lang/base</code> · Доступно з: 1.0.0</p>
</tldr>

```typescript
function isNull(value: unknown): value is null;
```

Строго `null`. `undefined` — окремий випадок, обидва відразу покриває `isNil`.

`isX` та `nonX` відповідають на питання й ніколи не переривають виконання; `requireX` і `requireNonX` повертають значення або викидають виняток. Схему розібрано в [](validation-conventions.md).

## Параметри

| Параметр | Тип       | Опис                        |
| :------- | :-------- | :-------------------------- |
| `value`  | `unknown` | Значення, що перевіряється. |

## Повертає

`boolean` — `true`, якщо значення є значенням `null`; інакше `false`.

У TypeScript усередині гілки значення `value` звужується до `null`.

## Приклади

### Що проходить, а що ні

```javascript
isNull(null); // => true
isNull(undefined); // => false
isNull(0); // => false
isNull(""); // => false
```

## Дивіться також

- [`nonNull`](nonNull.md)
- [`requireNull`](requireNull.md)
- [`requireNonNil`](requireNonNil.md)
- [](reference-base.md)

## Вихідний код

[`src/lang/base/isNull.ts`](https://github.com/MaksymStoianov/apps-script-utils/blob/main/src/lang/base/isNull.ts)
