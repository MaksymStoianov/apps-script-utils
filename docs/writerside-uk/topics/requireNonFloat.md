<!-- Generated by scripts/docs/generate-topics.mjs. Edits are overwritten. Source: src/lang/number/requireNonFloat.ts. -->

# requireNonFloat

<link-summary>Повертає значення, переконавшись, що воно не є числом з дробовою частиною.</link-summary>

<web-summary>requireNonFloat() — Повертає значення, переконавшись, що воно не є числом з дробовою частиною. apps-script-utils, Українська.</web-summary>

<tldr>
<p>Модуль: <code>lang/number</code> · Доступно з: 1.11.0</p>
</tldr>

```typescript
function requireNonFloat<T>(
  value: T | number,
  message: string = "Expected a value without a fractional part."
): T;
```

У JavaScript один числовий тип, тому `1.0` і `1` — одне значення: питання в тому, чи збереглася дріб під час виконання, а не в тому, як записано літерал. У нескінченностей дробу немає, вони виключені.

`isX` та `nonX` відповідають на питання й ніколи не переривають виконання; `requireX` і `requireNonX` повертають значення або викидають виняток. Схему розібрано в [](validation-conventions.md).

## Параметри

| Параметр                                                                       | Тип           | Опис                                                                        |
| :----------------------------------------------------------------------------- | :------------ | :-------------------------------------------------------------------------- |
| `value`                                                                        | `T \| number` | Значення, яке потрібно перевірити.                                          |
| `message` _(необов'язковий)_ `= "Expected a value without a fractional part."` | `string`      | Повідомлення винятку. Якщо не задано, використовується типове повідомлення. |

## Повертає

`T` — те саме значення; після перевірки відомо, що воно не є числом з дробовою частиною.

## Винятки

| Виняток                    | Умова                                  |
| :------------------------- | :------------------------------------- |
| `IllegalArgumentException` | значення є числом з дробовою частиною. |

## Приклади

### Що проходить, а що ні

```javascript
requireNonFloat(42); // => 42
requireNonFloat(1.0); // => 1.0
requireNonFloat(1.5); // throws IllegalArgumentException
requireNonFloat(-0.1); // throws IllegalArgumentException
```

## Дивіться також

- [`isFloat`](isFloat.md)
- [`nonFloat`](nonFloat.md)
- [`requireFloat`](requireFloat.md)
- [](reference-base.md)

## Вихідний код

[`src/lang/number/requireNonFloat.ts`](https://github.com/MaksymStoianov/apps-script-utils/blob/main/src/lang/number/requireNonFloat.ts)
