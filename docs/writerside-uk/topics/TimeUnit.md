<!-- Generated by scripts/docs/generate-topics.mjs. Edits are overwritten. Source: src/time/types/TimeUnit.ts. -->

# TimeUnit

<link-summary>Одиниця, в якій можна виразити інтервал.</link-summary>

<web-summary>TimeUnit — Одиниця, в якій можна виразити інтервал. apps-script-utils, Українська.</web-summary>

<tldr>
<p>Модуль: <code>time/types</code> · Доступно з: 1.11.0</p>
</tldr>

```typescript
type TimeUnit = "millisecond" | "second" | "minute" | "hour" | "day" | "month" | "year";
```

`"millisecond"`, `"second"`, `"minute"` та `"hour"` — фіксовані частки мілісекунди. `"day"`, `"month"` і `"year"` — календарні одиниці без сталої довжини: у добі під час переходу на літній час 23 або 25 годин, а в місяці від 28 до 31 дня.

Яке з двох трактувань застосовує конкретна функція, сказано на її сторінці: `diff` рахує цілі календарні одиниці, `offset` зсуває на них і за потреби підтягує день місяця.

## Приклади

### Форма

```javascript
diff(a, b, "day");
offset(date, 1, "month");
```

## Дивіться також

- [`diff`](diff.md)
- [`offset`](offset.md)
- [](reference-base.md)

## Вихідний код

[`src/time/types/TimeUnit.ts`](https://github.com/MaksymStoianov/apps-script-utils/blob/main/src/time/types/TimeUnit.ts)
