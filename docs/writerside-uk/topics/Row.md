<!-- Generated by scripts/docs/generate-topics.mjs. Edits are overwritten. Source: src/appsscript/sheet/getValues.ts. -->

# Row

<link-summary>Один рядок у тому вигляді, в якому `getValues` передає його фільтру чи перетворювачу.</link-summary>

<web-summary>Row — Один рядок у тому вигляді, в якому `getValues` передає його фільтру чи перетворювачу. apps-script-utils, Українська.</web-summary>

<tldr>
<p>Модуль: <code>appsscript/sheet</code> · Доступно з: 1.11.0</p>
</tldr>

```typescript
interface Row
```

`values` — комірки зліва направо, `position` — номер рядка на аркуші, рахуючи від одиниці, той самий, що людина бачить на полях, а також рядок із ключами за іменами стовпців, якщо рядок заголовків було вказано.

Наявність позиції й дозволяє фільтру потім послатися на конкретний рядок або щось із ним зробити.

## Приклади

### Форма

```javascript
// What a filter receives when `headerRow` is set.
const row = {
  values: ["7", "ada@example.com"],
  position: 2,
  record: { id: "7", email: "ada@example.com" }
};
```

## Дивіться також

- [`getValues`](getValues.md)
- [`RowPredicate`](RowPredicate.md)
- [](reference-appsscript.md)

## Вихідний код

[`src/appsscript/sheet/getValues.ts`](https://github.com/MaksymStoianov/apps-script-utils/blob/main/src/appsscript/sheet/getValues.ts)
