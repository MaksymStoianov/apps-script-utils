<!-- Generated by scripts/docs/generate-topics.mjs. Edits are overwritten. Source: src/lang/function/retry.ts. -->

# RetryOptions

<link-summary>Як `retry` чекає і коли здається.</link-summary>

<web-summary>RetryOptions — Як `retry` чекає і коли здається. apps-script-utils, Українська.</web-summary>

<tldr>
<p>Модуль: <code>lang/function</code></p>
</tldr>

```typescript
interface RetryOptions
```

`attempts` обмежує кількість спроб, `delay` — пауза перед другою, `multiplier` збільшує кожну наступну, `maxDelay` обмежує одну паузу.

`shouldRetry` отримує помилку та номер невдалої спроби й вирішує, чи варто взагалі повторювати; `sleep` виконує очікування й типово бере `Utilities.sleep`, де той є, — це й дозволяє тесту не чекати.

## Приклади

### Форма

```javascript
const options = {
  attempts: 4,
  delay: 500,
  multiplier: 2,
  maxDelay: 10000
};
```

## Дивіться також

- [`retry`](retry.md)
- [](reference-base.md)

## Вихідний код

[`src/lang/function/retry.ts`](https://github.com/MaksymStoianov/apps-script-utils/blob/main/src/lang/function/retry.ts)
