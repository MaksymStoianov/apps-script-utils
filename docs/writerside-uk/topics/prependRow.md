<!-- Generated by scripts/docs/generate-topics.mjs. Edits are overwritten. Source: src/appsscript/sheet/prependRow.ts. -->

# prependRow

<link-summary>Вставляє один рядок над уже наявними даними аркуша.</link-summary>

<web-summary>prependRow() — Вставляє один рядок над уже наявними даними аркуша. apps-script-utils, Українська.</web-summary>

<tldr>
<p>Модуль: <code>appsscript/sheet</code> · Доступно з: 1.0.0</p>
</tldr>

```typescript
function prependRow(
  sheet: GoogleAppsScript.Spreadsheet.Sheet,
  values: unknown,
  options: PrependRowsOptions | null | undefined = {}
): GoogleAppsScript.Spreadsheet.Sheet;
```

`prependRows` для одного рядка. Значення передаються плоским масивом комірок, а не матрицею.

Комірка, текст якої починається з `=`, записується як формула, а не як текст — за тим самим правилом, що й у редакторі.

## Параметри

| Параметр                            | Тип                                       | Опис                                                                              |
| :---------------------------------- | :---------------------------------------- | :-------------------------------------------------------------------------------- |
| `sheet`                             | `GoogleAppsScript.Spreadsheet.Sheet`      | Аркуш, у який виконується запис.                                                  |
| `values`                            | `unknown`                                 | Комірки одного рядка.                                                             |
| `options` _(необов'язковий)_ `= {}` | `PrependRowsOptions \| null \| undefined` | `afterFrozenRows` розміщує нові рядки одразу під закріпленими, а не в самий верх. |

## Повертає

`GoogleAppsScript.Spreadsheet.Sheet` — той самий аркуш, щоб виклики можна було складати в ланцюжок.

## Винятки

| Виняток                 | Умова                                               |
| :---------------------- | :-------------------------------------------------- |
| `InvalidSheetException` | перший аргумент не є аркушем.                       |
| `TypeError`             | значення не є матрицею з рядками однакової довжини. |

## Приклади

### Запис

```javascript
const sheet = SpreadsheetApp.getActiveSheet();

prependRow(sheet, ["id", "name", "email"]);
```

## Дивіться також

- [`prependRows`](prependRows.md)
- [`appendRow`](appendRow.md)
- [](reference-appsscript.md)

## Вихідний код

[`src/appsscript/sheet/prependRow.ts`](https://github.com/MaksymStoianov/apps-script-utils/blob/main/src/appsscript/sheet/prependRow.ts)
