<!-- Generated by scripts/docs/generate-topics.mjs. Edits are overwritten. Source: src/appsscript/sheet/removeSchema.ts. -->

# removeSchema

<link-summary>Видаляє схему, збережену на аркуші.</link-summary>

<web-summary>removeSchema() — Видаляє схему, збережену на аркуші. apps-script-utils, Українська.</web-summary>

<tldr>
<p>Модуль: <code>appsscript/sheet</code> · Доступно з: 1.11.0</p>
</tldr>

```typescript
function removeSchema(
  sheet: GoogleAppsScript.Spreadsheet.Sheet,
  options: RemoveSchemaOptions | null | undefined = {}
): boolean;
```

Збережений опис видаляється; дані та рядок заголовків лишаються точно як були. Аркуш без збереженої схеми не чіпається, і виклик повертає `false`.

`validation: true` додатково знімає перевірку даних зі стовпців, які описувала схема. Типово вимкнено навмисно: Apps Script не дозволяє відрізнити правило, створене схемою, від доданого людиною, тому знімаються обидва.

## Параметри

| Параметр                            | Тип                                        | Опис                                                                  |
| :---------------------------------- | :----------------------------------------- | :-------------------------------------------------------------------- |
| `sheet`                             | `GoogleAppsScript.Spreadsheet.Sheet`       | Аркуш, з яким працюємо.                                               |
| `options` _(необов'язковий)_ `= {}` | `RemoveSchemaOptions \| null \| undefined` | `validation` додатково знімає перевірку даних, що випливала зі схеми. |

## Повертає

`boolean` — `true`, якщо схему було видалено.

## Винятки

| Виняток                 | Умова                         |
| :---------------------- | :---------------------------- |
| `InvalidSheetException` | перший аргумент не є аркушем. |

## Приклади

### Видалення схеми

```javascript
const sheet = SpreadsheetApp.getActiveSheet();

removeSchema(sheet); // => true when one was there
removeSchema(sheet, { validation: true }); // also clears the rules it implied
```

## Дивіться також

- [`getSchema`](getSchema.md)
- [`insertSchema`](insertSchema.md)
- [](reference-appsscript.md)

## Вихідний код

[`src/appsscript/sheet/removeSchema.ts`](https://github.com/MaksymStoianov/apps-script-utils/blob/main/src/appsscript/sheet/removeSchema.ts)
