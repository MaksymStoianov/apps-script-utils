<!-- Generated by scripts/docs/generate-topics.mjs. Edits are overwritten. Source: src/appsscript/sheet/prependColumn.ts. -->

# prependColumn

<link-summary>Вставляє один стовпець ліворуч від уже наявних даних аркуша.</link-summary>

<web-summary>prependColumn() — Вставляє один стовпець ліворуч від уже наявних даних аркуша. apps-script-utils, Українська.</web-summary>

<tldr>
<p>Модуль: <code>appsscript/sheet</code> · Доступно з: 1.11.0</p>
</tldr>

```typescript
function prependColumn(
  sheet: GoogleAppsScript.Spreadsheet.Sheet,
  values?: unknown[] | null,
  options: Options | null | undefined = {}
): GoogleAppsScript.Spreadsheet.Sheet;
```

`prependColumns` для одного стовпця. Значення передаються плоским масивом комірок, згори вниз.

Комірка, текст якої починається з `=`, записується як формула, а не як текст — за тим самим правилом, що й у редакторі.

## Параметри

| Параметр                            | Тип                                  | Опис                                                                              |
| :---------------------------------- | :----------------------------------- | :-------------------------------------------------------------------------------- |
| `sheet`                             | `GoogleAppsScript.Spreadsheet.Sheet` | Аркуш, у який виконується запис.                                                  |
| `values` _(необов'язковий)_         | `unknown[] \| null`                  | Комірки одного рядка.                                                             |
| `options` _(необов'язковий)_ `= {}` | `Options \| null \| undefined`       | `afterFrozenRows` розміщує нові рядки одразу під закріпленими, а не в самий верх. |

## Повертає

`GoogleAppsScript.Spreadsheet.Sheet` — той самий аркуш, щоб виклики можна було складати в ланцюжок.

## Винятки

| Виняток                 | Умова                                               |
| :---------------------- | :-------------------------------------------------- |
| `InvalidSheetException` | перший аргумент не є аркушем.                       |
| `TypeError`             | значення не є матрицею з рядками однакової довжини. |

## Приклади

### Запис

```javascript
const sheet = SpreadsheetApp.getActiveSheet();

prependColumn(sheet, ["#", "1", "2"]);
```

## Дивіться також

- [`prependColumns`](prependColumns.md)
- [`appendColumn`](appendColumn.md)
- [](reference-appsscript.md)

## Вихідний код

[`src/appsscript/sheet/prependColumn.ts`](https://github.com/MaksymStoianov/apps-script-utils/blob/main/src/appsscript/sheet/prependColumn.ts)
