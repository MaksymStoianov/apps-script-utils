<!-- Generated by scripts/docs/generate-topics.mjs. Edits are overwritten. Source: src/appsscript/sheet/types/SheetSchema.ts. -->

# SheetSchema

<link-summary>Як називаються стовпці аркуша і що в них лежить.</link-summary>

<web-summary>SheetSchema — Як називаються стовпці аркуша і що в них лежить. apps-script-utils, Українська.</web-summary>

<tldr>
<p>Модуль: <code>appsscript/sheet/types</code> · Доступно з: 1.11.0</p>
</tldr>

```typescript
interface SheetSchema
```

`version` позначає формат, щоб пізнішу версію можна було розпізнати, а не прочитати хибно; `headerRow` вказує рядок з іменами; `columns` описує стовпці зліва направо.

`getSchema` ставить `inferred: true`, якщо нічого не було збережено й опис прочитано з самого вмісту аркуша, — це здогад, а не оголошення.

## Приклади

### Форма

```javascript
const schema = {
  version: 1,
  headerRow: 1,
  columns: [
    { name: "id", type: "number" },
    { name: "email", type: "string" }
  ]
};
```

## Дивіться також

- [`getSchema`](getSchema.md)
- [`insertSchema`](insertSchema.md)
- [`SheetColumnSchema`](SheetColumnSchema.md)
- [](reference-appsscript.md)

## Вихідний код

[`src/appsscript/sheet/types/SheetSchema.ts`](https://github.com/MaksymStoianov/apps-script-utils/blob/main/src/appsscript/sheet/types/SheetSchema.ts)
