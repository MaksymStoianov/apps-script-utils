<!-- Generated by scripts/docs/generate-topics.mjs. Edits are overwritten. Source: src/appsscript/sheet/clearColumnsByConditional.ts. -->

# clearColumnsByConditional

<link-summary>Очищає стовпці, вибрані предикатом, не видаляючи самі стовпці.</link-summary>

<web-summary>clearColumnsByConditional() — Очищає стовпці, вибрані предикатом, не видаляючи самі стовпці. apps-script-utils, Українська.</web-summary>

<tldr>
<p>Модуль: <code>appsscript/sheet</code> · Доступно з: 1.11.0</p>
</tldr>

```typescript
function clearColumnsByConditional(
  sheet: GoogleAppsScript.Spreadsheet.Sheet,
  predicate: ColumnPredicate,
  options: ColumnConditionalOptions | null | undefined = {}
): number;
```

Те саме, що `clearRowsByConditional`, але по стовпцях: вміст зникає, стовпці лишаються, тому літери наступних стовпців не змінюються.

Аркуш читається один раз, очищення групується в суміжні блоки.

## Параметри

| Параметр                            | Тип                                             | Опис                                                                                                                                                                                  |
| :---------------------------------- | :---------------------------------------------- | :------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `sheet`                             | `GoogleAppsScript.Spreadsheet.Sheet`            | Аркуш, з яким працюємо.                                                                                                                                                               |
| `predicate`                         | `ColumnPredicate`                               | Отримує комірки стовпця, його позицію від одиниці та — якщо задано `headerColumn` — стовпець із ключами за іменами рядків. Поверніть `true` для стовпців, з якими треба щось зробити. |
| `options` _(необов'язковий)_ `= {}` | `ColumnConditionalOptions \| null \| undefined` | `headerColumn` вказує стовпець з іменами рядків і виводить цей стовпець з числа кандидатів.                                                                                           |

## Повертає

`number` — скількох це торкнулося.

## Винятки

| Виняток                    | Умова                                                      |
| :------------------------- | :--------------------------------------------------------- |
| `InvalidSheetException`    | перший аргумент не є аркушем.                              |
| `IllegalArgumentException` | предикат не функція або позиція заголовка не додатне ціле. |

## Приклади

### Застосування

```javascript
const sheet = SpreadsheetApp.getActiveSheet();

clearColumnsByConditional(sheet, (values) => values.every((cell) => cell === ""));
```

## Дивіться також

- [`deleteColumnsByConditional`](deleteColumnsByConditional.md)
- [`clearRowsByConditional`](clearRowsByConditional.md)
- [](reference-appsscript.md)

## Вихідний код

[`src/appsscript/sheet/clearColumnsByConditional.ts`](https://github.com/MaksymStoianov/apps-script-utils/blob/main/src/appsscript/sheet/clearColumnsByConditional.ts)
