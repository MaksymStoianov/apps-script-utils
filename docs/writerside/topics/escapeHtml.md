<!-- Generated by scripts/docs/generate-topics.mjs. Edits are overwritten. Source: src/html/escapeHtml.ts. -->

# escapeHtml

<link-summary>Replaces the five characters that would otherwise be read as markup.</link-summary>

<web-summary>escapeHtml() — Replaces the five characters that would otherwise be read as markup. apps-script-utils, English.</web-summary>

<tldr>
<p>Module: <code>html</code> · Since: 1.0.0</p>
</tldr>

```typescript
function escapeHtml(value: string): string;
```

`<`, `>`, `&`, `"` and `'` become named entities. This is what to call on anything a user supplied before it reaches an `HtmlOutput` or a template.

## Parameters

| Parameter | Type     | Description                                  |
| :-------- | :------- | :------------------------------------------- |
| `value`   | `string` | The string to convert. It must not be empty. |

## Returns

`string` — the escaped string.

## Throws

| Exception              | Condition                               |
| :--------------------- | :-------------------------------------- |
| `EmptyStringException` | the value is not a string, or is empty. |

## Examples

### Converting

```javascript
escapeHtml("<a>&"); // => "&lt;a&gt;&amp;"
```

## See also

- [`encodeHtml`](encodeHtml.md)
- [`escapeXml`](escapeXml.md)
- [`merge`](merge.md)
- [](reference-base.md)

## Source

[`src/html/escapeHtml.ts`](https://github.com/MaksymStoianov/apps-script-utils/blob/main/src/html/escapeHtml.ts)
