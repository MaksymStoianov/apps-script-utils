<!-- Generated by scripts/docs/generate-topics.mjs. Edits are overwritten. Source: src/lang/array/compact.ts. -->

# compact

<link-summary>Removes every falsy value from an array and narrows the element type without a cast.</link-summary>

<web-summary>compact() — Removes every falsy value from an array and narrows the element type without a cast. apps-script-utils, English.</web-summary>

<tldr>
<p>Module: <code>lang/array</code> · Since: 1.11.0</p>
</tldr>

```typescript
function compact<T>(array: Array<T | Falsy>): T[];
```

`compact` drops `false`, `null`, `undefined`, `0`, `-0`, `0n`, `""` and `NaN`, and returns what is left in a new
array. The input is not modified.

The point of the function over a plain `array.filter(Boolean)` is the type: TypeScript keeps the original element
type through `filter(Boolean)`, so the caller has to assert the nulls away afterwards. `compact` returns `T[]`
without a cast.

Note that `0` and `""` count as falsy. For values read out of a spreadsheet that is usually the wrong tool — `0` is
a legitimate number and an empty cell arrives as `""` — and `array.filter(nonNil)` removes only the absent values.

## Parameters

| Parameter | Type                | Description |
| :-------- | :------------------ | :---------- |
| `array`   | `Array<T \| Falsy>` | Falsy>`     |

## Returns

`T[]` — a new array holding only the truthy values, in their original order.

## Throws

| Exception   | Condition                |
| :---------- | :----------------------- |
| `TypeError` | `array` is not an array. |

## Examples

### Dropping the falsy values

```javascript
compact([0, 1, false, 2, "", 3, null]); // => [1, 2, 3]
compact([NaN, undefined]); // => []
compact([0, ""]); // => [] — both are falsy
```

### Reading a column without the blanks

The column arrives as `Array<string | null>` and leaves as `string[]`, with no cast in between.

<tabs group="code">
<tab title="TypeScript" group-key="ts">
<code-block lang="typescript" src="compact.ts" include-symbol="readIds"/>
</tab>
<tab title="JavaScript" group-key="js">
<code-block lang="javascript">
function readIds(sheet) {
  const column = sheet.getRange("A2:A").getValues().flat();
  return compact(column);
}
</code-block>
</tab>
</tabs>

### When `nonNil` is the better filter

```typescript
import { nonNil } from "apps-script-utils";

const cells = [0, "", 5, null]; // a column read from a sheet

compact(cells); // => [5] — drops the legitimate 0 and the empty cell
cells.filter(nonNil); // => [0, "", 5] — drops only the absent values
```

## See also

- [`without`](without.md) — removes the values you name, rather than the falsy ones.
- [](reference-base.md)

## Source

[`src/lang/array/compact.ts`](https://github.com/MaksymStoianov/apps-script-utils/blob/main/src/lang/array/compact.ts)
