<!-- Generated by scripts/docs/generate-topics.mjs. Edits are overwritten. Source: src/appsscript/sheet/getValues.ts. -->

# GetValuesConfig

<link-summary>How `getValues` reads a sheet.</link-summary>

<web-summary>GetValuesConfig — How `getValues` reads a sheet. apps-script-utils, English.</web-summary>

<tldr>
<p>Module: <code>appsscript/sheet</code> · Since: 1.11.0</p>
</tldr>

```typescript
interface GetValuesConfig
```

`headerRow` names the row of column names and keys every row by them. `display` reads the values as a person sees them rather than as they are stored. `filter` keeps rows, `offset` and `limit` page what is kept, and `mapper` decides what each one becomes.

The order is fixed: filter, then offset, then limit, then mapper — so a limit counts matching rows, not rows read.

## Examples

### Shape

```javascript
const config = {
  headerRow: 1,
  filter: (row) => row.record.status === "active",
  limit: 100
};
```

## See also

- [`getValues`](getValues.md)
- [`Row`](Row.md)
- [](reference-appsscript.md)

## Source

[`src/appsscript/sheet/getValues.ts`](https://github.com/MaksymStoianov/apps-script-utils/blob/main/src/appsscript/sheet/getValues.ts)
