<!-- Generated by scripts/docs/generate-topics.mjs. Edits are overwritten. Source: src/lang/base/isFunctionLike.ts. -->

# isFunctionLike

<link-summary>Checks whether a value is callable.</link-summary>

<web-summary>isFunctionLike() — Checks whether a value is callable. apps-script-utils, English.</web-summary>

<tldr>
<p>Module: <code>lang/base</code> · Since: 1.2.0</p>
</tldr>

```typescript
function isFunctionLike(value: unknown): value is Function;
```

The cheap test — `typeof value === "function"` — which is what you want before calling something. `isFunction` is the stricter reading of the same question.

`isX` and `nonX` answer a question and never interrupt control flow; `requireX` and `requireNonX` hand the value back or throw. The scheme is explained in [](validation-conventions.md).

## Parameters

| Parameter | Type      | Description         |
| :-------- | :-------- | :------------------ |
| `value`   | `unknown` | The value to check. |

## Returns

`boolean` — `true` when the value is callable; otherwise `false`.

In TypeScript the guard narrows `value` to `Function` inside the branch.

## Examples

### What passes and what does not

```javascript
isFunctionLike(function () {}); // => true
isFunctionLike(Math.max); // => true
isFunctionLike(null); // => false
isFunctionLike({}); // => false
isFunctionLike("fn"); // => false
```

## See also

- [`nonFunctionLike`](nonFunctionLike.md)
- [`requireFunctionLike`](requireFunctionLike.md)
- [`requireNonFunctionLike`](requireNonFunctionLike.md)
- [](reference-base.md)

## Source

[`src/lang/base/isFunctionLike.ts`](https://github.com/MaksymStoianov/apps-script-utils/blob/main/src/lang/base/isFunctionLike.ts)
