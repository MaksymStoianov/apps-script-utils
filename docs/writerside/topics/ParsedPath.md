<!-- Generated by scripts/docs/generate-topics.mjs. Edits are overwritten. Source: src/net/path/types/ParsedPath.ts. -->

# ParsedPath

<link-summary>The parts a path breaks into.</link-summary>

<web-summary>ParsedPath — The parts a path breaks into. apps-script-utils, English.</web-summary>

<tldr>
<p>Module: <code>net/path/types</code> · Since: 0.1.0</p>
</tldr>

```typescript
type ParsedPath =
```

`root` is the leading separator or scheme, `dir` everything up to the last separator, `base` the file name with its extension, `name` the same without it, and `ext` the extension including its dot.

Every part is optional, and a path that lacks one comes back without that key rather than with an empty string — so a bare file name yields `base`, `name` and `ext` alone. It is the shape Node's `path.parse` returns, which is what makes the two interchangeable in code that has to run in both places.

## Examples

### Shape

```javascript
parse("/a/b/c.txt");
// => { root: "/", dir: "/a/b", base: "c.txt", name: "c", ext: ".txt" }

parse("readme.md");
// => { base: "readme.md", name: "readme", ext: ".md" }
```

## See also

- [`parse`](parse.md)
- [`join`](join.md)
- [`normalize`](normalize.md)
- [](reference-path.md)

## Source

[`src/net/path/types/ParsedPath.ts`](https://github.com/MaksymStoianov/apps-script-utils/blob/main/src/net/path/types/ParsedPath.ts)
