<!-- Generated by scripts/docs/generate-topics.mjs. Edits are overwritten. Source: src/lang/string/nonValidVersion.ts. -->

# nonValidVersion

<link-summary>Checks whether a value is **not** a version string.</link-summary>

<web-summary>nonValidVersion() — Checks whether a value is **not** a version string. apps-script-utils, English.</web-summary>

<tldr>
<p>Module: <code>lang/string</code> · Since: 1.11.0</p>
</tldr>

```typescript
function nonValidVersion(value: string): boolean;
```

`nonValidVersion(value)` is the same test as `!isValidVersion(value)`, written so that a guard clause reads as a sentence rather than as a negation.

Dot-separated groups of digits, any number of them. A pre-release suffix, a leading `v` and an empty group are all rejected, so `"1.2.3-beta"` does not pass even though SemVer allows it.

`isX` and `nonX` answer a question and never interrupt control flow; `requireX` and `requireNonX` hand the value back or throw. The scheme is explained in [](validation-conventions.md).

## Parameters

| Parameter | Type     | Description         |
| :-------- | :------- | :------------------ |
| `value`   | `string` | The value to check. |

## Returns

`boolean` — `true` when the value is not a version string; otherwise `false`.

## Examples

### What passes and what does not

```javascript
nonValidVersion("1.2.3-beta"); // => true
nonValidVersion("v1.2"); // => true
nonValidVersion("1..2"); // => true
nonValidVersion("1"); // => false
nonValidVersion("1.2.3"); // => false
nonValidVersion("2026.01.15"); // => false
```

## See also

- [`isValidVersion`](isValidVersion.md)
- [`requireValidVersion`](requireValidVersion.md)
- [](reference-base.md)

## Source

[`src/lang/string/nonValidVersion.ts`](https://github.com/MaksymStoianov/apps-script-utils/blob/main/src/lang/string/nonValidVersion.ts)
