<!-- Generated by scripts/docs/generate-topics.mjs. Edits are overwritten. Source: src/lang/string/toSnakeCase.ts. -->

# toSnakeCase

<link-summary>Rewrites a string in snake_case.</link-summary>

<web-summary>toSnakeCase() — Rewrites a string in snake_case. apps-script-utils, English.</web-summary>

<tldr>
<p>Module: <code>lang/string</code> · Since: 1.0.0</p>
</tldr>

```typescript
function toSnakeCase(value: string, options?: ToSnakeCaseOptions): string;
```

The kebab-case rules with underscores in place of hyphens: spaces and hyphens become underscores, an underscore is inserted where a lower-case letter meets an upper-case one, and the result is lowered.

`clean` removes anything that is not a letter, a digit or an underscore; `trim` collapses the surrounding whitespace first.

## Parameters

| Parameter              | Type                 | Description                                                                       |
| :--------------------- | :------------------- | :-------------------------------------------------------------------------------- |
| `value`                | `string`             | The string to convert. It must not be empty.                                      |
| `options` _(optional)_ | `ToSnakeCaseOptions` | `clean` removes the remaining punctuation; `trim` trims and collapses whitespace. |

## Returns

`string` — the converted string.

## Throws

| Exception              | Condition                               |
| :--------------------- | :-------------------------------------- |
| `EmptyStringException` | the value is not a string, or is empty. |

## Examples

### Converting

```javascript
toSnakeCase("Hello World"); // => "hello_world"
toSnakeCase("userFirstName"); // => "user_first_name"
```

## See also

- [`toKebabCase`](toKebabCase.md)
- [`toCamelCase`](toCamelCase.md)
- [](reference-base.md)

## Source

[`src/lang/string/toSnakeCase.ts`](https://github.com/MaksymStoianov/apps-script-utils/blob/main/src/lang/string/toSnakeCase.ts)
