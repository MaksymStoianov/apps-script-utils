<!-- Generated by scripts/docs/generate-topics.mjs. Edits are overwritten. Source: src/lang/string/merge.ts. -->

# MergeOptions

<link-summary>How `merge` fills a template.</link-summary>

<web-summary>MergeOptions — How `merge` fills a template. apps-script-utils, English.</web-summary>

<tldr>
<p>Module: <code>lang/string</code> · Since: 1.11.0</p>
</tldr>

```typescript
interface MergeOptions
```

`onMissing` decides what happens to a placeholder whose key is absent: `"keep"`, the default, leaves it in the output where it is visible and traceable; `"empty"` substitutes nothing, which is tidier in something a reader will see.

`escape` is a function applied to every substituted value — `escapeHtml` when the result goes into a page.

## Examples

### Shape

```javascript
const options = { onMissing: "empty", escape: escapeHtml };
```

## See also

- [`merge`](merge.md)
- [`escapeHtml`](escapeHtml.md)
- [](reference-base.md)

## Source

[`src/lang/string/merge.ts`](https://github.com/MaksymStoianov/apps-script-utils/blob/main/src/lang/string/merge.ts)
