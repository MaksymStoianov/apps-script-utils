<!-- Generated by scripts/docs/generate-topics.mjs. Edits are overwritten. Source: src/lang/object/ObjectTag.ts. -->

# ObjectTag

<link-summary>The internal tags a value can report, as named constants.</link-summary>

<web-summary>ObjectTag — The internal tags a value can report, as named constants. apps-script-utils, English.</web-summary>

<tldr>
<p>Module: <code>lang/object</code> · Since: 1.0.0</p>
</tldr>

```typescript
enum ObjectTag
```

`objectToString` returns strings like `"[object Array]"`; this enum gives those strings names, so a comparison reads as `ObjectTag.ARRAY` rather than as a literal that a typo can quietly break.

Several guards in the library are built on it — it is how `isRegExp` and `isFunction` recognise a value that crossed a frame boundary, where `instanceof` no longer holds.

## Examples

### Shape

```javascript
objectToString([]) === ObjectTag.ARRAY; // => true
objectToString(null) === ObjectTag.NULL; // => true
```

## See also

- [`objectToString`](objectToString.md)
- [`isRegExp`](isRegExp.md)
- [`isFunction`](isFunction.md)
- [](reference-base.md)

## Source

[`src/lang/object/ObjectTag.ts`](https://github.com/MaksymStoianov/apps-script-utils/blob/main/src/lang/object/ObjectTag.ts)
