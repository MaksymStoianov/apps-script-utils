<!-- Generated by scripts/docs/generate-topics.mjs. Edits are overwritten. Source: src/appsscript/sheet/toA1Notation.ts. -->

# toA1Notation

<link-summary>Writes a grid range as A1 notation.</link-summary>

<web-summary>toA1Notation() — Writes a grid range as A1 notation. apps-script-utils, English.</web-summary>

<tldr>
<p>Module: <code>appsscript/sheet</code> · Since: 1.0.0</p>
</tldr>

```typescript
function toA1Notation(gridRange: GridRange): string;
```

The bounds are read the way the Sheets API means them: the start indices are inclusive, the end indices are not. A range one row and one column wide is written as a single cell, `"A1"`, rather than `"A1:A1"`.

An index left out means the range is open in that direction, which comes back as A1 notation without that bound — a whole column or a whole row.

## Parameters

| Parameter   | Type        | Description                                        |
| :---------- | :---------- | :------------------------------------------------- |
| `gridRange` | `GridRange` | The [`GridRange`](GridRange.md) object to convert. |

## Returns

`string` — the A1 notation.

## Examples

### Writing A1 notation

```javascript
toA1Notation({ startRowIndex: 0, endRowIndex: 2, startColumnIndex: 0, endColumnIndex: 2 }); // => "A1:B2"
toA1Notation({ startRowIndex: 0, endRowIndex: 1, startColumnIndex: 0, endColumnIndex: 1 }); // => "A1"
```

## See also

- [`parseA1Notation`](parseA1Notation.md)
- [`getColumnLetterByIndex`](getColumnLetterByIndex.md)
- [](reference-appsscript.md)

## Source

[`src/appsscript/sheet/toA1Notation.ts`](https://github.com/MaksymStoianov/apps-script-utils/blob/main/src/appsscript/sheet/toA1Notation.ts)
