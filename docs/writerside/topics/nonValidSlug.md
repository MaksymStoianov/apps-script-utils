<!-- Generated by scripts/docs/generate-topics.mjs. Edits are overwritten. Source: src/lang/string/nonValidSlug.ts. -->

# nonValidSlug

<link-summary>Checks whether a value is **not** a slug.</link-summary>

<web-summary>nonValidSlug() — Checks whether a value is **not** a slug. apps-script-utils, English.</web-summary>

<tldr>
<p>Module: <code>lang/string</code> · Since: 1.11.0</p>
</tldr>

```typescript
function nonValidSlug(value: string): boolean;
```

`nonValidSlug(value)` is the same test as `!isValidSlug(value)`, written so that a guard clause reads as a sentence rather than as a negation.

Starts with a letter, then letters, digits, hyphens and underscores — the shape a URL segment or a key can take. Spaces, leading digits and an empty string are rejected.

`isX` and `nonX` answer a question and never interrupt control flow; `requireX` and `requireNonX` hand the value back or throw. The scheme is explained in [](validation-conventions.md).

## Parameters

| Parameter | Type     | Description         |
| :-------- | :------- | :------------------ |
| `value`   | `string` | The value to check. |

## Returns

`boolean` — `true` when the value is not a slug; otherwise `false`.

## Examples

### What passes and what does not

```javascript
nonValidSlug("1-page"); // => true
nonValidSlug("my page"); // => true
nonValidSlug(""); // => true
nonValidSlug("my-page"); // => false
nonValidSlug("a_1"); // => false
```

## See also

- [`isValidSlug`](isValidSlug.md)
- [`requireValidSlug`](requireValidSlug.md)
- [](reference-base.md)

## Source

[`src/lang/string/nonValidSlug.ts`](https://github.com/MaksymStoianov/apps-script-utils/blob/main/src/lang/string/nonValidSlug.ts)
