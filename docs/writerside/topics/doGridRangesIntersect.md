<!-- Generated by scripts/docs/generate-topics.mjs. Edits are overwritten. Source: src/appsscript/sheet/doGridRangesIntersect.ts. -->

# doGridRangesIntersect

<link-summary>Reports whether two grid ranges overlap in any cell.</link-summary>

<web-summary>doGridRangesIntersect() — Reports whether two grid ranges overlap in any cell. apps-script-utils, English.</web-summary>

<tldr>
<p>Module: <code>appsscript/sheet</code> · Since: 1.0.0</p>
</tldr>

```typescript
function doGridRangesIntersect(gridRange1: GridRange, gridRange2: GridRange): boolean;
```

Ranges that merely touch along an edge do not intersect: the end bounds are exclusive, so `A1:B2` and `C1:D2` are neighbours, not overlaps. A bound left out means the range is open in that direction and so overlaps anything on that axis.

## Parameters

| Parameter    | Type        | Description                                             |
| :----------- | :---------- | :------------------------------------------------------ |
| `gridRange1` | `GridRange` | The first [`GridRange`](GridRange.md) object to check.  |
| `gridRange2` | `GridRange` | The second [`GridRange`](GridRange.md) object to check. |

## Returns

`boolean` — `true` when at least one cell belongs to both.

## Examples

### Checking an overlap

```javascript
const left = { startRowIndex: 0, endRowIndex: 2, startColumnIndex: 0, endColumnIndex: 2 };
const right = { startRowIndex: 1, endRowIndex: 3, startColumnIndex: 1, endColumnIndex: 3 };

doGridRangesIntersect(left, right); // => true
```

## See also

- [`isGridRangeContainedIn`](isGridRangeContainedIn.md)
- [`isCellGridRange`](isCellGridRange.md)
- [](reference-appsscript.md)

## Source

[`src/appsscript/sheet/doGridRangesIntersect.ts`](https://github.com/MaksymStoianov/apps-script-utils/blob/main/src/appsscript/sheet/doGridRangesIntersect.ts)
