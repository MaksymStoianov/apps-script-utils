<!-- Generated by scripts/docs/generate-topics.mjs. Edits are overwritten. Source: src/appsscript/requireRepository.ts. -->

# requireRepository

<link-summary>Returns a repository once it is known to exist, or throws.</link-summary>

<web-summary>requireRepository() — Returns a repository once it is known to exist, or throws. apps-script-utils, English.</web-summary>

<tldr>
<p>Module: <code>appsscript</code> · Since: 1.5.0</p>
</tldr>

```typescript
function requireRepository<T>(
  repository: T | null | undefined,
  message: string = "Repository is not defined."
): T;
```

`requireService` for the layer above the services: a data source that a container was supposed to provide and did not. The exception names the situation instead of leaving a `null` to travel further.

## Parameters

| Parameter                                               | Type                     | Description                                                   |
| :------------------------------------------------------ | :----------------------- | :------------------------------------------------------------ |
| `repository`                                            | `T \| null \| undefined` | The repository to check.                                      |
| `message` _(optional)_ `= "Repository is not defined."` | `string`                 | The message of the exception. A default is used when omitted. |

## Returns

`T` — the same repository.

## Throws

| Exception                         | Condition                                |
| :-------------------------------- | :--------------------------------------- |
| `RepositoryIsNotDefinedException` | the repository is `null` or `undefined`. |

## Examples

### In use

```javascript
const users = requireRepository(registry.users, "The user repository is not wired up.");
```

## See also

- [`requireService`](requireService.md)
- [`RepositoryIsNotDefinedException`](RepositoryIsNotDefinedException.md)
- [](reference-appsscript.md)

## Source

[`src/appsscript/requireRepository.ts`](https://github.com/MaksymStoianov/apps-script-utils/blob/main/src/appsscript/requireRepository.ts)
