<!-- Generated by scripts/docs/generate-topics.mjs. Edits are overwritten. Source: src/lang/array/intersect.ts. -->

# intersect

<link-summary>Finds the values present in every one of the given arrays, deduplicated and in the first array's order.</link-summary>

<web-summary>intersect() — Finds the values present in every one of the given arrays, deduplicated and in the first array's order. apps-script-utils, English.</web-summary>

<tldr>
<p>Module: <code>lang/array</code> · Since: 1.11.0</p>
</tldr>

```typescript
function intersect<T>(...arrays: T[][]): T[];
```

Order comes from the first array, and the result is deduplicated: a value repeated there appears once. Values are
compared with `SameValueZero`, the rule a `Set` uses, so `NaN` intersects with `NaN` — which a comparison by `===`
never does — and `-0` matches `0`.

The arrays after the first each become a set once, and the first is then walked a single time. The cost is linear in
the total input rather than the nested scan a hand-written version usually ends up being. The inputs are not
modified.

Called with a single array, `intersect` returns its unique values; called with none, it returns `[]`.

## Parameters

| Parameter   | Type    | Description                              |
| :---------- | :------ | :--------------------------------------- |
| `...arrays` | `T[][]` | The arrays to intersect. Left untouched. |

## Returns

`T[]` — a new array of the shared values, in the order of the first array, without duplicates.

## Throws

| Exception   | Condition                     |
| :---------- | :---------------------------- |
| `TypeError` | Any argument is not an array. |

## Examples

### Values shared by every array

```javascript
intersect([1, 2, 3], [2, 3, 4]); // => [2, 3]
intersect([1, 2, 3], [2, 3], [3, 9]); // => [3]
intersect([1, 2], [9]); // => []
```

### Finding the rows two sheets have in common

<tabs group="code">
<tab title="TypeScript" group-key="ts">
<code-block lang="typescript" src="intersect.ts" include-symbol="findSharedIds"/>
</tab>
<tab title="JavaScript" group-key="js">
<code-block lang="javascript">
function findSharedIds(active, billed) {
  const left = active.getRange("A2:A").getValues().flat();
  const right = billed.getRange("A2:A").getValues().flat();
  return intersect(left, right);
}
</code-block>
</tab>
</tabs>

### Deduplication and edge cases

```javascript
intersect([1, 1, 2], [1, 2]); // => [1, 2] — the result is deduplicated
intersect([NaN], [NaN]); // => [NaN]
intersect([1, 2]); // => [1, 2] — a single array, deduplicated
intersect(); // => []
```

## See also

- [`unique`](unique.md) — deduplicates a single array.
- [`without`](without.md) — the complement: removes the values you name.
- [](reference-base.md)

## Source

[`src/lang/array/intersect.ts`](https://github.com/MaksymStoianov/apps-script-utils/blob/main/src/lang/array/intersect.ts)
