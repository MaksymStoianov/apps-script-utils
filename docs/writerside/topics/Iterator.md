<!-- Generated by scripts/docs/generate-topics.mjs. Edits are overwritten. Source: src/lang/Iterator.ts. -->

# Iterator

<link-summary>A cursor over a sequence, which may also walk backwards.</link-summary>

<web-summary>Iterator — A cursor over a sequence, which may also walk backwards. apps-script-utils, English.</web-summary>

<tldr>
<p>Module: <code>lang</code> · Since: 1.0.0</p>
</tldr>

```typescript
interface Iterator<T>
```

`next` and `hasNext` are all an implementation has to provide. The rest — `previous`, `hasPrevious`, `index`, `moveTo`, `size`, `toStart`, `toEnd` — is optional, so a forward-only source can satisfy the interface without pretending to do more than it can.

It is the shape a paged reader takes: rows arriving from a service in batches look like one sequence to the code that consumes them.

## Examples

### Shape

```javascript
while (iterator.hasNext()) {
  const value = iterator.next();
}
```

## See also

- [`Class`](Class.md)
- [`first`](first.md)
- [`last`](last.md)
- [](reference-abstracts.md)

## Source

[`src/lang/Iterator.ts`](https://github.com/MaksymStoianov/apps-script-utils/blob/main/src/lang/Iterator.ts)
