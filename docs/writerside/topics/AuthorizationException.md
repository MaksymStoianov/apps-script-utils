<!-- Generated by scripts/docs/generate-topics.mjs. Edits are overwritten. Source: src/exception/AuthorizationException.ts. -->

# AuthorizationException

<link-summary>The caller is known but is not allowed to do this.</link-summary>

<web-summary>AuthorizationException — The caller is known but is not allowed to do this. apps-script-utils, English.</web-summary>

<tldr>
<p>Module: <code>exception</code> · Since: 1.11.0</p>
</tldr>

```typescript
class AuthorizationException extends RuntimeException
```

The caller is known but is not allowed to do this. Distinct from `AuthenticationException`, which reports that the caller could not be identified at all.

Every class in this hierarchy takes an optional message, another exception whose message it reuses, or nothing at all. `isException` recognises all of them; a plain `Error` is not one.

## Examples

### Throwing and catching

```javascript
throw new AuthorizationException("something specific about this call");

try {
  doWork();
} catch (error) {
  if (error instanceof AuthorizationException) {
    // handled
  }
}
```

## See also

- [`isException`](isException.md)
- [`requireException`](requireException.md)
- [](reference-exception.md)

## Source

[`src/exception/AuthorizationException.ts`](https://github.com/MaksymStoianov/apps-script-utils/blob/main/src/exception/AuthorizationException.ts)
