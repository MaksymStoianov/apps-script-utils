<!-- Generated by scripts/docs/generate-topics.mjs. Edits are overwritten. Source: src/lang/function/retry.ts. -->

# RetryOptions

<link-summary>How `retry` waits and when it gives up.</link-summary>

<web-summary>RetryOptions — How `retry` waits and when it gives up. apps-script-utils, English.</web-summary>

<tldr>
<p>Module: <code>lang/function</code></p>
</tldr>

```typescript
interface RetryOptions
```

`attempts` caps the tries, `delay` is the wait before the second one, `multiplier` grows each later wait and `maxDelay` caps a single one.

`shouldRetry` receives the error and the attempt that failed and decides whether repeating is worth it at all; `sleep` performs the wait, defaulting to `Utilities.sleep` where it exists, which is what lets a test run without waiting.

## Examples

### Shape

```javascript
const options = {
  attempts: 4,
  delay: 500,
  multiplier: 2,
  maxDelay: 10000
};
```

## See also

- [`retry`](retry.md)
- [](reference-base.md)

## Source

[`src/lang/function/retry.ts`](https://github.com/MaksymStoianov/apps-script-utils/blob/main/src/lang/function/retry.ts)
