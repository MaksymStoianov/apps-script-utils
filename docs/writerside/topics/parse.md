<!-- Generated by scripts/docs/generate-topics.mjs. Edits are overwritten. Source: src/net/path/parse.ts. -->

# parse

<link-summary>Breaks a path into its parts.</link-summary>

<web-summary>parse() — Breaks a path into its parts. apps-script-utils, English.</web-summary>

<tldr>
<p>Module: <code>net/path</code></p>
</tldr>

```typescript
function parse(path: string): ParsedPath;
```

The result carries `root`, `dir`, `base`, `name` and `ext`, the same five parts Node's `path.parse` reports. A part that the path does not have is simply absent from the object, so a bare file name comes back with `base`, `name` and `ext` only.

`ext` includes the dot, and a name that begins with one — `.gitignore` — is a name, not an extension.

## Parameters

| Parameter | Type     | Description              |
| :-------- | :------- | :----------------------- |
| `path`    | `string` | The path to break apart. |

## Returns

`ParsedPath` — an object with the parts the path has.

## Examples

### Breaking a path apart

```javascript
parse("/a/b/c.txt");
// => { root: "/", dir: "/a/b", base: "c.txt", name: "c", ext: ".txt" }

parse("readme.md");
// => { base: "readme.md", name: "readme", ext: ".md" }
```

## See also

- [`join`](join.md)
- [`normalize`](normalize.md)
- [](reference-path.md)

## Source

[`src/net/path/parse.ts`](https://github.com/MaksymStoianov/apps-script-utils/blob/main/src/net/path/parse.ts)
