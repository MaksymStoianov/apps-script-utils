<!-- Generated by scripts/docs/generate-topics.mjs. Edits are overwritten. Source: src/lang/number/requireFloat.ts. -->

# requireFloat

<link-summary>Returns the value as a number with a fractional part, or throws.</link-summary>

<web-summary>requireFloat() — Returns the value as a number with a fractional part, or throws. apps-script-utils, English.</web-summary>

<tldr>
<p>Module: <code>lang/number</code> · Since: 1.11.0</p>
</tldr>

```typescript
function requireFloat(
  value: unknown,
  message: string = "Expected a number with a fractional part."
): number;
```

JavaScript has one numeric type, so `1.0` and `1` are the same value: the question answered is whether a fraction survives at runtime, not how the literal was written. The infinities have no fraction and are excluded.

`isX` and `nonX` answer a question and never interrupt control flow; `requireX` and `requireNonX` hand the value back or throw. The scheme is explained in [](validation-conventions.md).

## Parameters

| Parameter                                                              | Type      | Description                                                   |
| :--------------------------------------------------------------------- | :-------- | :------------------------------------------------------------ |
| `value`                                                                | `unknown` | The value to validate.                                        |
| `message` _(optional)_ `= "Expected a number with a fractional part."` | `string`  | The message of the exception. A default is used when omitted. |

## Returns

`number` — the same value, typed as a number with a fractional part.

## Throws

| Exception                  | Condition                                         |
| :------------------------- | :------------------------------------------------ |
| `IllegalArgumentException` | the value is not a number with a fractional part. |

## Examples

### What passes and what does not

```javascript
requireFloat(1.5); // => 1.5
requireFloat(-0.1); // => -0.1
requireFloat(42); // throws IllegalArgumentException
requireFloat(1.0); // throws IllegalArgumentException
```

## See also

- [`isFloat`](isFloat.md)
- [`nonFloat`](nonFloat.md)
- [`requireNonFloat`](requireNonFloat.md)
- [](reference-base.md)

## Source

[`src/lang/number/requireFloat.ts`](https://github.com/MaksymStoianov/apps-script-utils/blob/main/src/lang/number/requireFloat.ts)
