<!-- Generated by scripts/docs/generate-topics.mjs. Edits are overwritten. Source: src/appsscript/ui/checkMultipleAccount.ts. -->

# checkMultipleAccount

<link-summary>Reports whether the script runs as a different account than the one that started it.</link-summary>

<web-summary>checkMultipleAccount() — Reports whether the script runs as a different account than the one that started it. apps-script-utils, English.</web-summary>

<tldr>
<p>Module: <code>appsscript/ui</code> · Since: 1.0.0</p>
</tldr>

```typescript
function checkMultipleAccount(email: string): boolean;
```

Signed in to several Google accounts in one browser, a person can trigger a script that then runs as another of their accounts — and writes, shares or mails as that one. Comparing the initiator's address with the effective user's is what catches it, so the script can say so instead of doing something surprising.

## Parameters

| Parameter | Type     | Description                                         |
| :-------- | :------- | :-------------------------------------------------- |
| `email`   | `string` | The address of the person who triggered the script. |

## Returns

`boolean` — `true` when the two addresses differ.

## Throws

| Exception                     | Condition                               |
| :---------------------------- | :-------------------------------------- |
| `InvalidEmailFormatException` | either address is missing or malformed. |

## Examples

### In use

```javascript
function onOpen(event) {
  if (checkMultipleAccount(event.user.getEmail())) {
    SpreadsheetApp.getUi().alert("You are signed in to more than one Google account.");
  }
}
```

## See also

- [`requireValidEmail`](requireValidEmail.md)
- [`isEmail`](isEmail.md)
- [](reference-appsscript.md)

## Source

[`src/appsscript/ui/checkMultipleAccount.ts`](https://github.com/MaksymStoianov/apps-script-utils/blob/main/src/appsscript/ui/checkMultipleAccount.ts)
