<!-- Generated by scripts/docs/generate-topics.mjs. Edits are overwritten. Source: src/time/types/TimeUnit.ts. -->

# TimeUnit

<link-summary>A unit an interval can be expressed in.</link-summary>

<web-summary>TimeUnit — A unit an interval can be expressed in. apps-script-utils, English.</web-summary>

<tldr>
<p>Module: <code>time/types</code> · Since: 1.11.0</p>
</tldr>

```typescript
type TimeUnit = "millisecond" | "second" | "minute" | "hour" | "day" | "month" | "year";
```

`"millisecond"`, `"second"`, `"minute"` and `"hour"` are fixed multiples of a millisecond. `"day"`, `"month"` and `"year"` are calendar units with no fixed length: a day is 23 or 25 hours across a daylight-saving change, and a month is anything from 28 to 31 days.

Which of the two a function applies is stated on its own page — `diff` counts whole calendar units, `offset` moves by them and clamps the day of the month where it has to.

## Examples

### Shape

```javascript
diff(a, b, "day");
offset(date, 1, "month");
```

## See also

- [`diff`](diff.md)
- [`offset`](offset.md)
- [](reference-base.md)

## Source

[`src/time/types/TimeUnit.ts`](https://github.com/MaksymStoianov/apps-script-utils/blob/main/src/time/types/TimeUnit.ts)
