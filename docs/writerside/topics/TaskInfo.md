<!-- Generated by scripts/docs/generate-topics.mjs. Edits are overwritten. Source: src/time/StopWatch.ts. -->

# TaskInfo

<link-summary>One timed task, as a `StopWatch` recorded it.</link-summary>

<web-summary>TaskInfo — One timed task, as a `StopWatch` recorded it. apps-script-utils, English.</web-summary>

<tldr>
<p>Module: <code>time</code></p>
</tldr>

```typescript
interface TaskInfo
```

The name the task was started with — empty when none was given — and how long it ran, in milliseconds and in seconds. The two durations are the same measurement, offered in both units so that neither the log line nor the arithmetic has to convert.

## Examples

### Shape

```javascript
const watch = new StopWatch();

watch.start("fetch");
watch.stop();

watch.getLastTaskInfo(); // { taskName: "fetch", timeMillis: …, timeSeconds: … }
```

## See also

- [`StopWatch`](StopWatch.md)
- [](reference-base.md)

## Source

[`src/time/StopWatch.ts`](https://github.com/MaksymStoianov/apps-script-utils/blob/main/src/time/StopWatch.ts)
