<!-- Generated by scripts/docs/generate-topics.mjs. Edits are overwritten. Source: src/lang/string/isValidSlug.ts. -->

# isValidSlug

<link-summary>Checks whether a value is a slug.</link-summary>

<web-summary>isValidSlug() — Checks whether a value is a slug. apps-script-utils, English.</web-summary>

<tldr>
<p>Module: <code>lang/string</code> · Since: 1.0.0</p>
</tldr>

```typescript
function isValidSlug(value: string): boolean;
```

Starts with a letter, then letters, digits, hyphens and underscores — the shape a URL segment or a key can take. Spaces, leading digits and an empty string are rejected.

`isX` and `nonX` answer a question and never interrupt control flow; `requireX` and `requireNonX` hand the value back or throw. The scheme is explained in [](validation-conventions.md).

## Parameters

| Parameter | Type     | Description         |
| :-------- | :------- | :------------------ |
| `value`   | `string` | The value to check. |

## Returns

`boolean` — `true` when the value is a slug; otherwise `false`.

## Examples

### What passes and what does not

```javascript
isValidSlug("my-page"); // => true
isValidSlug("a_1"); // => true
isValidSlug("1-page"); // => false
isValidSlug("my page"); // => false
isValidSlug(""); // => false
```

## See also

- [`nonValidSlug`](nonValidSlug.md)
- [`requireValidSlug`](requireValidSlug.md)
- [](reference-base.md)

## Source

[`src/lang/string/isValidSlug.ts`](https://github.com/MaksymStoianov/apps-script-utils/blob/main/src/lang/string/isValidSlug.ts)
