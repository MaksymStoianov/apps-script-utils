<!-- Generated by scripts/docs/generate-topics.mjs. Edits are overwritten. Source: src/appsscript/sheet/getValues.ts. -->

# Row

<link-summary>One row, as `getValues` hands it to a filter or a mapper.</link-summary>

<web-summary>Row — One row, as `getValues` hands it to a filter or a mapper. apps-script-utils, English.</web-summary>

<tldr>
<p>Module: <code>appsscript/sheet</code> · Since: 1.11.0</p>
</tldr>

```typescript
interface Row
```

`values` is the cells left to right, `position` the one-based row on the sheet — the number a person sees in the margin — and the row keyed by column names when a header row was named.

Having the position in hand is what lets a filter report or act on a specific row afterwards.

## Examples

### Shape

```javascript
// What a filter receives when `headerRow` is set.
const row = {
  values: ["7", "ada@example.com"],
  position: 2,
  record: { id: "7", email: "ada@example.com" }
};
```

## See also

- [`getValues`](getValues.md)
- [`RowPredicate`](RowPredicate.md)
- [](reference-appsscript.md)

## Source

[`src/appsscript/sheet/getValues.ts`](https://github.com/MaksymStoianov/apps-script-utils/blob/main/src/appsscript/sheet/getValues.ts)
