{
  "summary": "Возвращает значение, убедившись, что оно не является безопасным целым числом.",
  "description": [
    "Целое число, которое точно представимо в IEEE-754. За пределами этого диапазона разные математические целые схлопываются в одно значение, и арифметика незаметно перестаёт быть точной.",
    "`isX` и `nonX` отвечают на вопрос и никогда не прерывают выполнение; `requireX` и `requireNonX` возвращают значение или выбрасывают исключение. Схема разобрана в [](validation-conventions.md)."
  ],
  "params": {
    "value": "Значение, которое нужно проверить.",
    "message": "Сообщение исключения. Если не задано, используется сообщение по умолчанию.",
    "strict": "При `true` строка считается пустой, только если в ней вовсе нет символов; по умолчанию пробелы сначала отбрасываются."
  },
  "returns": "то же значение; после проверки известно, что оно не является безопасным целым числом.",
  "throws": {
    "IllegalArgumentException": "значение является безопасным целым числом."
  },
  "examples": [
    {
      "title": "Что проходит, а что нет",
      "body": "```javascript\nrequireNonSafeInteger(Number.MAX_SAFE_INTEGER + 2); // => Number.MAX_SAFE_INTEGER + 2\nrequireNonSafeInteger(1.5); // => 1.5\nrequireNonSafeInteger(42); // throws IllegalArgumentException\nrequireNonSafeInteger(Number.MAX_SAFE_INTEGER); // throws IllegalArgumentException\n```"
    }
  ],
  "seeAlso": [
    {
      "name": "isSafeInteger"
    },
    {
      "name": "nonSafeInteger"
    },
    {
      "name": "requireSafeInteger"
    }
  ]
}
