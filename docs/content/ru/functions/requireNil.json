{
  "summary": "Возвращает значение как `null` или `undefined` или выбрасывает исключение.",
  "description": [
    "Только отсутствующие значения: `0`, `\"\"` и `false` — это присутствующие значения, они не подходят.",
    "`isX` и `nonX` отвечают на вопрос и никогда не прерывают выполнение; `requireX` и `requireNonX` возвращают значение или выбрасывают исключение. Схема разобрана в [](validation-conventions.md)."
  ],
  "params": {
    "value": "Значение, которое нужно проверить.",
    "message": "Сообщение исключения. Если не задано, используется сообщение по умолчанию.",
    "strict": "При `true` строка считается пустой, только если в ней вовсе нет символов; по умолчанию пробелы сначала отбрасываются."
  },
  "returns": "то же значение, уже проверенное как `null` или `undefined`.",
  "throws": {
    "IllegalArgumentException": "значение не является значением `null` или `undefined`."
  },
  "examples": [
    {
      "title": "Что проходит, а что нет",
      "body": "```javascript\nrequireNil(null); // => null\nrequireNil(undefined); // => undefined\nrequireNil(0); // throws IllegalArgumentException\nrequireNil(\"\"); // throws IllegalArgumentException\n```"
    }
  ],
  "seeAlso": [
    {
      "name": "isNil"
    },
    {
      "name": "nonNil"
    },
    {
      "name": "requireNonNil"
    }
  ]
}
