{
  "summary": "Очищает строки, выбранные предикатом, не удаляя сами строки.",
  "description": [
    "Содержимое уходит, строки остаются, поэтому позиции не сдвигаются и всё, что ссылается на строку по номеру, продолжает указывать на то же место. Удаляет строки `deleteRowsByConditional`.",
    "Лист читается один раз, а очистка группируется в идущие подряд блоки, поэтому даже разрозненная выборка стоит немногих обращений к сервису."
  ],
  "params": {
    "sheet": "Лист, с которым работаем.",
    "predicate": "Получает ячейки строки, её позицию от единицы и — если задан `headerRow` — строку с ключами по именам столбцов. Верните `true` для строк, с которыми нужно что-то сделать.",
    "options": "`headerRow` указывает строку с именами столбцов. Если она задана, каждая строка приходит с ключами по этим именам, а сама строка заголовков выбывает из кандидатов."
  },
  "returns": "сколько их было затронуто.",
  "throws": {
    "InvalidSheetException": "первый аргумент не является листом.",
    "IllegalArgumentException": "предикат не функция или позиция заголовка не положительное целое."
  },
  "examples": [
    {
      "title": "Применение"
    }
  ],
  "seeAlso": [
    {
      "name": "deleteRowsByConditional"
    },
    {
      "name": "clearColumnsByConditional"
    },
    {
      "name": "getValues"
    }
  ]
}
