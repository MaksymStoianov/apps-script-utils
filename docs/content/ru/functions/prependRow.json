{
  "summary": "Вставляет одну строку перед данными — на листе или у диапазона.",
  "description": [
    "`prependRows` с одной строкой: сначала освобождается место, поэтому ничего из уже имеющегося не перезаписывается. Лист получает строку наверху; диапазон — прямо над своей первой строкой, а значения пишутся по столбцам диапазона.",
    "Ячейка, текст которой начинается с `=`, записывается как формула, а не как текст — по тому же правилу, что и в редакторе."
  ],
  "params": {
    "target": "Лист, в который вставляем, или диапазон, у которого вставляем.",
    "values": "Одна строка: плоский массив, по значению на столбец.",
    "options": "`afterFrozenRows` вставляет ниже закреплённых строк, а не в самый верх. Диапазон задаёт позицию сам, поэтому там опция ничего не меняет."
  },
  "returns": "лист, чтобы вызовы можно было объединять в цепочку.",
  "throws": {
    "IllegalArgumentException": "не передан ни один аргумент.",
    "InvalidSheetException": "первый аргумент не является ни листом, ни диапазоном.",
    "TypeError": "значения не являются плоским массивом."
  },
  "examples": [
    {
      "title": "Вставка наверх листа",
      "body": "```javascript\nconst sheet = SpreadsheetApp.getActiveSheet();\n\nprependRow(sheet, [\"Name\", \"Email\"]);\n```"
    },
    {
      "title": "Вставка у диапазона",
      "body": "```javascript\nconst sheet = SpreadsheetApp.getActiveSheet();\n\nprependRow(sheet.getRange(\"B4:C13\"), [\"Name\", \"Email\"]);\n```"
    }
  ],
  "seeAlso": [
    {
      "name": "prependRows"
    },
    {
      "name": "appendRow"
    }
  ]
}
