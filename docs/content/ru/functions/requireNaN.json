{
  "summary": "Возвращает значение как `NaN` или выбрасывает исключение.",
  "description": [
    "Аргумент не приводится к числу — этим проверка и отличается от глобального `isNaN`, который сначала преобразует значение, так что `isNaN(\"abc\")` и `isNaN(undefined)` дают `true`.",
    "`isX` и `nonX` отвечают на вопрос и никогда не прерывают выполнение; `requireX` и `requireNonX` возвращают значение или выбрасывают исключение. Схема разобрана в [](validation-conventions.md)."
  ],
  "params": {
    "value": "Значение, которое нужно проверить.",
    "message": "Сообщение исключения. Если не задано, используется сообщение по умолчанию.",
    "strict": "При `true` строка считается пустой, только если в ней вовсе нет символов; по умолчанию пробелы сначала отбрасываются."
  },
  "returns": "то же значение, уже проверенное как `NaN`.",
  "throws": {
    "IllegalArgumentException": "значение не является числом `NaN`."
  },
  "examples": [
    {
      "title": "Что проходит, а что нет",
      "body": "```javascript\nrequireNaN(NaN); // => NaN\nrequireNaN(0 / 0); // => 0 / 0\nrequireNaN(0); // throws IllegalArgumentException\nrequireNaN(\"abc\"); // throws IllegalArgumentException\n```"
    }
  ],
  "seeAlso": [
    {
      "name": "isNaN"
    },
    {
      "name": "nonNaN"
    },
    {
      "name": "requireNonNaN"
    }
  ]
}
