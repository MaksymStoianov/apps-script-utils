{
  "summary": "Возвращает значение как строку или выбрасывает исключение.",
  "description": [
    "Проверяется `typeof value === \"string\"`, поэтому пустая строка подходит, а объект `String` — нет, он объект.",
    "`isX` и `nonX` отвечают на вопрос и никогда не прерывают выполнение; `requireX` и `requireNonX` возвращают значение или выбрасывают исключение. Схема разобрана в [](validation-conventions.md)."
  ],
  "params": {
    "value": "Значение, которое нужно проверить.",
    "message": "Сообщение исключения. Если не задано, используется сообщение по умолчанию.",
    "strict": "При `true` строка считается пустой, только если в ней вовсе нет символов; по умолчанию пробелы сначала отбрасываются."
  },
  "returns": "то же значение, уже проверенное как строку.",
  "throws": {
    "InvalidStringException": "значение не является строкой."
  },
  "examples": [
    {
      "title": "Что проходит, а что нет",
      "body": "```javascript\nrequireString(\"\"); // => \"\"\nrequireString(\"abc\"); // => \"abc\"\nrequireString(1); // throws InvalidStringException\nrequireString(null); // throws InvalidStringException\n```"
    }
  ],
  "seeAlso": [
    {
      "name": "isString"
    },
    {
      "name": "nonString"
    },
    {
      "name": "requireNonString"
    }
  ]
}
