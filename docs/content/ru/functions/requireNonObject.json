{
  "summary": "Возвращает значение, убедившись, что оно не является объектом.",
  "description": [
    "Проверяется `typeof value === \"object\"` с исключением `null`, поэтому массивы, даты и экземпляры классов подходят, а функция — нет. Функции принимает `isObjectLike`.",
    "`isX` и `nonX` отвечают на вопрос и никогда не прерывают выполнение; `requireX` и `requireNonX` возвращают значение или выбрасывают исключение. Схема разобрана в [](validation-conventions.md)."
  ],
  "params": {
    "value": "Значение, которое нужно проверить.",
    "message": "Сообщение исключения. Если не задано, используется сообщение по умолчанию.",
    "strict": "При `true` строка считается пустой, только если в ней вовсе нет символов; по умолчанию пробелы сначала отбрасываются."
  },
  "returns": "то же значение; после проверки известно, что оно не является объектом.",
  "throws": {
    "IllegalArgumentException": "значение является объектом."
  },
  "examples": [
    {
      "title": "Что проходит, а что нет",
      "body": "```javascript\nrequireNonObject(null); // => null\nrequireNonObject(\"abc\"); // => \"abc\"\nrequireNonObject({}); // throws IllegalArgumentException\nrequireNonObject([]); // throws IllegalArgumentException\n```"
    }
  ],
  "seeAlso": [
    {
      "name": "isObject"
    },
    {
      "name": "nonObject"
    },
    {
      "name": "requireObject"
    }
  ]
}
