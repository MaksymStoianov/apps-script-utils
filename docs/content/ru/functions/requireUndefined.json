{
  "summary": "Возвращает значение как `undefined` или выбрасывает исключение.",
  "description": [
    "Проверяется `typeof value === \"undefined\"` — в отличие от прямого сравнения, это безопасно даже для необъявленной переменной.",
    "`isX` и `nonX` отвечают на вопрос и никогда не прерывают выполнение; `requireX` и `requireNonX` возвращают значение или выбрасывают исключение. Схема разобрана в [](validation-conventions.md)."
  ],
  "params": {
    "value": "Значение, которое нужно проверить.",
    "message": "Сообщение исключения. Если не задано, используется сообщение по умолчанию.",
    "strict": "При `true` строка считается пустой, только если в ней вовсе нет символов; по умолчанию пробелы сначала отбрасываются."
  },
  "returns": "то же значение, уже проверенное как `undefined`.",
  "throws": {
    "IllegalArgumentException": "значение не является значением `undefined`."
  },
  "examples": [
    {
      "title": "Что проходит, а что нет",
      "body": "```javascript\nrequireUndefined(undefined); // => undefined\nrequireUndefined(null); // throws IllegalArgumentException\nrequireUndefined(0); // throws IllegalArgumentException\n```"
    }
  ],
  "seeAlso": [
    {
      "name": "isUndefined"
    },
    {
      "name": "nonUndefined"
    },
    {
      "name": "requireNonUndefined"
    }
  ]
}
