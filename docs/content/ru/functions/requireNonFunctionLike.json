{
  "summary": "Возвращает значение, убедившись, что оно не является вызываемым значением.",
  "description": [
    "Дешёвая проверка — `typeof value === \"function\"`, — именно то, что нужно перед вызовом. `isFunction` отвечает на тот же вопрос строже.",
    "`isX` и `nonX` отвечают на вопрос и никогда не прерывают выполнение; `requireX` и `requireNonX` возвращают значение или выбрасывают исключение. Схема разобрана в [](validation-conventions.md)."
  ],
  "params": {
    "value": "Значение, которое нужно проверить.",
    "message": "Сообщение исключения. Если не задано, используется сообщение по умолчанию.",
    "strict": "При `true` строка считается пустой, только если в ней вовсе нет символов; по умолчанию пробелы сначала отбрасываются."
  },
  "returns": "то же значение; после проверки известно, что оно не является вызываемым значением.",
  "throws": {
    "IllegalArgumentException": "значение является вызываемым значением."
  },
  "examples": [
    {
      "title": "Что проходит, а что нет",
      "body": "```javascript\nrequireNonFunctionLike(null); // => null\nrequireNonFunctionLike({}); // => {}\nrequireNonFunctionLike(function () {}); // throws IllegalArgumentException\nrequireNonFunctionLike(Math.max); // throws IllegalArgumentException\n```"
    }
  ],
  "seeAlso": [
    {
      "name": "isFunctionLike"
    },
    {
      "name": "nonFunctionLike"
    },
    {
      "name": "requireFunctionLike"
    }
  ]
}
