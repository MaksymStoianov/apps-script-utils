{
  "summary": "Gibt den Wert als eine Versionsangabe zurück oder wirft.",
  "description": [
    "Durch Punkte getrennte Zifferngruppen, beliebig viele. Ein Vorabversions-Suffix, ein führendes `v` und eine leere Gruppe werden abgelehnt: `\"1.2.3-beta\"` besteht also nicht, obwohl SemVer es erlaubt.",
    "`isX` und `nonX` beantworten eine Frage und unterbrechen den Ablauf nie; `requireX` und `requireNonX` geben den Wert zurück oder werfen. Das Schema erklärt [](validation-conventions.md)."
  ],
  "params": {
    "value": "Der zu validierende Wert.",
    "message": "Die Meldung der Ausnahme. Ohne Angabe wird eine Standardmeldung verwendet.",
    "strict": "Bei `true` gilt eine Zeichenkette nur dann als leer, wenn sie gar keine Zeichen enthält; standardmäßig wird zuvor Leerraum entfernt."
  },
  "returns": "denselben Wert, typisiert als eine Versionsangabe.",
  "throws": {
    "IllegalArgumentException": "der Wert ist nicht eine Versionsangabe."
  },
  "examples": [
    {
      "title": "Was besteht und was nicht",
      "body": "```javascript\nrequireValidVersion(\"1\"); // => \"1\"\nrequireValidVersion(\"1.2.3\"); // => \"1.2.3\"\nrequireValidVersion(\"1.2.3-beta\"); // throws IllegalArgumentException\nrequireValidVersion(\"v1.2\"); // throws IllegalArgumentException\n```"
    }
  ],
  "seeAlso": [
    {
      "name": "isValidVersion"
    },
    {
      "name": "nonValidVersion"
    }
  ]
}
