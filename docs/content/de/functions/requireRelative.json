{
  "summary": "Gibt den Wert als einen relativen Pfad zurück oder wirft.",
  "description": [
    "Das exakte Gegenstück zu `isAbsolute`: alles, was nicht an einer Wurzel beginnt, die leere Zeichenkette eingeschlossen.",
    "`isX` und `nonX` beantworten eine Frage und unterbrechen den Ablauf nie; `requireX` und `requireNonX` geben den Wert zurück oder werfen. Das Schema erklärt [](validation-conventions.md)."
  ],
  "params": {
    "value": "Der zu validierende Wert.",
    "message": "Die Meldung der Ausnahme. Ohne Angabe wird eine Standardmeldung verwendet.",
    "strict": "Bei `true` gilt eine Zeichenkette nur dann als leer, wenn sie gar keine Zeichen enthält; standardmäßig wird zuvor Leerraum entfernt."
  },
  "returns": "denselben Wert, typisiert als einen relativen Pfad.",
  "throws": {
    "IllegalArgumentException": "der Wert ist nicht ein relativer Pfad."
  },
  "examples": [
    {
      "title": "Was besteht und was nicht",
      "body": "```javascript\nrequireRelative(\"docs/readme.md\"); // => \"docs/readme.md\"\nrequireRelative(\"./a\"); // => \"./a\"\nrequireRelative(\"/var/log\"); // throws IllegalArgumentException\nrequireRelative(\"https://example.com/a\"); // throws IllegalArgumentException\n```"
    }
  ],
  "seeAlso": [
    {
      "name": "isRelative"
    },
    {
      "name": "nonRelative"
    }
  ]
}
