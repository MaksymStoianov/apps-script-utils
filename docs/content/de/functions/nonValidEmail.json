{
  "summary": "Prüft, ob ein Wert **nicht** eine E-Mail-Adresse ist.",
  "description": [
    "`nonValidEmail(value)` ist dieselbe Prüfung wie `!isValidEmail(value)`, so geschrieben, dass eine Wächterklausel sich wie ein Satz liest und nicht wie eine Verneinung.",
    "Die Adresse wird an genau einem `@` geteilt: die Domain wird geprüft wie von `isValidDomain`, der lokale Teil muss nicht leer und frei von Leerzeichen sein. Eine Formatprüfung, kein Beleg dafür, dass das Postfach existiert.",
    "`isX` und `nonX` beantworten eine Frage und unterbrechen den Ablauf nie; `requireX` und `requireNonX` geben den Wert zurück oder werfen. Das Schema erklärt [](validation-conventions.md)."
  ],
  "params": {
    "value": "Der zu prüfende Wert."
  },
  "returns": "`true`, wenn der Wert nicht eine E-Mail-Adresse ist; sonst `false`.",
  "throws": {},
  "examples": [
    {
      "title": "Was besteht und was nicht",
      "body": "```javascript\nnonValidEmail(\"user@@example.com\"); // => true\nnonValidEmail(\"user@\"); // => true\nnonValidEmail(\"example.com\"); // => true\nnonValidEmail(\"\"); // => true\nnonValidEmail(\"user@example.com\"); // => false\nnonValidEmail(\"first.last@sub.example.co.uk\"); // => false\n```"
    }
  ],
  "seeAlso": [
    {
      "name": "isEmail"
    },
    {
      "name": "requireValidEmail"
    }
  ]
}
