{
  "summary": "Gibt den Wert zurück, nachdem geprüft wurde, dass er nicht `undefined` ist.",
  "description": [
    "Geprüft wird `typeof value === \"undefined\"`, was anders als ein direkter Vergleich auch bei einer nie deklarierten Variablen sicher ist.",
    "`isX` und `nonX` beantworten eine Frage und unterbrechen den Ablauf nie; `requireX` und `requireNonX` geben den Wert zurück oder werfen. Das Schema erklärt [](validation-conventions.md)."
  ],
  "params": {
    "value": "Der zu validierende Wert.",
    "message": "Die Meldung der Ausnahme. Ohne Angabe wird eine Standardmeldung verwendet.",
    "strict": "Bei `true` gilt eine Zeichenkette nur dann als leer, wenn sie gar keine Zeichen enthält; standardmäßig wird zuvor Leerraum entfernt."
  },
  "returns": "denselben Wert; nach der Prüfung steht fest, dass er nicht `undefined` ist.",
  "throws": {
    "IllegalArgumentException": "der Wert ist `undefined`."
  },
  "examples": [
    {
      "title": "Was besteht und was nicht",
      "body": "```javascript\nrequireNonUndefined(null); // => null\nrequireNonUndefined(0); // => 0\nrequireNonUndefined(undefined); // throws IllegalArgumentException\n```"
    }
  ],
  "seeAlso": [
    {
      "name": "isUndefined"
    },
    {
      "name": "nonUndefined"
    },
    {
      "name": "requireUndefined"
    }
  ]
}
