{
  "summary": "Renvoie la valeur après avoir vérifié qu'elle n'est pas un entier.",
  "description": [
    "Un nombre sans partie fractionnaire. Rien n'est converti : la chaîne `\"42\"` n'est pas un entier, et `NaN` non plus.",
    "`isX` et `nonX` répondent à une question sans jamais interrompre le flot ; `requireX` et `requireNonX` rendent la valeur ou lèvent. Le schéma est expliqué dans [](validation-conventions.md)."
  ],
  "params": {
    "value": "La valeur à valider.",
    "message": "Le message de l'exception. À défaut, un message par défaut est utilisé.",
    "strict": "Avec `true`, une chaîne n'est vide que si elle ne contient aucun caractère ; par défaut, les espaces sont retirés d'abord."
  },
  "returns": "la même valeur ; après le test, elle n'est pas un entier.",
  "throws": {
    "IllegalArgumentException": "la valeur est un entier."
  },
  "examples": [
    {
      "title": "Ce qui passe et ce qui ne passe pas",
      "body": "```javascript\nrequireNonInteger(1.5); // => 1.5\nrequireNonInteger(NaN); // => NaN\nrequireNonInteger(42); // throws IllegalArgumentException\nrequireNonInteger(-7); // throws IllegalArgumentException\n```"
    }
  ],
  "seeAlso": [
    {
      "name": "isInteger"
    },
    {
      "name": "nonInteger"
    },
    {
      "name": "requireInteger"
    }
  ]
}
