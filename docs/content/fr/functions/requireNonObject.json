{
  "summary": "Renvoie la valeur après avoir vérifié qu'elle n'est pas un objet.",
  "description": [
    "Le test est `typeof value === \"object\"` sans `null` : tableaux, dates et instances de classe comptent, une fonction non. C'est `isObjectLike` qui accepte aussi les fonctions.",
    "`isX` et `nonX` répondent à une question sans jamais interrompre le flot ; `requireX` et `requireNonX` rendent la valeur ou lèvent. Le schéma est expliqué dans [](validation-conventions.md)."
  ],
  "params": {
    "value": "La valeur à valider.",
    "message": "Le message de l'exception. À défaut, un message par défaut est utilisé.",
    "strict": "Avec `true`, une chaîne n'est vide que si elle ne contient aucun caractère ; par défaut, les espaces sont retirés d'abord."
  },
  "returns": "la même valeur ; après le test, elle n'est pas un objet.",
  "throws": {
    "IllegalArgumentException": "la valeur est un objet."
  },
  "examples": [
    {
      "title": "Ce qui passe et ce qui ne passe pas",
      "body": "```javascript\nrequireNonObject(null); // => null\nrequireNonObject(\"abc\"); // => \"abc\"\nrequireNonObject({}); // throws IllegalArgumentException\nrequireNonObject([]); // throws IllegalArgumentException\n```"
    }
  ],
  "seeAlso": [
    {
      "name": "isObject"
    },
    {
      "name": "nonObject"
    },
    {
      "name": "requireObject"
    }
  ]
}
