{
  "summary": "Vérifie qu'une valeur n'est **pas** une fonction.",
  "description": [
    "`nonFunction(value)` est le même test que `!isFunction(value)`, écrit pour qu'une clause de garde se lise comme une phrase plutôt que comme une négation.",
    "Déterminé par le tag interne : fonctions ordinaires, `async`, générateurs et générateurs asynchrones passent toutes. Une classe est également une fonction.",
    "`isX` et `nonX` répondent à une question sans jamais interrompre le flot ; `requireX` et `requireNonX` rendent la valeur ou lèvent. Le schéma est expliqué dans [](validation-conventions.md)."
  ],
  "params": {
    "value": "La valeur à vérifier."
  },
  "returns": "`true` si la valeur n'est pas une fonction ; sinon `false`.",
  "throws": {},
  "examples": [
    {
      "title": "Ce qui passe et ce qui ne passe pas",
      "body": "```javascript\nnonFunction(null); // => true\nnonFunction(\"fn\"); // => true\nnonFunction({}); // => true\nnonFunction(function () {}); // => false\nnonFunction(async function () {}); // => false\nnonFunction(Math.max); // => false\n```"
    }
  ],
  "seeAlso": [
    {
      "name": "isFunction"
    },
    {
      "name": "requireFunction"
    },
    {
      "name": "requireNonFunction"
    }
  ]
}
