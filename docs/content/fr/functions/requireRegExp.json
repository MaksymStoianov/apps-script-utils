{
  "summary": "Renvoie la valeur en tant que une expression régulière, ou lève une exception.",
  "description": [
    "Identifiée par son tag interne plutôt que par `instanceof`, si bien qu'une expression régulière venue d'un autre frame est reconnue aussi.",
    "`isX` et `nonX` répondent à une question sans jamais interrompre le flot ; `requireX` et `requireNonX` rendent la valeur ou lèvent. Le schéma est expliqué dans [](validation-conventions.md)."
  ],
  "params": {
    "value": "La valeur à valider.",
    "message": "Le message de l'exception. À défaut, un message par défaut est utilisé.",
    "strict": "Avec `true`, une chaîne n'est vide que si elle ne contient aucun caractère ; par défaut, les espaces sont retirés d'abord."
  },
  "returns": "la même valeur, typée comme une expression régulière.",
  "throws": {
    "IllegalArgumentException": "la valeur n'est pas une expression régulière."
  },
  "examples": [
    {
      "title": "Ce qui passe et ce qui ne passe pas",
      "body": "```javascript\nrequireRegExp(/a/); // => /a/\nrequireRegExp(new RegExp(\"a\")); // => new RegExp(\"a\")\nrequireRegExp(\"/a/\"); // throws IllegalArgumentException\nrequireRegExp({}); // throws IllegalArgumentException\n```"
    }
  ],
  "seeAlso": [
    {
      "name": "isRegExp"
    },
    {
      "name": "nonRegExp"
    },
    {
      "name": "requireNonRegExp"
    }
  ]
}
