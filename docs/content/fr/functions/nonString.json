{
  "summary": "Vérifie qu'une valeur n'est **pas** une chaîne de caractères.",
  "description": [
    "`nonString(value)` est le même test que `!isString(value)`, écrit pour qu'une clause de garde se lise comme une phrase plutôt que comme une négation.",
    "Le test est `typeof value === \"string\"` : la chaîne vide passe, un objet `String` non, puisque c'est un objet.",
    "`isX` et `nonX` répondent à une question sans jamais interrompre le flot ; `requireX` et `requireNonX` rendent la valeur ou lèvent. Le schéma est expliqué dans [](validation-conventions.md)."
  ],
  "params": {
    "value": "La valeur à vérifier."
  },
  "returns": "`true` si la valeur n'est pas une chaîne de caractères ; sinon `false`.",
  "throws": {},
  "examples": [
    {
      "title": "Ce qui passe et ce qui ne passe pas",
      "body": "```javascript\nnonString(1); // => true\nnonString(null); // => true\nnonString([]); // => true\nnonString(\"\"); // => false\nnonString(\"abc\"); // => false\n```"
    }
  ],
  "seeAlso": [
    {
      "name": "isString"
    },
    {
      "name": "requireString"
    },
    {
      "name": "requireNonString"
    }
  ]
}
