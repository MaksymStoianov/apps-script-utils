{
  "summary": "Vérifie qu'une valeur n'est **pas** une `Exception`.",
  "description": [
    "`nonException(value)` est le même test que `!isException(value)`, écrit pour qu'une clause de garde se lise comme une phrase plutôt que comme une négation.",
    "Uniquement la hiérarchie propre à la bibliothèque. Un `Error` ordinaire ne compte pas, ce qui rend le test utile dans un `catch` qui doit distinguer les deux.",
    "`isX` et `nonX` répondent à une question sans jamais interrompre le flot ; `requireX` et `requireNonX` rendent la valeur ou lèvent. Le schéma est expliqué dans [](validation-conventions.md)."
  ],
  "params": {
    "value": "La valeur à vérifier."
  },
  "returns": "`true` si la valeur n'est pas une `Exception` ; sinon `false`.",
  "throws": {},
  "examples": [
    {
      "title": "Ce qui passe et ce qui ne passe pas",
      "body": "```javascript\nnonException(new Error()); // => true\nnonException({}); // => true\nnonException(null); // => true\nnonException(new IllegalArgumentException()); // => false\n```"
    }
  ],
  "seeAlso": [
    {
      "name": "isException"
    },
    {
      "name": "requireException"
    },
    {
      "name": "requireNonException"
    }
  ]
}
