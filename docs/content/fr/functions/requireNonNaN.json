{
  "summary": "Renvoie la valeur après avoir vérifié qu'elle n'est pas le nombre `NaN`.",
  "description": [
    "Rien n'est converti au préalable, et c'est là que ce test diffère du `isNaN` global : celui-ci convertit son argument, si bien que `isNaN(\"abc\")` et `isNaN(undefined)` renvoient `true`.",
    "`isX` et `nonX` répondent à une question sans jamais interrompre le flot ; `requireX` et `requireNonX` rendent la valeur ou lèvent. Le schéma est expliqué dans [](validation-conventions.md)."
  ],
  "params": {
    "value": "La valeur à valider.",
    "message": "Le message de l'exception. À défaut, un message par défaut est utilisé.",
    "strict": "Avec `true`, une chaîne n'est vide que si elle ne contient aucun caractère ; par défaut, les espaces sont retirés d'abord."
  },
  "returns": "la même valeur ; après le test, elle n'est pas le nombre `NaN`.",
  "throws": {
    "IllegalArgumentException": "la valeur est le nombre `NaN`."
  },
  "examples": [
    {
      "title": "Ce qui passe et ce qui ne passe pas",
      "body": "```javascript\nrequireNonNaN(0); // => 0\nrequireNonNaN(\"abc\"); // => \"abc\"\nrequireNonNaN(NaN); // throws IllegalArgumentException\nrequireNonNaN(0 / 0); // throws IllegalArgumentException\n```"
    }
  ],
  "seeAlso": [
    {
      "name": "isNaN"
    },
    {
      "name": "nonNaN"
    },
    {
      "name": "requireNaN"
    }
  ]
}
