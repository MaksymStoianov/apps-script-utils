{
  "summary": "Vérifie qu'une valeur n'est **pas** un entier.",
  "description": [
    "`nonInteger(value)` est le même test que `!isInteger(value)`, écrit pour qu'une clause de garde se lise comme une phrase plutôt que comme une négation.",
    "Un nombre sans partie fractionnaire. Rien n'est converti : la chaîne `\"42\"` n'est pas un entier, et `NaN` non plus.",
    "`isX` et `nonX` répondent à une question sans jamais interrompre le flot ; `requireX` et `requireNonX` rendent la valeur ou lèvent. Le schéma est expliqué dans [](validation-conventions.md)."
  ],
  "params": {
    "value": "La valeur à vérifier."
  },
  "returns": "`true` si la valeur n'est pas un entier ; sinon `false`.",
  "throws": {},
  "examples": [
    {
      "title": "Ce qui passe et ce qui ne passe pas",
      "body": "```javascript\nnonInteger(1.5); // => true\nnonInteger(NaN); // => true\nnonInteger(\"42\"); // => true\nnonInteger(42); // => false\nnonInteger(-7); // => false\nnonInteger(0); // => false\n```"
    }
  ],
  "seeAlso": [
    {
      "name": "isInteger"
    },
    {
      "name": "requireInteger"
    },
    {
      "name": "requireNonInteger"
    }
  ]
}
