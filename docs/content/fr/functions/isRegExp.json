{
  "summary": "Vérifie si une valeur est une expression régulière.",
  "description": [
    "Identifiée par son tag interne plutôt que par `instanceof`, si bien qu'une expression régulière venue d'un autre frame est reconnue aussi.",
    "`isX` et `nonX` répondent à une question sans jamais interrompre le flot ; `requireX` et `requireNonX` rendent la valeur ou lèvent. Le schéma est expliqué dans [](validation-conventions.md)."
  ],
  "params": {
    "value": "La valeur à vérifier."
  },
  "returns": "`true` si la valeur est une expression régulière ; sinon `false`.",
  "throws": {},
  "examples": [
    {
      "title": "Ce qui passe et ce qui ne passe pas",
      "body": "```javascript\nisRegExp(/a/); // => true\nisRegExp(new RegExp(\"a\")); // => true\nisRegExp(\"/a/\"); // => false\nisRegExp({}); // => false\nisRegExp(null); // => false\n```"
    }
  ],
  "seeAlso": [
    {
      "name": "nonRegExp"
    },
    {
      "name": "requireRegExp"
    },
    {
      "name": "requireNonRegExp"
    }
  ]
}
