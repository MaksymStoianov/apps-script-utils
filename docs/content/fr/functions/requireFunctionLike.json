{
  "summary": "Renvoie la valeur en tant que une valeur appelable, ou lève une exception.",
  "description": [
    "Le test bon marché — `typeof value === \"function\"` — celui qu'on veut avant d'appeler quelque chose. `isFunction` lit la même question plus strictement.",
    "`isX` et `nonX` répondent à une question sans jamais interrompre le flot ; `requireX` et `requireNonX` rendent la valeur ou lèvent. Le schéma est expliqué dans [](validation-conventions.md)."
  ],
  "params": {
    "value": "La valeur à valider.",
    "message": "Le message de l'exception. À défaut, un message par défaut est utilisé.",
    "strict": "Avec `true`, une chaîne n'est vide que si elle ne contient aucun caractère ; par défaut, les espaces sont retirés d'abord."
  },
  "returns": "la même valeur, typée comme une valeur appelable.",
  "throws": {
    "IllegalArgumentException": "la valeur n'est pas appelable."
  },
  "examples": [
    {
      "title": "Ce qui passe et ce qui ne passe pas",
      "body": "```javascript\nrequireFunctionLike(function () {}); // => function () {}\nrequireFunctionLike(Math.max); // => Math.max\nrequireFunctionLike(null); // throws IllegalArgumentException\nrequireFunctionLike({}); // throws IllegalArgumentException\n```"
    }
  ],
  "seeAlso": [
    {
      "name": "isFunctionLike"
    },
    {
      "name": "nonFunctionLike"
    },
    {
      "name": "requireNonFunctionLike"
    }
  ]
}
