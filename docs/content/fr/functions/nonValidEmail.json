{
  "summary": "Vérifie qu'une valeur n'est **pas** une adresse e-mail.",
  "description": [
    "`nonValidEmail(value)` est le même test que `!isValidEmail(value)`, écrit pour qu'une clause de garde se lise comme une phrase plutôt que comme une négation.",
    "L'adresse est coupée sur un unique `@` : le domaine est vérifié comme le fait `isValidDomain`, et la partie locale doit être non vide et sans espaces. C'est un contrôle de format, pas la preuve que la boîte existe.",
    "`isX` et `nonX` répondent à une question sans jamais interrompre le flot ; `requireX` et `requireNonX` rendent la valeur ou lèvent. Le schéma est expliqué dans [](validation-conventions.md)."
  ],
  "params": {
    "value": "La valeur à vérifier."
  },
  "returns": "`true` si la valeur n'est pas une adresse e-mail ; sinon `false`.",
  "throws": {},
  "examples": [
    {
      "title": "Ce qui passe et ce qui ne passe pas",
      "body": "```javascript\nnonValidEmail(\"user@@example.com\"); // => true\nnonValidEmail(\"user@\"); // => true\nnonValidEmail(\"example.com\"); // => true\nnonValidEmail(\"\"); // => true\nnonValidEmail(\"user@example.com\"); // => false\nnonValidEmail(\"first.last@sub.example.co.uk\"); // => false\n```"
    }
  ],
  "seeAlso": [
    {
      "name": "isEmail"
    },
    {
      "name": "requireValidEmail"
    }
  ]
}
