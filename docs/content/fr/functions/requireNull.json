{
  "summary": "Renvoie la valeur en tant que `null`, ou lève une exception.",
  "description": [
    "Strictement `null`. `undefined` est un cas distinct — `isNil` couvre les deux.",
    "`isX` et `nonX` répondent à une question sans jamais interrompre le flot ; `requireX` et `requireNonX` rendent la valeur ou lèvent. Le schéma est expliqué dans [](validation-conventions.md)."
  ],
  "params": {
    "value": "La valeur à valider.",
    "message": "Le message de l'exception. À défaut, un message par défaut est utilisé.",
    "strict": "Avec `true`, une chaîne n'est vide que si elle ne contient aucun caractère ; par défaut, les espaces sont retirés d'abord."
  },
  "returns": "la même valeur, typée comme `null`.",
  "throws": {
    "IllegalArgumentException": "la valeur n'est pas `null`."
  },
  "examples": [
    {
      "title": "Ce qui passe et ce qui ne passe pas",
      "body": "```javascript\nrequireNull(null); // => null\nrequireNull(undefined); // throws IllegalArgumentException\nrequireNull(0); // throws IllegalArgumentException\n```"
    }
  ],
  "seeAlso": [
    {
      "name": "isNull"
    },
    {
      "name": "nonNull"
    },
    {
      "name": "requireNonNil"
    }
  ]
}
