{
  "summary": "Renvoie la valeur en tant que une adresse e-mail, ou lève une exception.",
  "description": [
    "L'adresse est coupée sur un unique `@` : le domaine est vérifié comme le fait `isValidDomain`, et la partie locale doit être non vide et sans espaces. C'est un contrôle de format, pas la preuve que la boîte existe.",
    "`isX` et `nonX` répondent à une question sans jamais interrompre le flot ; `requireX` et `requireNonX` rendent la valeur ou lèvent. Le schéma est expliqué dans [](validation-conventions.md)."
  ],
  "params": {
    "value": "La valeur à valider.",
    "message": "Le message de l'exception. À défaut, un message par défaut est utilisé.",
    "strict": "Avec `true`, une chaîne n'est vide que si elle ne contient aucun caractère ; par défaut, les espaces sont retirés d'abord."
  },
  "returns": "la même valeur, typée comme une adresse e-mail.",
  "throws": {
    "InvalidEmailFormatException": "la valeur n'est pas une adresse e-mail."
  },
  "examples": [
    {
      "title": "Ce qui passe et ce qui ne passe pas",
      "body": "```javascript\nrequireValidEmail(\"user@example.com\"); // => \"user@example.com\"\nrequireValidEmail(\"first.last@sub.example.co.uk\"); // => \"first.last@sub.example.co.uk\"\nrequireValidEmail(\"user@@example.com\"); // throws InvalidEmailFormatException\nrequireValidEmail(\"user@\"); // throws InvalidEmailFormatException\n```"
    }
  ],
  "seeAlso": [
    {
      "name": "isEmail"
    },
    {
      "name": "nonValidEmail"
    }
  ]
}
