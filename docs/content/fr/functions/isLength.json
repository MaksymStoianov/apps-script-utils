{
  "summary": "Vérifie si une valeur est une longueur de tableau valide.",
  "description": [
    "Un entier de `0` à `Number.MAX_SAFE_INTEGER` — l'intervalle qu'une propriété `length` peut contenir.",
    "`isX` et `nonX` répondent à une question sans jamais interrompre le flot ; `requireX` et `requireNonX` rendent la valeur ou lèvent. Le schéma est expliqué dans [](validation-conventions.md)."
  ],
  "params": {
    "value": "La valeur à vérifier."
  },
  "returns": "`true` si la valeur est une longueur de tableau valide ; sinon `false`.",
  "throws": {},
  "examples": [
    {
      "title": "Ce qui passe et ce qui ne passe pas",
      "body": "```javascript\nisLength(0); // => true\nisLength(42); // => true\nisLength(Number.MAX_SAFE_INTEGER); // => true\nisLength(-1); // => false\nisLength(1.5); // => false\nisLength(\"3\"); // => false\n```"
    }
  ],
  "seeAlso": [
    {
      "name": "nonLength"
    },
    {
      "name": "requireLength"
    },
    {
      "name": "requireNonLength"
    }
  ]
}
