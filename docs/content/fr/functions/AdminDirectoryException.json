{
  "summary": "Le service Admin SDK Directory n'est pas disponible ou n'a pas été activé pour le script.",
  "description": [
    "Le service Admin SDK Directory n'est pas disponible ou n'a pas été activé pour le script.",
    "Levée par [`requireAdmin`](requireAdmin.md).",
    "Toute classe de cette hiérarchie accepte un message facultatif, une autre exception dont le message est repris, ou rien du tout. `isException` les reconnaît toutes ; un `Error` ordinaire n'en fait pas partie."
  ],
  "params": {},
  "returns": "",
  "throws": {},
  "examples": [
    {
      "title": "Lever et intercepter",
      "body": "```javascript\nthrow new AdminDirectoryException(\"something specific about this call\");\n\ntry {\n  doWork();\n} catch (error) {\n  if (error instanceof AdminDirectoryException) {\n    // handled\n  }\n}\n```"
    }
  ],
  "seeAlso": [
    {
      "name": "isException"
    },
    {
      "name": "requireException"
    }
  ]
}
