{
  "summary": "Renvoie la valeur en tant que `undefined`, ou lève une exception.",
  "description": [
    "Le test est `typeof value === \"undefined\"`, sûr même pour une variable jamais déclarée, contrairement à une comparaison directe.",
    "`isX` et `nonX` répondent à une question sans jamais interrompre le flot ; `requireX` et `requireNonX` rendent la valeur ou lèvent. Le schéma est expliqué dans [](validation-conventions.md)."
  ],
  "params": {
    "value": "La valeur à valider.",
    "message": "Le message de l'exception. À défaut, un message par défaut est utilisé.",
    "strict": "Avec `true`, une chaîne n'est vide que si elle ne contient aucun caractère ; par défaut, les espaces sont retirés d'abord."
  },
  "returns": "la même valeur, typée comme `undefined`.",
  "throws": {
    "IllegalArgumentException": "la valeur n'est pas `undefined`."
  },
  "examples": [
    {
      "title": "Ce qui passe et ce qui ne passe pas",
      "body": "```javascript\nrequireUndefined(undefined); // => undefined\nrequireUndefined(null); // throws IllegalArgumentException\nrequireUndefined(0); // throws IllegalArgumentException\n```"
    }
  ],
  "seeAlso": [
    {
      "name": "isUndefined"
    },
    {
      "name": "nonUndefined"
    },
    {
      "name": "requireNonUndefined"
    }
  ]
}
