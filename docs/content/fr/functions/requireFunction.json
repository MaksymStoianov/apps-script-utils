{
  "summary": "Renvoie la valeur en tant que une fonction, ou lève une exception.",
  "description": [
    "Déterminé par le tag interne : fonctions ordinaires, `async`, générateurs et générateurs asynchrones passent toutes. Une classe est également une fonction.",
    "`isX` et `nonX` répondent à une question sans jamais interrompre le flot ; `requireX` et `requireNonX` rendent la valeur ou lèvent. Le schéma est expliqué dans [](validation-conventions.md)."
  ],
  "params": {
    "value": "La valeur à valider.",
    "message": "Le message de l'exception. À défaut, un message par défaut est utilisé.",
    "strict": "Avec `true`, une chaîne n'est vide que si elle ne contient aucun caractère ; par défaut, les espaces sont retirés d'abord."
  },
  "returns": "la même valeur, typée comme une fonction.",
  "throws": {
    "IllegalArgumentException": "la valeur n'est pas une fonction."
  },
  "examples": [
    {
      "title": "Ce qui passe et ce qui ne passe pas",
      "body": "```javascript\nrequireFunction(function () {}); // => function () {}\nrequireFunction(async function () {}); // => async function () {}\nrequireFunction(null); // throws IllegalArgumentException\nrequireFunction(\"fn\"); // throws IllegalArgumentException\n```"
    }
  ],
  "seeAlso": [
    {
      "name": "isFunction"
    },
    {
      "name": "nonFunction"
    },
    {
      "name": "requireNonFunction"
    }
  ]
}
