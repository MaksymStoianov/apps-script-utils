{
  "summary": "Vérifie si une valeur est une `Exception`.",
  "description": [
    "Uniquement la hiérarchie propre à la bibliothèque. Un `Error` ordinaire ne compte pas, ce qui rend le test utile dans un `catch` qui doit distinguer les deux.",
    "`isX` et `nonX` répondent à une question sans jamais interrompre le flot ; `requireX` et `requireNonX` rendent la valeur ou lèvent. Le schéma est expliqué dans [](validation-conventions.md)."
  ],
  "params": {
    "value": "La valeur à vérifier."
  },
  "returns": "`true` si la valeur est une `Exception` ; sinon `false`.",
  "throws": {},
  "examples": [
    {
      "title": "Ce qui passe et ce qui ne passe pas",
      "body": "```javascript\nisException(new IllegalArgumentException()); // => true\nisException(new Error()); // => false\nisException({}); // => false\nisException(null); // => false\n```"
    }
  ],
  "seeAlso": [
    {
      "name": "nonException"
    },
    {
      "name": "requireException"
    },
    {
      "name": "requireNonException"
    }
  ]
}
