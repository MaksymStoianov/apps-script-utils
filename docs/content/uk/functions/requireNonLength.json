{
  "summary": "Повертає значення, переконавшись, що воно не є припустимою довжиною масиву.",
  "description": [
    "Ціле число від `0` до `Number.MAX_SAFE_INTEGER` — діапазон, який може містити властивість `length`.",
    "`isX` та `nonX` відповідають на питання й ніколи не переривають виконання; `requireX` і `requireNonX` повертають значення або викидають виняток. Схему розібрано в [](validation-conventions.md)."
  ],
  "params": {
    "value": "Значення, яке потрібно перевірити.",
    "message": "Повідомлення винятку. Якщо не задано, використовується типове повідомлення.",
    "strict": "За `true` рядок вважається порожнім, лише якщо в ньому зовсім немає символів; типово пробіли спершу відкидаються."
  },
  "returns": "те саме значення; після перевірки відомо, що воно не є припустимою довжиною масиву.",
  "throws": {
    "IllegalArgumentException": "значення є припустимою довжиною масиву."
  },
  "examples": [
    {
      "title": "Що проходить, а що ні",
      "body": "```javascript\nrequireNonLength(-1); // => -1\nrequireNonLength(1.5); // => 1.5\nrequireNonLength(0); // throws IllegalArgumentException\nrequireNonLength(42); // throws IllegalArgumentException\n```"
    }
  ],
  "seeAlso": [
    {
      "name": "isLength"
    },
    {
      "name": "nonLength"
    },
    {
      "name": "requireLength"
    }
  ]
}
