{
  "summary": "Очищає рядки, вибрані предикатом, не видаляючи самі рядки.",
  "description": [
    "Вміст зникає, рядки лишаються, тому позиції не зсуваються і все, що посилається на рядок за номером, далі вказує на те саме місце. Видаляє рядки `deleteRowsByConditional`.",
    "Аркуш читається один раз, а очищення групується в суміжні блоки, тому навіть розрізнена вибірка коштує небагатьох звернень до сервісу.",
    "Аркуш означає весь його діапазон даних; діапазон — лише свої клітинки, решта аркуша не читається й не чіпається. В обох випадках предикат отримує позицію на аркуші, тож рядок зберігає свій справжній номер."
  ],
  "params": {
    "target": "Аркуш, з яким працюємо, або діапазон, усередині якого працюємо: читаються лише його клітинки, і очищаються лише вони.",
    "predicate": "Отримує комірки рядка, його позицію від одиниці та — якщо задано `headerRow` — рядок із ключами за іменами стовпців. Поверніть `true` для рядків, з якими треба щось зробити.",
    "options": "`headerRow` вказує рядок з іменами стовпців. Якщо його задано, кожен рядок приходить із ключами за цими іменами, а сам рядок заголовків вибуває з кандидатів."
  },
  "returns": "скількох це торкнулося.",
  "throws": {
    "InvalidSheetException": "перший аргумент не є ні аркушем, ні діапазоном.",
    "IllegalArgumentException": "предикат не функція або позиція заголовка не додатне ціле."
  },
  "examples": [
    {
      "title": "Застосування"
    },
    {
      "title": "Усередині одного блоку",
      "body": "```javascript\nconst sheet = SpreadsheetApp.getActiveSheet();\n\n// Only B2:D100 is read, and only those cells are cleared.\nclearRowsByConditional(sheet.getRange(\"B2:D100\"), (values) => values.every((cell) => cell === \"\"));\n```"
    }
  ],
  "seeAlso": [
    {
      "name": "deleteRowsByConditional"
    },
    {
      "name": "clearColumnsByConditional"
    },
    {
      "name": "getValues"
    }
  ]
}
