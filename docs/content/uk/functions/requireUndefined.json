{
  "summary": "Повертає значення як `undefined` або викидає виняток.",
  "description": [
    "Перевіряється `typeof value === \"undefined\"` — на відміну від прямого порівняння, це безпечно навіть для неоголошеної змінної.",
    "`isX` та `nonX` відповідають на питання й ніколи не переривають виконання; `requireX` і `requireNonX` повертають значення або викидають виняток. Схему розібрано в [](validation-conventions.md)."
  ],
  "params": {
    "value": "Значення, яке потрібно перевірити.",
    "message": "Повідомлення винятку. Якщо не задано, використовується типове повідомлення.",
    "strict": "За `true` рядок вважається порожнім, лише якщо в ньому зовсім немає символів; типово пробіли спершу відкидаються."
  },
  "returns": "те саме значення, вже перевірене як `undefined`.",
  "throws": {
    "IllegalArgumentException": "значення не є значенням `undefined`."
  },
  "examples": [
    {
      "title": "Що проходить, а що ні",
      "body": "```javascript\nrequireUndefined(undefined); // => undefined\nrequireUndefined(null); // throws IllegalArgumentException\nrequireUndefined(0); // throws IllegalArgumentException\n```"
    }
  ],
  "seeAlso": [
    {
      "name": "isUndefined"
    },
    {
      "name": "nonUndefined"
    },
    {
      "name": "requireNonUndefined"
    }
  ]
}
