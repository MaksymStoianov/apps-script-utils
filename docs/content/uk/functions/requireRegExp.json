{
  "summary": "Повертає значення як регулярний вираз або викидає виняток.",
  "description": [
    "Визначається за внутрішнім тегом, а не через `instanceof`, тому розпізнається й регулярний вираз з іншого фрейму.",
    "`isX` та `nonX` відповідають на питання й ніколи не переривають виконання; `requireX` і `requireNonX` повертають значення або викидають виняток. Схему розібрано в [](validation-conventions.md)."
  ],
  "params": {
    "value": "Значення, яке потрібно перевірити.",
    "message": "Повідомлення винятку. Якщо не задано, використовується типове повідомлення.",
    "strict": "За `true` рядок вважається порожнім, лише якщо в ньому зовсім немає символів; типово пробіли спершу відкидаються."
  },
  "returns": "те саме значення, вже перевірене як регулярний вираз.",
  "throws": {
    "IllegalArgumentException": "значення не є регулярним виразом."
  },
  "examples": [
    {
      "title": "Що проходить, а що ні",
      "body": "```javascript\nrequireRegExp(/a/); // => /a/\nrequireRegExp(new RegExp(\"a\")); // => new RegExp(\"a\")\nrequireRegExp(\"/a/\"); // throws IllegalArgumentException\nrequireRegExp({}); // throws IllegalArgumentException\n```"
    }
  ],
  "seeAlso": [
    {
      "name": "isRegExp"
    },
    {
      "name": "nonRegExp"
    },
    {
      "name": "requireNonRegExp"
    }
  ]
}
