{
  "summary": "Повертає значення як логічне значення або викидає виняток.",
  "description": [
    "Підходять лише два примітиви. `new Boolean(false)` — це об'єкт, а рядки `\"true\"` і `\"false\"` лишаються рядками.",
    "`isX` та `nonX` відповідають на питання й ніколи не переривають виконання; `requireX` і `requireNonX` повертають значення або викидають виняток. Схему розібрано в [](validation-conventions.md)."
  ],
  "params": {
    "value": "Значення, яке потрібно перевірити.",
    "message": "Повідомлення винятку. Якщо не задано, використовується типове повідомлення.",
    "strict": "За `true` рядок вважається порожнім, лише якщо в ньому зовсім немає символів; типово пробіли спершу відкидаються."
  },
  "returns": "те саме значення, вже перевірене як логічне значення.",
  "throws": {
    "IllegalArgumentException": "значення не є логічним значенням."
  },
  "examples": [
    {
      "title": "Що проходить, а що ні",
      "body": "```javascript\nrequireBoolean(true); // => true\nrequireBoolean(false); // => false\nrequireBoolean(\"true\"); // throws IllegalArgumentException\nrequireBoolean(1); // throws IllegalArgumentException\n```"
    }
  ],
  "seeAlso": [
    {
      "name": "isBoolean"
    },
    {
      "name": "nonBoolean"
    },
    {
      "name": "requireNonBoolean"
    }
  ]
}
