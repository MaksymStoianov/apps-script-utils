{
  "summary": "Повертає значення як рядок або викидає виняток.",
  "description": [
    "Перевіряється `typeof value === \"string\"`, тому порожній рядок підходить, а об'єкт `String` — ні, він об'єкт.",
    "`isX` та `nonX` відповідають на питання й ніколи не переривають виконання; `requireX` і `requireNonX` повертають значення або викидають виняток. Схему розібрано в [](validation-conventions.md)."
  ],
  "params": {
    "value": "Значення, яке потрібно перевірити.",
    "message": "Повідомлення винятку. Якщо не задано, використовується типове повідомлення.",
    "strict": "За `true` рядок вважається порожнім, лише якщо в ньому зовсім немає символів; типово пробіли спершу відкидаються."
  },
  "returns": "те саме значення, вже перевірене як рядок.",
  "throws": {
    "InvalidStringException": "значення не є рядком."
  },
  "examples": [
    {
      "title": "Що проходить, а що ні",
      "body": "```javascript\nrequireString(\"\"); // => \"\"\nrequireString(\"abc\"); // => \"abc\"\nrequireString(1); // throws InvalidStringException\nrequireString(null); // throws InvalidStringException\n```"
    }
  ],
  "seeAlso": [
    {
      "name": "isString"
    },
    {
      "name": "nonString"
    },
    {
      "name": "requireNonString"
    }
  ]
}
