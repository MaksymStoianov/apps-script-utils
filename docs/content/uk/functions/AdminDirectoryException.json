{
  "summary": "Сервіс Admin SDK Directory недоступний або не увімкнений для скрипта.",
  "description": [
    "Сервіс Admin SDK Directory недоступний або не увімкнений для скрипта.",
    "Кидається функціями [`requireAdmin`](requireAdmin.md).",
    "Будь-який клас цієї ієрархії приймає необов'язкове повідомлення, інший виняток, чиє повідомлення буде перевикористано, або взагалі нічого. `isException` розпізнає їх усі; звичайний `Error` до них не належить."
  ],
  "params": {},
  "returns": "",
  "throws": {},
  "examples": [
    {
      "title": "Кинути й упіймати",
      "body": "```javascript\nthrow new AdminDirectoryException(\"something specific about this call\");\n\ntry {\n  doWork();\n} catch (error) {\n  if (error instanceof AdminDirectoryException) {\n    // handled\n  }\n}\n```"
    }
  ],
  "seeAlso": [
    {
      "name": "isException"
    },
    {
      "name": "requireException"
    }
  ]
}
