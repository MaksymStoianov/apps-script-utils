{
  "summary": "Повертає значення як об'єкт або викидає виняток.",
  "description": [
    "Перевіряється `typeof value === \"object\"` з винятком для `null`, тому масиви, дати та екземпляри класів підходять, а функція — ні. Функції приймає `isObjectLike`.",
    "`isX` та `nonX` відповідають на питання й ніколи не переривають виконання; `requireX` і `requireNonX` повертають значення або викидають виняток. Схему розібрано в [](validation-conventions.md)."
  ],
  "params": {
    "value": "Значення, яке потрібно перевірити.",
    "message": "Повідомлення винятку. Якщо не задано, використовується типове повідомлення.",
    "strict": "За `true` рядок вважається порожнім, лише якщо в ньому зовсім немає символів; типово пробіли спершу відкидаються."
  },
  "returns": "те саме значення, вже перевірене як об'єкт.",
  "throws": {
    "IllegalArgumentException": "значення не є об'єктом."
  },
  "examples": [
    {
      "title": "Що проходить, а що ні",
      "body": "```javascript\nrequireObject({}); // => {}\nrequireObject([]); // => []\nrequireObject(null); // throws IllegalArgumentException\nrequireObject(\"abc\"); // throws IllegalArgumentException\n```"
    }
  ],
  "seeAlso": [
    {
      "name": "isObject"
    },
    {
      "name": "nonObject"
    },
    {
      "name": "requireNonObject"
    }
  ]
}
