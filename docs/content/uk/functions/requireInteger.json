{
  "summary": "Повертає значення як ціле число або викидає виняток.",
  "description": [
    "Число без дробової частини. Приведення типів немає, тому рядок `\"42\"` цілим числом не є, як і `NaN`.",
    "`isX` та `nonX` відповідають на питання й ніколи не переривають виконання; `requireX` і `requireNonX` повертають значення або викидають виняток. Схему розібрано в [](validation-conventions.md)."
  ],
  "params": {
    "value": "Значення, яке потрібно перевірити.",
    "message": "Повідомлення винятку. Якщо не задано, використовується типове повідомлення.",
    "strict": "За `true` рядок вважається порожнім, лише якщо в ньому зовсім немає символів; типово пробіли спершу відкидаються."
  },
  "returns": "те саме значення, вже перевірене як ціле число.",
  "throws": {
    "IllegalArgumentException": "значення не є цілим числом."
  },
  "examples": [
    {
      "title": "Що проходить, а що ні",
      "body": "```javascript\nrequireInteger(42); // => 42\nrequireInteger(-7); // => -7\nrequireInteger(1.5); // throws IllegalArgumentException\nrequireInteger(NaN); // throws IllegalArgumentException\n```"
    }
  ],
  "seeAlso": [
    {
      "name": "isInteger"
    },
    {
      "name": "nonInteger"
    },
    {
      "name": "requireNonInteger"
    }
  ]
}
