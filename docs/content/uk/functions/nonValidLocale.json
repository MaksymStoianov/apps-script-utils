{
  "summary": "Перевіряє, що значення **не** є кодом локалі.",
  "description": [
    "`nonValidLocale(value)` — та сама перевірка, що й `!isValidLocale(value)`, записана так, щоб охоронна умова читалася як твердження, а не як заперечення.",
    "Рівно дві латинські літери в будь-якому регістрі — `\"en\"`, `\"UK\"`. Код регіону чи писемності, як у `\"en-US\"`, не проходить: це короткий код мови, а не повний тег BCP 47.",
    "`isX` та `nonX` відповідають на питання й ніколи не переривають виконання; `requireX` і `requireNonX` повертають значення або викидають виняток. Схему розібрано в [](validation-conventions.md)."
  ],
  "params": {
    "value": "Значення, що перевіряється."
  },
  "returns": "`true`, якщо значення не є кодом локалі; інакше `false`.",
  "throws": {},
  "examples": [
    {
      "title": "Що проходить, а що ні",
      "body": "```javascript\nnonValidLocale(\"en-US\"); // => true\nnonValidLocale(\"eng\"); // => true\nnonValidLocale(\"\"); // => true\nnonValidLocale(\"en\"); // => false\nnonValidLocale(\"UK\"); // => false\n```"
    }
  ],
  "seeAlso": [
    {
      "name": "isValidLocale"
    },
    {
      "name": "requireValidLocale"
    }
  ]
}
