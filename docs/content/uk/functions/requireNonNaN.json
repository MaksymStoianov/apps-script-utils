{
  "summary": "Повертає значення, переконавшись, що воно не є числом `NaN`.",
  "description": [
    "Аргумент не приводиться до числа — цим перевірка й відрізняється від глобального `isNaN`, який спершу перетворює значення, тож `isNaN(\"abc\")` і `isNaN(undefined)` дають `true`.",
    "`isX` та `nonX` відповідають на питання й ніколи не переривають виконання; `requireX` і `requireNonX` повертають значення або викидають виняток. Схему розібрано в [](validation-conventions.md)."
  ],
  "params": {
    "value": "Значення, яке потрібно перевірити.",
    "message": "Повідомлення винятку. Якщо не задано, використовується типове повідомлення.",
    "strict": "За `true` рядок вважається порожнім, лише якщо в ньому зовсім немає символів; типово пробіли спершу відкидаються."
  },
  "returns": "те саме значення; після перевірки відомо, що воно не є числом `NaN`.",
  "throws": {
    "IllegalArgumentException": "значення є числом `NaN`."
  },
  "examples": [
    {
      "title": "Що проходить, а що ні",
      "body": "```javascript\nrequireNonNaN(0); // => 0\nrequireNonNaN(\"abc\"); // => \"abc\"\nrequireNonNaN(NaN); // throws IllegalArgumentException\nrequireNonNaN(0 / 0); // throws IllegalArgumentException\n```"
    }
  ],
  "seeAlso": [
    {
      "name": "isNaN"
    },
    {
      "name": "nonNaN"
    },
    {
      "name": "requireNaN"
    }
  ]
}
