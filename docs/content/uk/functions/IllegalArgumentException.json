{
  "summary": "Аргумент виявився не того роду або поза діапазоном, який приймає функція.",
  "description": [
    "Аргумент виявився не того роду або поза діапазоном, який приймає функція. Саме його кидає більшість перевірок `requireX`.",
    "Кидається функціями [`requireArray`](requireArray.md), [`requireInteger`](requireInteger.md), [`requireNonEmpty`](requireNonEmpty.md).",
    "Будь-який клас цієї ієрархії приймає необов'язкове повідомлення, інший виняток, чиє повідомлення буде перевикористано, або взагалі нічого. `isException` розпізнає їх усі; звичайний `Error` до них не належить."
  ],
  "params": {},
  "returns": "",
  "throws": {},
  "examples": [
    {
      "title": "Кинути й упіймати",
      "body": "```javascript\nthrow new IllegalArgumentException(\"something specific about this call\");\n\ntry {\n  doWork();\n} catch (error) {\n  if (error instanceof IllegalArgumentException) {\n    // handled\n  }\n}\n```"
    }
  ],
  "seeAlso": [
    {
      "name": "isException"
    },
    {
      "name": "requireException"
    }
  ]
}
