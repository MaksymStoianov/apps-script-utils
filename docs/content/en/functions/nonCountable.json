{
  "summary": "Checks whether a value is **not** a countable number.",
  "description": [
    "`nonCountable(value)` is the same test as `!isCountable(value)`, written so that a guard clause reads as a sentence rather than as a negation.",
    "A value that can serve as a quantity, a length or a zero-based index: a whole number, not negative, and small enough that integer arithmetic on it is still exact.",
    "`isX` and `nonX` answer a question and never interrupt control flow; `requireX` and `requireNonX` hand the value back or throw. The scheme is explained in [](validation-conventions.md)."
  ],
  "params": {
    "value": "The value to check."
  },
  "returns": "`true` when the value is not a countable number; otherwise `false`.",
  "throws": {},
  "examples": [
    {
      "title": "What passes and what does not",
      "body": "```javascript\nnonCountable(-1); // => true\nnonCountable(1.5); // => true\nnonCountable(Number.MAX_SAFE_INTEGER + 2); // => true\nnonCountable(0); // => false\nnonCountable(42); // => false\n```"
    }
  ],
  "seeAlso": [
    {
      "name": "isCountable"
    },
    {
      "name": "requireCountable"
    }
  ]
}
