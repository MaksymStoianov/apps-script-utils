{
  "summary": "Checks whether a value is a relative path.",
  "description": [
    "The exact complement of `isAbsolute`: anything that does not start at a root, which includes the empty string.",
    "`isX` and `nonX` answer a question and never interrupt control flow; `requireX` and `requireNonX` hand the value back or throw. The scheme is explained in [](validation-conventions.md)."
  ],
  "params": {
    "value": "The value to check."
  },
  "returns": "`true` when the value is a relative path; otherwise `false`.",
  "throws": {},
  "examples": [
    {
      "title": "What passes and what does not",
      "body": "```javascript\nisRelative(\"docs/readme.md\"); // => true\nisRelative(\"./a\"); // => true\nisRelative(\"\"); // => true\nisRelative(\"/var/log\"); // => false\nisRelative(\"https://example.com/a\"); // => false\n```"
    }
  ],
  "seeAlso": [
    {
      "name": "nonRelative"
    },
    {
      "name": "requireRelative"
    }
  ]
}
