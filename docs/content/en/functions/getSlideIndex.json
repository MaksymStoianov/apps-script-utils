{
  "summary": "Finds the position of a slide in a presentation.",
  "description": [
    "The slides are compared by their object id, so a slide read earlier is still recognised. A slide that belongs to another presentation, or has since been removed, gives `null`."
  ],
  "params": {
    "slide": "The slide to locate.",
    "presentation": "The presentation to look in."
  },
  "returns": "the zero-based position, or `null` when the slide is not there.",
  "throws": {},
  "examples": [
    {
      "title": "Looking up",
      "body": "```javascript\nconst presentation = SlidesApp.getActivePresentation();\nconst slide = presentation.getSlides()[2];\n\ngetSlideIndex(slide, presentation); // => 2\n```"
    }
  ],
  "seeAlso": [
    {
      "name": "getSlideByIndex"
    },
    {
      "name": "isSlide"
    }
  ]
}
