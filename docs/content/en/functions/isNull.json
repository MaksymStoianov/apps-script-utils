{
  "summary": "Checks whether a value is `null`.",
  "description": [
    "Strictly `null`. `undefined` is a separate case — `isNil` covers both.",
    "`isX` and `nonX` answer a question and never interrupt control flow; `requireX` and `requireNonX` hand the value back or throw. The scheme is explained in [](validation-conventions.md)."
  ],
  "params": {
    "value": "The value to check."
  },
  "returns": "`true` when the value is `null`; otherwise `false`.",
  "throws": {},
  "examples": [
    {
      "title": "What passes and what does not",
      "body": "```javascript\nisNull(null); // => true\nisNull(undefined); // => false\nisNull(0); // => false\nisNull(\"\"); // => false\n```"
    }
  ],
  "seeAlso": [
    {
      "name": "nonNull"
    },
    {
      "name": "requireNull"
    },
    {
      "name": "requireNonNil"
    }
  ]
}
