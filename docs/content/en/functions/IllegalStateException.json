{
  "summary": "The call arrived at the wrong moment: the arguments may be perfectly valid, but the object is not in a state that allows it.",
  "description": [
    "The call arrived at the wrong moment: the arguments may be perfectly valid, but the object is not in a state that allows it.",
    "Thrown by [`requireState`](requireState.md).",
    "Every class in this hierarchy takes an optional message, another exception whose message it reuses, or nothing at all. `isException` recognises all of them; a plain `Error` is not one."
  ],
  "params": {},
  "returns": "",
  "throws": {},
  "examples": [
    {
      "title": "Throwing and catching",
      "body": "```javascript\nthrow new IllegalStateException(\"something specific about this call\");\n\ntry {\n  doWork();\n} catch (error) {\n  if (error instanceof IllegalStateException) {\n    // handled\n  }\n}\n```"
    }
  ],
  "seeAlso": [
    {
      "name": "isException"
    },
    {
      "name": "requireException"
    }
  ]
}
