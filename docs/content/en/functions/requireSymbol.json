{
  "summary": "Returns the value as a symbol, or throws.",
  "description": [
    "Registered symbols from `Symbol.for` and the well-known symbols both pass; nothing else does.",
    "`isX` and `nonX` answer a question and never interrupt control flow; `requireX` and `requireNonX` hand the value back or throw. The scheme is explained in [](validation-conventions.md)."
  ],
  "params": {
    "value": "The value to validate.",
    "message": "The message of the exception. A default is used when omitted.",
    "strict": "When `true`, a string counts as empty only if it has no characters at all; by default whitespace is trimmed first."
  },
  "returns": "the same value, typed as a symbol.",
  "throws": {
    "IllegalArgumentException": "the value is not a symbol."
  },
  "examples": [
    {
      "title": "What passes and what does not",
      "body": "```javascript\nrequireSymbol(Symbol(\"id\")); // => Symbol(\"id\")\nrequireSymbol(Symbol.iterator); // => Symbol.iterator\nrequireSymbol(\"id\"); // throws IllegalArgumentException\nrequireSymbol(null); // throws IllegalArgumentException\n```"
    }
  ],
  "seeAlso": [
    {
      "name": "isSymbol"
    },
    {
      "name": "nonSymbol"
    },
    {
      "name": "requireNonSymbol"
    }
  ]
}
