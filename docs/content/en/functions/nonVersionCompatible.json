{
  "summary": "Checks whether a version falls short of the one required.",
  "description": [
    "The negation of `isVersionCompatible`, for the guard clause that stops early when the environment is too old."
  ],
  "params": {
    "currentVersion": "The version in hand.",
    "requiredVersion": "The minimum that is acceptable."
  },
  "returns": "`true` when the current version is lower than required.",
  "throws": {},
  "examples": [
    {
      "title": "Leaving early",
      "body": "```javascript\nnonVersionCompatible(\"1.2.0\", \"1.3.0\"); // => true\n```"
    }
  ],
  "seeAlso": [
    {
      "name": "isVersionCompatible"
    },
    {
      "name": "requireVersionCompatible"
    }
  ]
}
