{
  "summary": "Returns the value as an object, or throws.",
  "description": [
    "The test is `typeof value === \"object\"` with `null` excluded, so arrays, dates and class instances all count, while a function does not. `isObjectLike` is the one that also accepts functions.",
    "`isX` and `nonX` answer a question and never interrupt control flow; `requireX` and `requireNonX` hand the value back or throw. The scheme is explained in [](validation-conventions.md)."
  ],
  "params": {
    "value": "The value to validate.",
    "message": "The message of the exception. A default is used when omitted.",
    "strict": "When `true`, a string counts as empty only if it has no characters at all; by default whitespace is trimmed first."
  },
  "returns": "the same value, typed as an object.",
  "throws": {
    "IllegalArgumentException": "the value is not an object."
  },
  "examples": [
    {
      "title": "What passes and what does not",
      "body": "```javascript\nrequireObject({}); // => {}\nrequireObject([]); // => []\nrequireObject(null); // throws IllegalArgumentException\nrequireObject(\"abc\"); // throws IllegalArgumentException\n```"
    }
  ],
  "seeAlso": [
    {
      "name": "isObject"
    },
    {
      "name": "nonObject"
    },
    {
      "name": "requireNonObject"
    }
  ]
}
