{
  "summary": "Returns the value after checking that it is not object-like.",
  "description": [
    "Everything `isObject` accepts, plus functions: the question it answers is whether properties can be read off the value at all.",
    "`isX` and `nonX` answer a question and never interrupt control flow; `requireX` and `requireNonX` hand the value back or throw. The scheme is explained in [](validation-conventions.md)."
  ],
  "params": {
    "value": "The value to validate.",
    "message": "The message of the exception. A default is used when omitted.",
    "strict": "When `true`, a string counts as empty only if it has no characters at all; by default whitespace is trimmed first."
  },
  "returns": "the same value, with object-like excluded from its type.",
  "throws": {
    "IllegalArgumentException": "the value is object-like."
  },
  "examples": [
    {
      "title": "What passes and what does not",
      "body": "```javascript\nrequireNonObjectLike(null); // => null\nrequireNonObjectLike(\"abc\"); // => \"abc\"\nrequireNonObjectLike({}); // throws IllegalArgumentException\nrequireNonObjectLike([]); // throws IllegalArgumentException\n```"
    }
  ],
  "seeAlso": [
    {
      "name": "isObjectLike"
    },
    {
      "name": "nonObjectLike"
    },
    {
      "name": "requireObjectLike"
    }
  ]
}
