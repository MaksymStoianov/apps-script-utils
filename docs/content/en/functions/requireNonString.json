{
  "summary": "Returns the value after checking that it is not a string.",
  "description": [
    "The test is `typeof value === \"string\"`, so the empty string passes and a `String` object, being an object, does not.",
    "`isX` and `nonX` answer a question and never interrupt control flow; `requireX` and `requireNonX` hand the value back or throw. The scheme is explained in [](validation-conventions.md)."
  ],
  "params": {
    "value": "The value to validate.",
    "message": "The message of the exception. A default is used when omitted.",
    "strict": "When `true`, a string counts as empty only if it has no characters at all; by default whitespace is trimmed first."
  },
  "returns": "the same value, with a string excluded from its type.",
  "throws": {
    "IllegalArgumentException": "the value is a string."
  },
  "examples": [
    {
      "title": "What passes and what does not",
      "body": "```javascript\nrequireNonString(1); // => 1\nrequireNonString(null); // => null\nrequireNonString(\"\"); // throws IllegalArgumentException\nrequireNonString(\"abc\"); // throws IllegalArgumentException\n```"
    }
  ],
  "seeAlso": [
    {
      "name": "isString"
    },
    {
      "name": "nonString"
    },
    {
      "name": "requireString"
    }
  ]
}
