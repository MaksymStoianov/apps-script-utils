{
  "summary": "Finds a slide by its position in a presentation.",
  "description": [
    "The index counts from zero. A position past the end gives `null` rather than an exception, which is what a loop over a deck that someone may be editing needs."
  ],
  "params": {
    "presentation": "The presentation to look in.",
    "index": "The zero-based position of the slide."
  },
  "returns": "the slide, or `null` when the index is past the end.",
  "throws": {},
  "examples": [
    {
      "title": "Looking up",
      "body": "```javascript\nconst presentation = SlidesApp.getActivePresentation();\n\nconst first = getSlideByIndex(presentation, 0);\n```"
    }
  ],
  "seeAlso": [
    {
      "name": "getSlideIndex"
    },
    {
      "name": "requireSlide"
    },
    {
      "name": "isValidSlideId"
    }
  ]
}
