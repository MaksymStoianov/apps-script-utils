{
  "summary": "Returns the value as a function, or throws.",
  "description": [
    "Classified by its internal tag, so ordinary, `async`, generator and async-generator functions all pass. A class is a function too.",
    "`isX` and `nonX` answer a question and never interrupt control flow; `requireX` and `requireNonX` hand the value back or throw. The scheme is explained in [](validation-conventions.md)."
  ],
  "params": {
    "value": "The value to validate.",
    "message": "The message of the exception. A default is used when omitted.",
    "strict": "When `true`, a string counts as empty only if it has no characters at all; by default whitespace is trimmed first."
  },
  "returns": "the same value, typed as a function.",
  "throws": {
    "IllegalArgumentException": "the value is not a function."
  },
  "examples": [
    {
      "title": "What passes and what does not",
      "body": "```javascript\nrequireFunction(function () {}); // => function () {}\nrequireFunction(async function () {}); // => async function () {}\nrequireFunction(null); // throws IllegalArgumentException\nrequireFunction(\"fn\"); // throws IllegalArgumentException\n```"
    }
  ],
  "seeAlso": [
    {
      "name": "isFunction"
    },
    {
      "name": "nonFunction"
    },
    {
      "name": "requireNonFunction"
    }
  ]
}
