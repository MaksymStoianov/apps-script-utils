<!-- Generated by scripts/docs/generate-topics.mjs. Edits are overwritten. Source: src/lang/string/merge.ts. -->

# MergeOptions

<link-summary>Wie `merge` eine Vorlage füllt.</link-summary>

<web-summary>MergeOptions — Wie `merge` eine Vorlage füllt. apps-script-utils, Deutsch.</web-summary>

<tldr>
<p>Modul: <code>lang/string</code> · Verfügbar seit: 1.11.0</p>
</tldr>

```typescript
interface MergeOptions
```

`onMissing` entscheidet über einen Platzhalter ohne Schlüssel: `"keep"` — die Vorgabe — lässt ihn in der Ausgabe stehen, wo er sichtbar und nachvollziehbar ist; `"empty"` setzt nichts ein, was in etwas, das ein Leser sieht, aufgeräumter wirkt.

`escape` ist eine Funktion, die auf jeden eingesetzten Wert angewendet wird — `escapeHtml`, wenn das Ergebnis in eine Seite geht.

## Beispiele

### Form

```javascript
const options = { onMissing: "empty", escape: escapeHtml };
```

## Siehe auch

- [`merge`](merge.md)
- [`escapeHtml`](escapeHtml.md)
- [](reference-base.md)

## Quellcode

[`src/lang/string/merge.ts`](https://github.com/MaksymStoianov/apps-script-utils/blob/main/src/lang/string/merge.ts)
