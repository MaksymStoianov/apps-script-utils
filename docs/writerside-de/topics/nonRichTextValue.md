<!-- Generated by scripts/docs/generate-topics.mjs. Edits are overwritten. Source: src/appsscript/sheet/nonRichTextValue.ts. -->

# nonRichTextValue

<link-summary>Prüft, ob ein Wert **nicht** ein Rich-Text-Wert ist.</link-summary>

<web-summary>nonRichTextValue() — Prüft, ob ein Wert **nicht** ein Rich-Text-Wert ist. apps-script-utils, Deutsch.</web-summary>

<tldr>
<p>Modul: <code>appsscript/sheet</code> · Verfügbar seit: 1.11.0</p>
</tldr>

```typescript
function nonRichTextValue<T>(value: T | GoogleAppsScript.Spreadsheet.RichTextValue): value is T;
```

`nonRichTextValue(value)` ist dieselbe Prüfung wie `!isRichTextValue(value)`, so geschrieben, dass eine Wächterklausel sich wie ein Satz liest und nicht wie eine Verneinung.

Ein Apps-Script-Dienstobjekt hat keinen Konstruktor, gegen den ein Skript prüfen könnte, also liest die Prüfung den Tag, den das Objekt über sich selbst meldet: `value.toString() === "RichTextValue"`. Ein einfaches Objekt mit demselben Tag besteht ebenfalls — in der Praxis ist das ein Test-Double.

`isX` und `nonX` beantworten eine Frage und unterbrechen den Ablauf nie; `requireX` und `requireNonX` geben den Wert zurück oder werfen. Das Schema erklärt [](validation-conventions.md).

## Parameter

| Parameter | Typ                                               | Beschreibung          |
| :-------- | :------------------------------------------------ | :-------------------- |
| `value`   | `T \| GoogleAppsScript.Spreadsheet.RichTextValue` | Der zu prüfende Wert. |

## Rückgabewert

`boolean` — `true`, wenn der Wert nicht ein Rich-Text-Wert ist; sonst `false`.

In TypeScript verengt die Prüfung `value` innerhalb des Zweigs auf `T`.

## Beispiele

### Was besteht und was nicht

```javascript
const value = SpreadsheetApp.newRichTextValue().setText("a").build();

nonRichTextValue({}); // => true
nonRichTextValue(null); // => true
nonRichTextValue(value); // => false
```

## Siehe auch

- [`isRichTextValue`](isRichTextValue.md)
- [`requireRichTextValue`](requireRichTextValue.md)
- [](reference-appsscript.md)

## Quellcode

[`src/appsscript/sheet/nonRichTextValue.ts`](https://github.com/MaksymStoianov/apps-script-utils/blob/main/src/appsscript/sheet/nonRichTextValue.ts)
