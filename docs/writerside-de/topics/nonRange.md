<!-- Generated by scripts/docs/generate-topics.mjs. Edits are overwritten. Source: src/appsscript/sheet/nonRange.ts. -->

# nonRange

<link-summary>Prüft, ob ein Wert **nicht** ein Bereich ist.</link-summary>

<web-summary>nonRange() — Prüft, ob ein Wert **nicht** ein Bereich ist. apps-script-utils, Deutsch.</web-summary>

<tldr>
<p>Modul: <code>appsscript/sheet</code> · Verfügbar seit: 1.3.0</p>
</tldr>

```typescript
function nonRange<T>(value: T | GoogleAppsScript.Spreadsheet.Range): value is T;
```

`nonRange(value)` ist dieselbe Prüfung wie `!isRange(value)`, so geschrieben, dass eine Wächterklausel sich wie ein Satz liest und nicht wie eine Verneinung.

Ein Apps-Script-Dienstobjekt hat keinen Konstruktor, gegen den ein Skript prüfen könnte, also liest die Prüfung den Tag, den das Objekt über sich selbst meldet: `value.toString() === "Range"`. Ein einfaches Objekt mit demselben Tag besteht ebenfalls — in der Praxis ist das ein Test-Double.

`isX` und `nonX` beantworten eine Frage und unterbrechen den Ablauf nie; `requireX` und `requireNonX` geben den Wert zurück oder werfen. Das Schema erklärt [](validation-conventions.md).

## Parameter

| Parameter | Typ                                       | Beschreibung          |
| :-------- | :---------------------------------------- | :-------------------- |
| `value`   | `T \| GoogleAppsScript.Spreadsheet.Range` | Der zu prüfende Wert. |

## Rückgabewert

`boolean` — `true`, wenn der Wert nicht ein Bereich ist; sonst `false`.

In TypeScript verengt die Prüfung `value` innerhalb des Zweigs auf `T`.

## Beispiele

### Was besteht und was nicht

```javascript
const range = SpreadsheetApp.getActiveRange();

nonRange({}); // => true
nonRange(null); // => true
nonRange(range); // => false
```

## Siehe auch

- [`isRange`](isRange.md)
- [`requireRange`](requireRange.md)
- [](reference-appsscript.md)

## Quellcode

[`src/appsscript/sheet/nonRange.ts`](https://github.com/MaksymStoianov/apps-script-utils/blob/main/src/appsscript/sheet/nonRange.ts)
