<!-- Generated by scripts/docs/generate-topics.mjs. Edits are overwritten. Source: src/net/path/types/ParsedPath.ts. -->

# ParsedPath

<link-summary>Die Teile, in die ein Pfad zerfällt.</link-summary>

<web-summary>ParsedPath — Die Teile, in die ein Pfad zerfällt. apps-script-utils, Deutsch.</web-summary>

<tldr>
<p>Modul: <code>net/path/types</code> · Verfügbar seit: 0.1.0</p>
</tldr>

```typescript
type ParsedPath =
```

`root` ist der führende Trenner oder das Schema, `dir` alles bis zum letzten Trenner, `base` der Dateiname samt Endung, `name` derselbe ohne sie, und `ext` die Endung mitsamt Punkt.

Jeder Teil ist optional, und ein Pfad, dem einer fehlt, kommt ohne diesen Schlüssel zurück statt mit einer leeren Zeichenkette: ein blanker Dateiname ergibt nur `base`, `name` und `ext`. Es ist die Form, die Nodes `path.parse` liefert — und genau das macht Code, der an beiden Orten laufen muss, austauschbar.

## Beispiele

### Form

```javascript
parse("/a/b/c.txt");
// => { root: "/", dir: "/a/b", base: "c.txt", name: "c", ext: ".txt" }

parse("readme.md");
// => { base: "readme.md", name: "readme", ext: ".md" }
```

## Siehe auch

- [`parse`](parse.md)
- [`join`](join.md)
- [`normalize`](normalize.md)
- [](reference-path.md)

## Quellcode

[`src/net/path/types/ParsedPath.ts`](https://github.com/MaksymStoianov/apps-script-utils/blob/main/src/net/path/types/ParsedPath.ts)
