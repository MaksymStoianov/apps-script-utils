<!-- Generated by scripts/docs/generate-topics.mjs. Edits are overwritten. Source: src/appsscript/sheet/nonValidSheetName.ts. -->

# nonValidSheetName

<link-summary>Prüft, ob ein Wert **nicht** ein Blattname ist.</link-summary>

<web-summary>nonValidSheetName() — Prüft, ob ein Wert **nicht** ein Blattname ist. apps-script-utils, Deutsch.</web-summary>

<tldr>
<p>Modul: <code>appsscript/sheet</code> · Verfügbar seit: 1.11.0</p>
</tldr>

```typescript
function nonValidSheetName(value: unknown): boolean;
```

`nonValidSheetName(value)` ist dieselbe Prüfung wie `!isValidSheetName(value)`, so geschrieben, dass eine Wächterklausel sich wie ein Satz liest und nicht wie eine Verneinung.

Was Google Tabellen akzeptiert: höchstens 100 Zeichen, nicht leer, keines von `\ / ? * [ ]` und nicht der reservierte Name `history` in beliebiger Schreibweise.

`isX` und `nonX` beantworten eine Frage und unterbrechen den Ablauf nie; `requireX` und `requireNonX` geben den Wert zurück oder werfen. Das Schema erklärt [](validation-conventions.md).

## Parameter

| Parameter | Typ       | Beschreibung          |
| :-------- | :-------- | :-------------------- |
| `value`   | `unknown` | Der zu prüfende Wert. |

## Rückgabewert

`boolean` — `true`, wenn der Wert nicht ein Blattname ist; sonst `false`.

## Beispiele

### Was besteht und was nicht

```javascript
nonValidSheetName("history"); // => true
nonValidSheetName("Data/2026"); // => true
nonValidSheetName(""); // => true
nonValidSheetName("Data"); // => false
nonValidSheetName("Report 2026"); // => false
```

## Siehe auch

- [`isValidSheetName`](isValidSheetName.md)
- [`requireValidSheetName`](requireValidSheetName.md)
- [](reference-appsscript.md)

## Quellcode

[`src/appsscript/sheet/nonValidSheetName.ts`](https://github.com/MaksymStoianov/apps-script-utils/blob/main/src/appsscript/sheet/nonValidSheetName.ts)
