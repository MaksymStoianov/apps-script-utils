<!-- Generated by scripts/docs/generate-topics.mjs. Edits are overwritten. Source: src/lang/base/requireFunctionLike.ts. -->

# requireFunctionLike

<link-summary>Gibt den Wert als einen aufrufbaren Wert zurück oder wirft.</link-summary>

<web-summary>requireFunctionLike() — Gibt den Wert als einen aufrufbaren Wert zurück oder wirft. apps-script-utils, Deutsch.</web-summary>

<tldr>
<p>Modul: <code>lang/base</code> · Verfügbar seit: 1.11.0</p>
</tldr>

```typescript
function requireFunctionLike( value: unknown, message: string = "Expected a function." // eslint-disable-next-line @typescript-eslint/no-unsafe-function-type ): Function;
```

Die billige Prüfung — `typeof value === "function"` — genau das, was man vor einem Aufruf braucht. `isFunction` liest dieselbe Frage strenger.

`isX` und `nonX` beantworten eine Frage und unterbrechen den Ablauf nie; `requireX` und `requireNonX` geben den Wert zurück oder werfen. Das Schema erklärt [](validation-conventions.md).

## Parameter

| Parameter                                         | Typ       | Beschreibung                                                               |
| :------------------------------------------------ | :-------- | :------------------------------------------------------------------------- |
| `value`                                           | `unknown` | Der zu validierende Wert.                                                  |
| `message` _(optional)_ `= "Expected a function."` | `string`  | Die Meldung der Ausnahme. Ohne Angabe wird eine Standardmeldung verwendet. |

## Rückgabewert

`Function` — denselben Wert, typisiert als einen aufrufbaren Wert.

## Ausnahmen

| Ausnahme                   | Bedingung                     |
| :------------------------- | :---------------------------- |
| `IllegalArgumentException` | der Wert ist nicht aufrufbar. |

## Beispiele

### Was besteht und was nicht

```javascript
requireFunctionLike(function () {}); // => function () {}
requireFunctionLike(Math.max); // => Math.max
requireFunctionLike(null); // throws IllegalArgumentException
requireFunctionLike({}); // throws IllegalArgumentException
```

## Siehe auch

- [`isFunctionLike`](isFunctionLike.md)
- [`nonFunctionLike`](nonFunctionLike.md)
- [`requireNonFunctionLike`](requireNonFunctionLike.md)
- [](reference-base.md)

## Quellcode

[`src/lang/base/requireFunctionLike.ts`](https://github.com/MaksymStoianov/apps-script-utils/blob/main/src/lang/base/requireFunctionLike.ts)
