<!-- Generated by scripts/docs/generate-topics.mjs. Edits are overwritten. Source: src/exception/RuntimeException.ts. -->

# RuntimeException

<link-summary>Die Oberklasse jeder Ausnahme, die ein laufendes Skript auslösen kann.</link-summary>

<web-summary>RuntimeException — Die Oberklasse jeder Ausnahme, die ein laufendes Skript auslösen kann. apps-script-utils, Deutsch.</web-summary>

<tldr>
<p>Modul: <code>exception</code> · Verfügbar seit: 1.0.0</p>
</tldr>

```typescript
class RuntimeException extends Exception
```

Die Oberklasse jeder Ausnahme, die ein laufendes Skript auslösen kann. Fast alle Klassen darunter erweitern sie statt `Exception` direkt.

Jede Klasse dieser Hierarchie nimmt eine optionale Meldung, eine andere Ausnahme, deren Meldung übernommen wird, oder gar nichts. `isException` erkennt sie alle; ein einfacher `Error` gehört nicht dazu.

## Beispiele

### Werfen und fangen

```javascript
throw new RuntimeException("something specific about this call");

try {
  doWork();
} catch (error) {
  if (error instanceof RuntimeException) {
    // handled
  }
}
```

## Siehe auch

- [`isException`](isException.md)
- [`requireException`](requireException.md)
- [](reference-exception.md)

## Quellcode

[`src/exception/RuntimeException.ts`](https://github.com/MaksymStoianov/apps-script-utils/blob/main/src/exception/RuntimeException.ts)
