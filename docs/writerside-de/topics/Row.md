<!-- Generated by scripts/docs/generate-topics.mjs. Edits are overwritten. Source: src/appsscript/sheet/getValues.ts. -->

# Row

<link-summary>Eine Zeile, wie `getValues` sie einem Filter oder Mapper übergibt.</link-summary>

<web-summary>Row — Eine Zeile, wie `getValues` sie einem Filter oder Mapper übergibt. apps-script-utils, Deutsch.</web-summary>

<tldr>
<p>Modul: <code>appsscript/sheet</code> · Verfügbar seit: 1.11.0</p>
</tldr>

```typescript
interface Row
```

`values` sind die Zellen von links nach rechts, `position` die einsbasierte Zeile auf dem Blatt — die Nummer, die ein Mensch am Rand sieht — und dazu die Zeile mit den Spaltennamen als Schlüsseln, wenn eine Kopfzeile benannt wurde.

Die Position zur Hand zu haben ist das, was einem Filter erlaubt, hinterher auf eine bestimmte Zeile zu zeigen oder an ihr zu wirken.

## Beispiele

### Form

```javascript
// What a filter receives when `headerRow` is set.
const row = {
  values: ["7", "ada@example.com"],
  position: 2,
  record: { id: "7", email: "ada@example.com" }
};
```

## Siehe auch

- [`getValues`](getValues.md)
- [`RowPredicate`](RowPredicate.md)
- [](reference-appsscript.md)

## Quellcode

[`src/appsscript/sheet/getValues.ts`](https://github.com/MaksymStoianov/apps-script-utils/blob/main/src/appsscript/sheet/getValues.ts)
