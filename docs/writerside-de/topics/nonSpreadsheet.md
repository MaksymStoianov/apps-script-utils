<!-- Generated by scripts/docs/generate-topics.mjs. Edits are overwritten. Source: src/appsscript/sheet/nonSpreadsheet.ts. -->

# nonSpreadsheet

<link-summary>Prüft, ob ein Wert **nicht** eine Tabelle ist.</link-summary>

<web-summary>nonSpreadsheet() — Prüft, ob ein Wert **nicht** eine Tabelle ist. apps-script-utils, Deutsch.</web-summary>

<tldr>
<p>Modul: <code>appsscript/sheet</code> · Verfügbar seit: 1.11.0</p>
</tldr>

```typescript
function nonSpreadsheet<T>(value: T | GoogleAppsScript.Spreadsheet.Spreadsheet): value is T;
```

`nonSpreadsheet(value)` ist dieselbe Prüfung wie `!isSpreadsheet(value)`, so geschrieben, dass eine Wächterklausel sich wie ein Satz liest und nicht wie eine Verneinung.

Ein Apps-Script-Dienstobjekt hat keinen Konstruktor, gegen den ein Skript prüfen könnte, also liest die Prüfung den Tag, den das Objekt über sich selbst meldet: `value.toString() === "Spreadsheet"`. Ein einfaches Objekt mit demselben Tag besteht ebenfalls — in der Praxis ist das ein Test-Double.

`isX` und `nonX` beantworten eine Frage und unterbrechen den Ablauf nie; `requireX` und `requireNonX` geben den Wert zurück oder werfen. Das Schema erklärt [](validation-conventions.md).

## Parameter

| Parameter | Typ                                             | Beschreibung          |
| :-------- | :---------------------------------------------- | :-------------------- |
| `value`   | `T \| GoogleAppsScript.Spreadsheet.Spreadsheet` | Der zu prüfende Wert. |

## Rückgabewert

`boolean` — `true`, wenn der Wert nicht eine Tabelle ist; sonst `false`.

In TypeScript verengt die Prüfung `value` innerhalb des Zweigs auf `T`.

## Beispiele

### Was besteht und was nicht

```javascript
const spreadsheet = SpreadsheetApp.getActive();

nonSpreadsheet({}); // => true
nonSpreadsheet(null); // => true
nonSpreadsheet(spreadsheet); // => false
```

## Siehe auch

- [`isSpreadsheet`](isSpreadsheet.md)
- [`requireSpreadsheet`](requireSpreadsheet.md)
- [`Spreadsheet`](https://developers.google.com/apps-script/reference/spreadsheet/spreadsheet)
- [](reference-appsscript.md)

## Quellcode

[`src/appsscript/sheet/nonSpreadsheet.ts`](https://github.com/MaksymStoianov/apps-script-utils/blob/main/src/appsscript/sheet/nonSpreadsheet.ts)
