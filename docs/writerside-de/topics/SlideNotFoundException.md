<!-- Generated by scripts/docs/generate-topics.mjs. Edits are overwritten. Source: src/exception/appsscript/slide/SlideNotFoundException.ts. -->

# SlideNotFoundException

<link-summary>Eine Folie war erwartet, der Wert ist keine, oder die gesuchte Folie gibt es in der Präsentation nicht.</link-summary>

<web-summary>SlideNotFoundException — Eine Folie war erwartet, der Wert ist keine, oder die gesuchte Folie gibt es in der Präsentation nicht. apps-script-utils, Deutsch.</web-summary>

<tldr>
<p>Modul: <code>exception/appsscript/slide</code> · Verfügbar seit: 1.5.0</p>
</tldr>

```typescript
class SlideNotFoundException extends RuntimeException
```

Eine Folie war erwartet, der Wert ist keine, oder die gesuchte Folie gibt es in der Präsentation nicht.

Wird geworfen von [`requireSlide`](requireSlide.md).

Jede Klasse dieser Hierarchie nimmt eine optionale Meldung, eine andere Ausnahme, deren Meldung übernommen wird, oder gar nichts. `isException` erkennt sie alle; ein einfacher `Error` gehört nicht dazu.

## Beispiele

### Werfen und fangen

```javascript
throw new SlideNotFoundException("something specific about this call");

try {
  doWork();
} catch (error) {
  if (error instanceof SlideNotFoundException) {
    // handled
  }
}
```

## Siehe auch

- [`isException`](isException.md)
- [`requireException`](requireException.md)
- [](reference-exception.md)

## Quellcode

[`src/exception/appsscript/slide/SlideNotFoundException.ts`](https://github.com/MaksymStoianov/apps-script-utils/blob/main/src/exception/appsscript/slide/SlideNotFoundException.ts)
