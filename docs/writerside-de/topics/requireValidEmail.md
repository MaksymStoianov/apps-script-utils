<!-- Generated by scripts/docs/generate-topics.mjs. Edits are overwritten. Source: src/lang/string/requireValidEmail.ts. -->

# requireValidEmail

<link-summary>Gibt den Wert als eine E-Mail-Adresse zurück oder wirft.</link-summary>

<web-summary>requireValidEmail() — Gibt den Wert als eine E-Mail-Adresse zurück oder wirft. apps-script-utils, Deutsch.</web-summary>

<tldr>
<p>Modul: <code>lang/string</code> · Verfügbar seit: 1.0.0</p>
</tldr>

```typescript
function requireValidEmail(email: string | null | undefined, message?: string): string;
```

Die Adresse wird an genau einem `@` geteilt: die Domain wird geprüft wie von `isValidDomain`, der lokale Teil muss nicht leer und frei von Leerzeichen sein. Eine Formatprüfung, kein Beleg dafür, dass das Postfach existiert.

`isX` und `nonX` beantworten eine Frage und unterbrechen den Ablauf nie; `requireX` und `requireNonX` geben den Wert zurück oder werfen. Das Schema erklärt [](validation-conventions.md).

## Parameter

| Parameter              | Typ                           | Beschreibung                                                                      |
| :--------------------- | :---------------------------- | :-------------------------------------------------------------------------------- |
| `email`                | `string \| null \| undefined` | The string value representing an email to validate. Can be `null` or `undefined`. |
| `message` _(optional)_ | `string`                      | Die Meldung der Ausnahme. Ohne Angabe wird eine Standardmeldung verwendet.        |

## Rückgabewert

`string` — denselben Wert, typisiert als eine E-Mail-Adresse.

## Ausnahmen

| Ausnahme                      | Bedingung                               |
| :---------------------------- | :-------------------------------------- |
| `InvalidEmailFormatException` | der Wert ist nicht eine E-Mail-Adresse. |

## Beispiele

### Was besteht und was nicht

```javascript
requireValidEmail("user@example.com"); // => "user@example.com"
requireValidEmail("first.last@sub.example.co.uk"); // => "first.last@sub.example.co.uk"
requireValidEmail("user@@example.com"); // throws InvalidEmailFormatException
requireValidEmail("user@"); // throws InvalidEmailFormatException
```

## Siehe auch

- [`isEmail`](isEmail.md)
- [`nonValidEmail`](nonValidEmail.md)
- [](reference-base.md)

## Quellcode

[`src/lang/string/requireValidEmail.ts`](https://github.com/MaksymStoianov/apps-script-utils/blob/main/src/lang/string/requireValidEmail.ts)
