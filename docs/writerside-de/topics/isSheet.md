<!-- Generated by scripts/docs/generate-topics.mjs. Edits are overwritten. Source: src/appsscript/sheet/isSheet.ts. -->

# isSheet

<link-summary>Prüft, ob ein Wert ein Blatt ist.</link-summary>

<web-summary>isSheet() — Prüft, ob ein Wert ein Blatt ist. apps-script-utils, Deutsch.</web-summary>

<tldr>
<p>Modul: <code>appsscript/sheet</code> · Verfügbar seit: 1.0.0</p>
</tldr>

```typescript
function isSheet(value: unknown): value is GoogleAppsScript.Spreadsheet.Sheet;
```

Ein Apps-Script-Dienstobjekt hat keinen Konstruktor, gegen den ein Skript prüfen könnte, also liest die Prüfung den Tag, den das Objekt über sich selbst meldet: `value.toString() === "Sheet"`. Ein einfaches Objekt mit demselben Tag besteht ebenfalls — in der Praxis ist das ein Test-Double.

`isX` und `nonX` beantworten eine Frage und unterbrechen den Ablauf nie; `requireX` und `requireNonX` geben den Wert zurück oder werfen. Das Schema erklärt [](validation-conventions.md).

## Parameter

| Parameter | Typ       | Beschreibung          |
| :-------- | :-------- | :-------------------- |
| `value`   | `unknown` | Der zu prüfende Wert. |

## Rückgabewert

`boolean` — `true`, wenn der Wert ein Blatt ist; sonst `false`.

In TypeScript verengt die Prüfung `value` innerhalb des Zweigs auf `GoogleAppsScript.Spreadsheet.Sheet`.

## Beispiele

### Was besteht und was nicht

```javascript
const sheet = SpreadsheetApp.getActiveSheet();

isSheet(sheet); // => true
isSheet({}); // => false
isSheet(null); // => false
```

## Siehe auch

- [`nonSheet`](nonSheet.md)
- [`requireSheet`](requireSheet.md)
- [](reference-appsscript.md)

## Quellcode

[`src/appsscript/sheet/isSheet.ts`](https://github.com/MaksymStoianov/apps-script-utils/blob/main/src/appsscript/sheet/isSheet.ts)
