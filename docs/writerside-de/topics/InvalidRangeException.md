<!-- Generated by scripts/docs/generate-topics.mjs. Edits are overwritten. Source: src/exception/appsscript/sheet/InvalidRangeException.ts. -->

# InvalidRangeException

<link-summary>Ein Bereich war erwartet, der Wert ist keiner.</link-summary>

<web-summary>InvalidRangeException — Ein Bereich war erwartet, der Wert ist keiner. apps-script-utils, Deutsch.</web-summary>

<tldr>
<p>Modul: <code>exception/appsscript/sheet</code> · Verfügbar seit: 1.5.0</p>
</tldr>

```typescript
class InvalidRangeException extends RuntimeException
```

Ein Bereich war erwartet, der Wert ist keiner.

Wird geworfen von [`requireRange`](requireRange.md).

Jede Klasse dieser Hierarchie nimmt eine optionale Meldung, eine andere Ausnahme, deren Meldung übernommen wird, oder gar nichts. `isException` erkennt sie alle; ein einfacher `Error` gehört nicht dazu.

## Beispiele

### Werfen und fangen

```javascript
throw new InvalidRangeException("something specific about this call");

try {
  doWork();
} catch (error) {
  if (error instanceof InvalidRangeException) {
    // handled
  }
}
```

## Siehe auch

- [`isException`](isException.md)
- [`requireException`](requireException.md)
- [Class Range](https://developers.google.com/apps-script/reference/spreadsheet/range)
- [](reference-exception.md)

## Quellcode

[`src/exception/appsscript/sheet/InvalidRangeException.ts`](https://github.com/MaksymStoianov/apps-script-utils/blob/main/src/exception/appsscript/sheet/InvalidRangeException.ts)
