<!-- Generated by scripts/docs/generate-topics.mjs. Edits are overwritten. Source: src/appsscript/sheet/nonCellGridRange.ts. -->

# nonCellGridRange

<link-summary>Prüft, ob ein Wert **nicht** ein Einzelzellbereich ist.</link-summary>

<web-summary>nonCellGridRange() — Prüft, ob ein Wert **nicht** ein Einzelzellbereich ist. apps-script-utils, Deutsch.</web-summary>

<tldr>
<p>Modul: <code>appsscript/sheet</code> · Verfügbar seit: 1.11.0</p>
</tldr>

```typescript
function nonCellGridRange(gridRange: GridRange): boolean;
```

`nonCellGridRange(value)` ist dieselbe Prüfung wie `!isCellGridRange(value)`, so geschrieben, dass eine Wächterklausel sich wie ein Satz liest und nicht wie eine Verneinung.

Eine Zeile und eine Spalte, gemessen an den Grenzen und nicht daran, wie der Bereich geschrieben wurde: die Endindizes gehören nicht dazu, eine einzelne Zelle ist also `endRowIndex - startRowIndex === 1` und ebenso bei den Spalten. Ein Bereich mit weggelassener Grenze ist in diese Richtung offen und nie eine einzelne Zelle.

`isX` und `nonX` beantworten eine Frage und unterbrechen den Ablauf nie; `requireX` und `requireNonX` geben den Wert zurück oder werfen. Das Schema erklärt [](validation-conventions.md).

## Parameter

| Parameter   | Typ         | Beschreibung             |
| :---------- | :---------- | :----------------------- |
| `gridRange` | `GridRange` | The grid range to check. |

## Rückgabewert

`boolean` — `true`, wenn der Wert nicht ein Einzelzellbereich ist; sonst `false`.

## Beispiele

### Was besteht und was nicht

```javascript
nonCellGridRange({ startRowIndex: 0, endRowIndex: 2, startColumnIndex: 0, endColumnIndex: 1 }); // => true
nonCellGridRange({}); // => true
nonCellGridRange(null); // => true
nonCellGridRange({ startRowIndex: 0, endRowIndex: 1, startColumnIndex: 0, endColumnIndex: 1 }); // => false
```

## Siehe auch

- [`isCellGridRange`](isCellGridRange.md)
- [`requireCellGridRange`](requireCellGridRange.md)
- [](reference-appsscript.md)

## Quellcode

[`src/appsscript/sheet/nonCellGridRange.ts`](https://github.com/MaksymStoianov/apps-script-utils/blob/main/src/appsscript/sheet/nonCellGridRange.ts)
