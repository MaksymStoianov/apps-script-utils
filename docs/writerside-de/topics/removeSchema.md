<!-- Generated by scripts/docs/generate-topics.mjs. Edits are overwritten. Source: src/appsscript/sheet/removeSchema.ts. -->

# removeSchema

<link-summary>Entfernt das auf einem Blatt hinterlegte Schema.</link-summary>

<web-summary>removeSchema() — Entfernt das auf einem Blatt hinterlegte Schema. apps-script-utils, Deutsch.</web-summary>

<tldr>
<p>Modul: <code>appsscript/sheet</code> · Verfügbar seit: 1.11.0</p>
</tldr>

```typescript
function removeSchema(
  sheet: GoogleAppsScript.Spreadsheet.Sheet,
  options: RemoveSchemaOptions | null | undefined = {}
): boolean;
```

Die hinterlegte Beschreibung verschwindet; Daten und Kopfzeile bleiben genau so, wie sie sind. Ein Blatt ohne gespeichertes Schema bleibt unangetastet und der Aufruf meldet `false`.

`validation: true` löscht zusätzlich die Datenprüfung der beschriebenen Spalten. Sie ist absichtlich standardmäßig aus: Apps Script bietet keine Möglichkeit, eine vom Schema angelegte Regel von einer handgemachten zu unterscheiden, also trifft das Löschen beide.

## Parameter

| Parameter                     | Typ                                        | Beschreibung                                                            |
| :---------------------------- | :----------------------------------------- | :---------------------------------------------------------------------- |
| `sheet`                       | `GoogleAppsScript.Spreadsheet.Sheet`       | Das Blatt, auf dem gearbeitet wird.                                     |
| `options` _(optional)_ `= {}` | `RemoveSchemaOptions \| null \| undefined` | `validation` löscht zusätzlich die vom Schema implizierte Datenprüfung. |

## Rückgabewert

`boolean` — `true`, wenn ein Schema entfernt wurde.

## Ausnahmen

| Ausnahme                | Bedingung                          |
| :---------------------- | :--------------------------------- |
| `InvalidSheetException` | das erste Argument ist kein Blatt. |

## Beispiele

### Ein Schema entfernen

```javascript
const sheet = SpreadsheetApp.getActiveSheet();

removeSchema(sheet); // => true when one was there
removeSchema(sheet, { validation: true }); // also clears the rules it implied
```

## Siehe auch

- [`getSchema`](getSchema.md)
- [`insertSchema`](insertSchema.md)
- [](reference-appsscript.md)

## Quellcode

[`src/appsscript/sheet/removeSchema.ts`](https://github.com/MaksymStoianov/apps-script-utils/blob/main/src/appsscript/sheet/removeSchema.ts)
