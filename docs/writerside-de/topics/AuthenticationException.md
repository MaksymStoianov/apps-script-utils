<!-- Generated by scripts/docs/generate-topics.mjs. Edits are overwritten. Source: src/exception/net/AuthenticationException.ts. -->

# AuthenticationException

<link-summary>Die Authentifizierung schlug fehl: wer aufruft, ließ sich nicht feststellen.</link-summary>

<web-summary>AuthenticationException — Die Authentifizierung schlug fehl: wer aufruft, ließ sich nicht feststellen. apps-script-utils, Deutsch.</web-summary>

<tldr>
<p>Modul: <code>exception/net</code> · Verfügbar seit: 1.5.0</p>
</tldr>

```typescript
class AuthenticationException extends RuntimeException
```

Die Authentifizierung schlug fehl: wer aufruft, ließ sich nicht feststellen.

Jede Klasse dieser Hierarchie nimmt eine optionale Meldung, eine andere Ausnahme, deren Meldung übernommen wird, oder gar nichts. `isException` erkennt sie alle; ein einfacher `Error` gehört nicht dazu.

## Beispiele

### Werfen und fangen

```javascript
throw new AuthenticationException("something specific about this call");

try {
  doWork();
} catch (error) {
  if (error instanceof AuthenticationException) {
    // handled
  }
}
```

## Siehe auch

- [`isException`](isException.md)
- [`requireException`](requireException.md)
- [](reference-exception.md)

## Quellcode

[`src/exception/net/AuthenticationException.ts`](https://github.com/MaksymStoianov/apps-script-utils/blob/main/src/exception/net/AuthenticationException.ts)
