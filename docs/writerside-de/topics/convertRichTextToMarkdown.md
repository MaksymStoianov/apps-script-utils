<!-- Generated by scripts/docs/generate-topics.mjs. Edits are overwritten. Source: src/appsscript/slide/convertRichTextToMarkdown.ts. -->

# convertRichTextToMarkdown

<link-summary>Führt gestaltete Textabschnitte wieder in Markdown zurück.</link-summary>

<web-summary>convertRichTextToMarkdown() — Führt gestaltete Textabschnitte wieder in Markdown zurück. apps-script-utils, Deutsch.</web-summary>

<tldr>
<p>Modul: <code>appsscript/slide</code> · Verfügbar seit: 2.0.0</p>
</tldr>

```typescript
function convertRichTextToMarkdown(runs: RichTextRun[], theme: Theme = {}): string;
```

Die Umkehrung von [`convertMarkdownToRichText`](convertMarkdownToRichText.md): Fett, Kursiv und Durchgestrichen werden wieder zu ihren Zeichen, ein Abschnitt mit Link zu `[Text](url)` und ein Abschnitt in der Code-Schrift des Themes zu einem Code-Span.

Ein Code-Abschnitt trägt kein eigenes Merkmal — er wird an seiner Schrift erkannt —, also übergeben Sie in beide Richtungen dasselbe Theme, dann bleibt das Paar verlustfrei. Die Abschnitte werden in der gegebenen Reihenfolge hintereinander geschrieben, ohne Nachbarn gleichen Stils zusammenzufassen; tief verschachtelte Auszeichnung kann daher anders geschrieben zurückkommen und sich dennoch gleich lesen.

## Parameter

| Parameter                   | Typ             | Beschreibung                                                                                  |
| :-------------------------- | :-------------- | :-------------------------------------------------------------------------------------------- |
| `runs`                      | `RichTextRun[]` | Die umzuwandelnden Abschnitte. Ein leeres Array ergibt eine leere Zeichenkette.               |
| `theme` _(optional)_ `= {}` | `Theme`         | Das Theme, mit dem die Abschnitte gestaltet wurden. Gelesen wird daraus nur die Code-Schrift. |

## Rückgabewert

`string` — das Markdown, das die Abschnitte darstellt.

## Ausnahmen

| Ausnahme                   | Bedingung                                                                                            |
| :------------------------- | :--------------------------------------------------------------------------------------------------- |
| `IllegalArgumentException` | kein Argument übergeben wurde, der Wert kein Array ist oder ein Element kein Abschnitt mit Text ist. |

## Beispiele

### Zurück nach Markdown

```javascript
const runs = convertMarkdownToRichText("**bold** and `code`");

convertRichTextToMarkdown(runs); // => "**bold** and `code`"
```

### Ein Link behält die Gestaltung seines Textes

```javascript
convertRichTextToMarkdown([
  { text: "site", style: { bold: true, link: { url: "https://example.com" } } }
]); // => "[**site**](https://example.com)"
```

## Siehe auch

- [`convertMarkdownToRichText`](convertMarkdownToRichText.md)
- [`convertRichTextToHtml`](convertRichTextToHtml.md)
- [](reference-appsscript.md)

## Quellcode

[`src/appsscript/slide/convertRichTextToMarkdown.ts`](https://github.com/MaksymStoianov/apps-script-utils/blob/main/src/appsscript/slide/convertRichTextToMarkdown.ts)
