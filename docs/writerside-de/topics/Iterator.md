<!-- Generated by scripts/docs/generate-topics.mjs. Edits are overwritten. Source: src/lang/Iterator.ts. -->

# Iterator

<link-summary>Ein Zeiger über eine Folge, der auch rückwärts gehen kann.</link-summary>

<web-summary>Iterator — Ein Zeiger über eine Folge, der auch rückwärts gehen kann. apps-script-utils, Deutsch.</web-summary>

<tldr>
<p>Modul: <code>lang</code> · Verfügbar seit: 1.0.0</p>
</tldr>

```typescript
interface Iterator<T>
```

Pflicht sind nur `next` und `hasNext`. Der Rest — `previous`, `hasPrevious`, `index`, `moveTo`, `size`, `toStart`, `toEnd` — ist optional, sodass eine nur vorwärts lesbare Quelle die Schnittstelle erfüllt, ohne mehr vorzugeben, als sie kann.

Es ist die Form eines seitenweisen Lesers: Zeilen, die ein Dienst in Blöcken liefert, sehen für den verbrauchenden Code wie eine Folge aus.

## Beispiele

### Form

```javascript
while (iterator.hasNext()) {
  const value = iterator.next();
}
```

## Siehe auch

- [`Class`](Class.md)
- [`first`](first.md)
- [`last`](last.md)
- [](reference-abstracts.md)

## Quellcode

[`src/lang/Iterator.ts`](https://github.com/MaksymStoianov/apps-script-utils/blob/main/src/lang/Iterator.ts)
