<!-- Generated by scripts/docs/generate-topics.mjs. Edits are overwritten. Source: src/exception/AuthorizationException.ts. -->

# AuthorizationException

<link-summary>Der Aufrufer ist bekannt, darf dies aber nicht.</link-summary>

<web-summary>AuthorizationException — Der Aufrufer ist bekannt, darf dies aber nicht. apps-script-utils, Deutsch.</web-summary>

<tldr>
<p>Modul: <code>exception</code> · Verfügbar seit: 1.11.0</p>
</tldr>

```typescript
class AuthorizationException extends RuntimeException
```

Der Aufrufer ist bekannt, darf dies aber nicht. Anders als `AuthenticationException`, die meldet, dass der Aufrufer gar nicht bestimmt werden konnte.

Jede Klasse dieser Hierarchie nimmt eine optionale Meldung, eine andere Ausnahme, deren Meldung übernommen wird, oder gar nichts. `isException` erkennt sie alle; ein einfacher `Error` gehört nicht dazu.

## Beispiele

### Werfen und fangen

```javascript
throw new AuthorizationException("something specific about this call");

try {
  doWork();
} catch (error) {
  if (error instanceof AuthorizationException) {
    // handled
  }
}
```

## Siehe auch

- [`isException`](isException.md)
- [`requireException`](requireException.md)
- [](reference-exception.md)

## Quellcode

[`src/exception/AuthorizationException.ts`](https://github.com/MaksymStoianov/apps-script-utils/blob/main/src/exception/AuthorizationException.ts)
