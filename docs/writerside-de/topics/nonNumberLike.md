<!-- Generated by scripts/docs/generate-topics.mjs. Edits are overwritten. Source: src/lang/base/nonNumberLike.ts. -->

# nonNumberLike

<link-summary>Prüft, ob ein Wert **nicht** in eine Zahl umwandelbar ist.</link-summary>

<web-summary>nonNumberLike() — Prüft, ob ein Wert **nicht** in eine Zahl umwandelbar ist. apps-script-utils, Deutsch.</web-summary>

<tldr>
<p>Modul: <code>lang/base</code> · Verfügbar seit: 1.11.0</p>
</tldr>

```typescript
function nonNumberLike(value: unknown): boolean;
```

`nonNumberLike(value)` ist dieselbe Prüfung wie `!isNumberLike(value)`, so geschrieben, dass eine Wächterklausel sich wie ein Satz liest und nicht wie eine Verneinung.

Eine endliche Zahl oder eine Zeichenkette, die sich in eine solche umwandelt. `NaN`, die Unendlichkeiten und die leere Zeichenkette sind ausgeschlossen — das unterscheidet die Prüfung von einem bloßen `Number(value)`.

`isX` und `nonX` beantworten eine Frage und unterbrechen den Ablauf nie; `requireX` und `requireNonX` geben den Wert zurück oder werfen. Das Schema erklärt [](validation-conventions.md).

## Parameter

| Parameter | Typ       | Beschreibung          |
| :-------- | :-------- | :-------------------- |
| `value`   | `unknown` | Der zu prüfende Wert. |

## Rückgabewert

`boolean` — `true`, wenn der Wert nicht in eine Zahl umwandelbar ist; sonst `false`.

## Beispiele

### Was besteht und was nicht

```javascript
nonNumberLike(NaN); // => true
nonNumberLike(Infinity); // => true
nonNumberLike(""); // => true
nonNumberLike(null); // => true
nonNumberLike(1); // => false
nonNumberLike("1.5"); // => false
nonNumberLike(" 42 "); // => false
```

## Siehe auch

- [`isNumberLike`](isNumberLike.md)
- [`requireNumberLike`](requireNumberLike.md)
- [`requireNonNumberLike`](requireNonNumberLike.md)
- [](reference-base.md)

## Quellcode

[`src/lang/base/nonNumberLike.ts`](https://github.com/MaksymStoianov/apps-script-utils/blob/main/src/lang/base/nonNumberLike.ts)
