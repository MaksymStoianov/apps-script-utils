<!-- Generated by scripts/docs/generate-topics.mjs. Edits are overwritten. Source: src/net/path/isRelative.ts. -->

# isRelative

<link-summary>Prüft, ob ein Wert ein relativer Pfad ist.</link-summary>

<web-summary>isRelative() — Prüft, ob ein Wert ein relativer Pfad ist. apps-script-utils, Deutsch.</web-summary>

<tldr>
<p>Modul: <code>net/path</code></p>
</tldr>

```typescript
function isRelative(path: string): path is string;
```

Das exakte Gegenstück zu `isAbsolute`: alles, was nicht an einer Wurzel beginnt, die leere Zeichenkette eingeschlossen.

`isX` und `nonX` beantworten eine Frage und unterbrechen den Ablauf nie; `requireX` und `requireNonX` geben den Wert zurück oder werfen. Das Schema erklärt [](validation-conventions.md).

## Parameter

| Parameter | Typ      | Beschreibung              |
| :-------- | :------- | :------------------------ |
| `path`    | `string` | The string path to check. |

## Rückgabewert

`boolean` — `true`, wenn der Wert ein relativer Pfad ist; sonst `false`.

In TypeScript verengt die Prüfung `path` innerhalb des Zweigs auf `string`.

## Beispiele

### Was besteht und was nicht

```javascript
isRelative("docs/readme.md"); // => true
isRelative("./a"); // => true
isRelative(""); // => true
isRelative("/var/log"); // => false
isRelative("https://example.com/a"); // => false
```

## Siehe auch

- [`nonRelative`](nonRelative.md)
- [`requireRelative`](requireRelative.md)
- [](reference-path.md)

## Quellcode

[`src/net/path/isRelative.ts`](https://github.com/MaksymStoianov/apps-script-utils/blob/main/src/net/path/isRelative.ts)
