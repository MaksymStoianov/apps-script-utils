<!-- Generated by scripts/docs/generate-topics.mjs. Edits are overwritten. Source: src/json/parseJson.ts. -->

# parseJson

<link-summary>Liest JSON und versucht es bei striktem Fehlschlag mit einem toleranten Leser erneut.</link-summary>

<web-summary>parseJson() — Liest JSON und versucht es bei striktem Fehlschlag mit einem toleranten Leser erneut. apps-script-utils, Deutsch.</web-summary>

<tldr>
<p>Modul: <code>json</code> · Verfügbar seit: 1.0.0 · <b>Veraltet</b></p>
</tldr>

> **Veraltet.** This function is deprecated and will be removed in future versions.

```typescript
function parseJson(value: string): object;
```

Zuerst läuft `JSON.parse`. Weigert es sich, wird der Text nach Regeln erneut zerlegt, die zulassen, was handgeschriebene Konfiguration meist enthält: Schlüssel ohne Anführungszeichen und Zeichenketten in einfachen Anführungszeichen.

Text, mit dem keiner der beiden Leser etwas anfangen kann, wirft. Gedacht für von Hand getippte Konfiguration, nicht für die Antwort eines Dienstes — dort ist `JSON.parse` mit seinem Fehler klarer.

## Parameter

| Parameter | Typ      | Beschreibung                                  |
| :-------- | :------- | :-------------------------------------------- |
| `value`   | `string` | Der zu lesende Text. Er darf nicht leer sein. |

## Rückgabewert

`object` — den gelesenen Wert.

## Ausnahmen

| Ausnahme               | Bedingung                                      |
| :--------------------- | :--------------------------------------------- |
| `EmptyStringException` | der Wert ist keine Zeichenkette oder ist leer. |
| `SyntaxError`          | keiner der beiden Leser kann den Text lesen.   |

## Beispiele

### Lesen

```javascript
parseJson('{"a":1,"b":[1,2]}'); // => {"a":1,"b":[1,2]}
parseJson("{a: 'x', b: 1}"); // => {"a":"x","b":1}
```

## Siehe auch

- [`stringifyJson`](stringifyJson.md)
- [](reference-base.md)

## Quellcode

[`src/json/parseJson.ts`](https://github.com/MaksymStoianov/apps-script-utils/blob/main/src/json/parseJson.ts)
