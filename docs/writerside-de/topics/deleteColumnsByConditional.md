<!-- Generated by scripts/docs/generate-topics.mjs. Edits are overwritten. Source: src/appsscript/sheet/deleteColumnsByConditional.ts. -->

# deleteColumnsByConditional

<link-summary>Löscht die vom Prädikat gewählten Spalten.</link-summary>

<web-summary>deleteColumnsByConditional() — Löscht die vom Prädikat gewählten Spalten. apps-script-utils, Deutsch.</web-summary>

<tldr>
<p>Modul: <code>appsscript/sheet</code> · Verfügbar seit: 1.11.0</p>
</tldr>

```typescript
function deleteColumnsByConditional(
  sheet: GoogleAppsScript.Spreadsheet.Sheet,
  predicate: ColumnPredicate,
  options: ColumnConditionalOptions | null | undefined = {}
): number;
```

Beurteilt wird am gelesenen Blatt, gelöscht vom rechten Rand nach innen in zusammenhängenden Blöcken, damit verschobene Positionen nicht die falsche Spalte mitnehmen.

Die Spalten sind dann fort: `clearColumnsByConditional` leert sie nur.

## Parameter

| Parameter                     | Typ                                             | Beschreibung                                                                                                                                                                                          |
| :---------------------------- | :---------------------------------------------- | :---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `sheet`                       | `GoogleAppsScript.Spreadsheet.Sheet`            | Das Blatt, auf dem gearbeitet wird.                                                                                                                                                                   |
| `predicate`                   | `ColumnPredicate`                               | Bekommt die Zellen der Spalte, ihre einsbasierte Position und — wenn `headerColumn` gesetzt ist — die Spalte mit den Zeilennamen als Schlüsseln. `true` für die Spalten, auf die gewirkt werden soll. |
| `options` _(optional)_ `= {}` | `ColumnConditionalOptions \| null \| undefined` | `headerColumn` benennt die Spalte mit den Zeilennamen und nimmt diese Spalte aus den Kandidaten heraus.                                                                                               |

## Rückgabewert

`number` — wie viele betroffen waren.

## Ausnahmen

| Ausnahme                   | Bedingung                                                                        |
| :------------------------- | :------------------------------------------------------------------------------- |
| `InvalidSheetException`    | das erste Argument ist kein Blatt.                                               |
| `IllegalArgumentException` | das Prädikat ist keine Funktion oder die Kopfposition keine positive ganze Zahl. |

## Beispiele

### Im Einsatz

```javascript
const sheet = SpreadsheetApp.getActiveSheet();

deleteColumnsByConditional(sheet, (values, position, column) => column.internal === true, {
  headerColumn: 1
});
```

## Siehe auch

- [`clearColumnsByConditional`](clearColumnsByConditional.md)
- [`deleteRowsByConditional`](deleteRowsByConditional.md)
- [](reference-appsscript.md)

## Quellcode

[`src/appsscript/sheet/deleteColumnsByConditional.ts`](https://github.com/MaksymStoianov/apps-script-utils/blob/main/src/appsscript/sheet/deleteColumnsByConditional.ts)
