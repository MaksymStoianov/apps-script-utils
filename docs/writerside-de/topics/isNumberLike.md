<!-- Generated by scripts/docs/generate-topics.mjs. Edits are overwritten. Source: src/lang/base/isNumberLike.ts. -->

# isNumberLike

<link-summary>Prüft, ob ein Wert in eine Zahl umwandelbar ist.</link-summary>

<web-summary>isNumberLike() — Prüft, ob ein Wert in eine Zahl umwandelbar ist. apps-script-utils, Deutsch.</web-summary>

<tldr>
<p>Modul: <code>lang/base</code> · Verfügbar seit: 1.0.0</p>
</tldr>

```typescript
function isNumberLike(value: unknown): boolean;
```

Eine endliche Zahl oder eine Zeichenkette, die sich in eine solche umwandelt. `NaN`, die Unendlichkeiten und die leere Zeichenkette sind ausgeschlossen — das unterscheidet die Prüfung von einem bloßen `Number(value)`.

`isX` und `nonX` beantworten eine Frage und unterbrechen den Ablauf nie; `requireX` und `requireNonX` geben den Wert zurück oder werfen. Das Schema erklärt [](validation-conventions.md).

## Parameter

| Parameter | Typ       | Beschreibung          |
| :-------- | :-------- | :-------------------- |
| `value`   | `unknown` | Der zu prüfende Wert. |

## Rückgabewert

`boolean` — `true`, wenn der Wert in eine Zahl umwandelbar ist; sonst `false`.

## Beispiele

### Was besteht und was nicht

```javascript
isNumberLike(1); // => true
isNumberLike("1.5"); // => true
isNumberLike(" 42 "); // => true
isNumberLike(NaN); // => false
isNumberLike(Infinity); // => false
isNumberLike(""); // => false
isNumberLike(null); // => false
```

## Siehe auch

- [`nonNumberLike`](nonNumberLike.md)
- [`requireNumberLike`](requireNumberLike.md)
- [`requireNonNumberLike`](requireNonNumberLike.md)
- [](reference-base.md)

## Quellcode

[`src/lang/base/isNumberLike.ts`](https://github.com/MaksymStoianov/apps-script-utils/blob/main/src/lang/base/isNumberLike.ts)
