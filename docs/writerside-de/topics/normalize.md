<!-- Generated by scripts/docs/generate-topics.mjs. Edits are overwritten. Source: src/net/path/normalize.ts. -->

# normalize

<link-summary>Löst die Segmente `.` und `..` eines Pfades auf.</link-summary>

<web-summary>normalize() — Löst die Segmente `.` und `..` eines Pfades auf. apps-script-utils, Deutsch.</web-summary>

<tldr>
<p>Modul: <code>net/path</code></p>
</tldr>

```typescript
function normalize(path: string): string;
```

Ein `.` entfällt, ein `..` entfernt das Segment davor, wiederholte Trenner fallen zusammen, ein abschließender Trenner wird entfernt.

Das Ergebnis beginnt immer an der Wurzel: `normalize("a/b")` ergibt `"/a/b"`. Soll ein relativer Pfad relativ bleiben, nehmen Sie `join`.

## Parameter

| Parameter | Typ      | Beschreibung                 |
| :-------- | :------- | :--------------------------- |
| `path`    | `string` | Der zu normalisierende Pfad. |

## Rückgabewert

`string` — den normalisierten Pfad.

## Beispiele

### Normalisieren

```javascript
normalize("a/./b/../c"); // => "/a/c"
normalize("/a/b/../c/"); // => "/a/c"
```

## Siehe auch

- [`join`](join.md)
- [`parse`](parse.md)
- [](reference-path.md)

## Quellcode

[`src/net/path/normalize.ts`](https://github.com/MaksymStoianov/apps-script-utils/blob/main/src/net/path/normalize.ts)
