<!-- Generated by scripts/docs/generate-topics.mjs. Edits are overwritten. Source: src/appsscript/sheet/appendRow.ts. -->

# appendRow

<link-summary>Fügt eine Zeile unter der letzten Zeile mit Daten an.</link-summary>

<web-summary>appendRow() — Fügt eine Zeile unter der letzten Zeile mit Daten an. apps-script-utils, Deutsch.</web-summary>

<tldr>
<p>Modul: <code>appsscript/sheet</code> · Verfügbar seit: 1.0.0</p>
</tldr>

```typescript
function appendRow(
  target: GoogleAppsScript.Spreadsheet.Sheet | GoogleAppsScript.Spreadsheet.Range,
  values: unknown,
  options: AppendRowsOptions | null | undefined = {}
): GoogleAppsScript.Spreadsheet.Sheet;
```

Ein Blatt wird ganz betrachtet und die Zeile ab Spalte 1 geschrieben; ein Bereich wird nur in sich betrachtet und die Zeile auf seinen Spalten geschrieben, unter der letzten Zeile des Bereichs mit Daten.

Es ist `appendRows` mit einer Zeile: dieselben Prüfungen, dasselbe Wachsen des Blatts, wenn das Ergebnis nicht passt, und dieselbe Regel, dass eine Zelle mit `=` zur Formel wird.

## Parameter

| Parameter                     | Typ                                                                        | Beschreibung                                                                       |
| :---------------------------- | :------------------------------------------------------------------------- | :--------------------------------------------------------------------------------- |
| `target`                      | `GoogleAppsScript.Spreadsheet.Sheet \| GoogleAppsScript.Spreadsheet.Range` | Das Blatt, an das angefügt wird, oder der Bereich, innerhalb dessen angefügt wird. |
| `values`                      | `unknown`                                                                  | Eine einzelne Zeile: ein flaches Array, ein Wert je Spalte.                        |
| `options` _(optional)_ `= {}` | `AppendRowsOptions \| null \| undefined`                                   | `afterFrozenRows` beginnt unter den fixierten Zeilen, wenn die Daten davor enden.  |

## Rückgabewert

`GoogleAppsScript.Spreadsheet.Sheet` — das Blatt, sodass Aufrufe verkettet werden können.

## Ausnahmen

| Ausnahme                   | Bedingung                                                |
| :------------------------- | :------------------------------------------------------- |
| `IllegalArgumentException` | kein Argument übergeben wurde.                           |
| `InvalidSheetException`    | das erste Argument weder ein Blatt noch ein Bereich ist. |
| `TypeError`                | die Werte kein flaches Array sind.                       |

## Beispiele

### An ein Blatt anfügen

```javascript
const sheet = SpreadsheetApp.getActiveSheet();

appendRow(sheet, ["Ada", "ada@example.com", "=TODAY()"]);
```

### Innerhalb eines Bereichs anfügen

```javascript
const sheet = SpreadsheetApp.getActiveSheet();

appendRow(sheet.getRange("B1:D10"), ["Ada", "ada@example.com"]);
```

## Siehe auch

- [`appendRows`](appendRows.md)
- [`prependRow`](prependRow.md)
- [](reference-appsscript.md)

## Quellcode

[`src/appsscript/sheet/appendRow.ts`](https://github.com/MaksymStoianov/apps-script-utils/blob/main/src/appsscript/sheet/appendRow.ts)
