<!-- Generated by scripts/docs/generate-topics.mjs. Edits are overwritten. Source: src/appsscript/sheet/appendRow.ts. -->

# appendRow

<link-summary>Fügt eine Zeile unter die bereits vorhandenen Daten eines Blattes ein.</link-summary>

<web-summary>appendRow() — Fügt eine Zeile unter die bereits vorhandenen Daten eines Blattes ein. apps-script-utils, Deutsch.</web-summary>

<tldr>
<p>Modul: <code>appsscript/sheet</code> · Verfügbar seit: 1.0.0</p>
</tldr>

```typescript
function appendRow(
  sheet: GoogleAppsScript.Spreadsheet.Sheet,
  values: unknown,
  options: AppendRowsOptions | null | undefined = {}
): GoogleAppsScript.Spreadsheet.Sheet;
```

`appendRows` für eine einzelne Zeile, wenn nur eine geschrieben wird. Die Werte kommen als flaches Array von Zellen statt als Matrix.

Eine Zelle, deren Text mit `=` beginnt, wird als Formel geschrieben, nicht als Text — dieselbe Regel wie im Editor.

## Parameter

| Parameter                     | Typ                                      | Beschreibung                                                                              |
| :---------------------------- | :--------------------------------------- | :---------------------------------------------------------------------------------------- |
| `sheet`                       | `GoogleAppsScript.Spreadsheet.Sheet`     | Das Blatt, in das geschrieben wird.                                                       |
| `values`                      | `unknown`                                | Die Zellen der einen Zeile.                                                               |
| `options` _(optional)_ `= {}` | `AppendRowsOptions \| null \| undefined` | `afterFrozenRows` setzt die neuen Zeilen direkt unter die fixierten statt ganz nach oben. |

## Rückgabewert

`GoogleAppsScript.Spreadsheet.Sheet` — dasselbe Blatt, damit sich Aufrufe verketten lassen.

## Ausnahmen

| Ausnahme                | Bedingung                                             |
| :---------------------- | :---------------------------------------------------- |
| `InvalidSheetException` | das erste Argument ist kein Blatt.                    |
| `TypeError`             | die Werte sind keine Matrix mit gleich langen Zeilen. |

## Beispiele

### Schreiben

```javascript
const sheet = SpreadsheetApp.getActiveSheet();

appendRow(sheet, ["Ada", "ada@example.com", "=TODAY()"]);
```

## Siehe auch

- [`appendRows`](appendRows.md)
- [`prependRow`](prependRow.md)
- [](reference-appsscript.md)

## Quellcode

[`src/appsscript/sheet/appendRow.ts`](https://github.com/MaksymStoianov/apps-script-utils/blob/main/src/appsscript/sheet/appendRow.ts)
