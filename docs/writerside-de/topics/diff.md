<!-- Generated by scripts/docs/generate-topics.mjs. Edits are overwritten. Source: src/time/diff.ts. -->

# diff

<link-summary>Gibt den Abstand zweier Daten in einer gewählten Einheit zurück.</link-summary>

<web-summary>diff() — Gibt den Abstand zweier Daten in einer gewählten Einheit zurück. apps-script-utils, Deutsch.</web-summary>

<tldr>
<p>Modul: <code>time</code> · Verfügbar seit: 1.11.0</p>
</tldr>

```typescript
function diff(
  left: Date,
  right: Date,
  unit: TimeUnit = "millisecond",
  float: boolean = false
): number;
```

Das Ergebnis ist das erste Datum minus das zweite, also negativ, wenn das zweite später liegt. Standardmäßig werden ganze Einheiten gemeldet; mit `float` kommt der Bruchteil hinzu.

Millisekunden, Sekunden, Minuten und Stunden haben feste Längen und werden exakt geteilt. Tage, Monate und Jahre zählen als ganze vergangene Einheiten — zwei Stunden über Mitternacht ergeben deshalb `0` Tage und nicht `1`.

## Parameter

| Parameter                             | Typ        | Beschreibung                                                                         |
| :------------------------------------ | :--------- | :----------------------------------------------------------------------------------- |
| `left`                                | `Date`     | Das Datum, von dem aus gemessen wird.                                                |
| `right`                               | `Date`     | Das Datum, das davon abgezogen wird.                                                 |
| `unit` _(optional)_ `= "millisecond"` | `TimeUnit` | `"millisecond"`, `"second"`, `"minute"`, `"hour"`, `"day"`, `"month"` oder `"year"`. |
| `float` _(optional)_ `= false`        | `boolean`  | Den Bruchteil behalten statt abzuschneiden. Nur bei Einheiten fester Länge.          |

## Rückgabewert

`number` — den Abstand, negativ wenn das zweite Datum später liegt.

## Ausnahmen

| Ausnahme                   | Bedingung                                                             |
| :------------------------- | :-------------------------------------------------------------------- |
| `IllegalArgumentException` | ein Argument ist kein gültiges `Date` oder die Einheit ist unbekannt. |

## Beispiele

### Einen Abstand messen

```javascript
diff(new Date(2026, 0, 31), new Date(2026, 0, 1), "day"); // => 30
diff(new Date(2026, 0, 1), new Date(2026, 0, 1, 12), "hour"); // => -12
diff(new Date(2026, 0, 2, 1), new Date(2026, 0, 1, 23), "day"); // => 0
```

## Siehe auch

- [`offset`](offset.md)
- [`getDaysInMonth`](getDaysInMonth.md)
- [](reference-base.md)

## Quellcode

[`src/time/diff.ts`](https://github.com/MaksymStoianov/apps-script-utils/blob/main/src/time/diff.ts)
