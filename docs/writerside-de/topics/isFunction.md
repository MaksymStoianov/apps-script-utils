<!-- Generated by scripts/docs/generate-topics.mjs. Edits are overwritten. Source: src/lang/base/isFunction.ts. -->

# isFunction

<link-summary>Prüft, ob ein Wert eine Funktion ist.</link-summary>

<web-summary>isFunction() — Prüft, ob ein Wert eine Funktion ist. apps-script-utils, Deutsch.</web-summary>

<tldr>
<p>Modul: <code>lang/base</code> · Verfügbar seit: 1.0.0</p>
</tldr>

```typescript
function isFunction(value: unknown): value is Function;
```

Anhand des internen Tags bestimmt: gewöhnliche, `async`-, Generator- und Async-Generator-Funktionen bestehen alle. Eine Klasse ist ebenfalls eine Funktion.

`isX` und `nonX` beantworten eine Frage und unterbrechen den Ablauf nie; `requireX` und `requireNonX` geben den Wert zurück oder werfen. Das Schema erklärt [](validation-conventions.md).

## Parameter

| Parameter | Typ       | Beschreibung          |
| :-------- | :-------- | :-------------------- |
| `value`   | `unknown` | Der zu prüfende Wert. |

## Rückgabewert

`boolean` — `true`, wenn der Wert eine Funktion ist; sonst `false`.

In TypeScript verengt die Prüfung `value` innerhalb des Zweigs auf `Function`.

## Beispiele

### Was besteht und was nicht

```javascript
isFunction(function () {}); // => true
isFunction(async function () {}); // => true
isFunction(Math.max); // => true
isFunction(null); // => false
isFunction("fn"); // => false
isFunction({}); // => false
```

## Siehe auch

- [`nonFunction`](nonFunction.md)
- [`requireFunction`](requireFunction.md)
- [`requireNonFunction`](requireNonFunction.md)
- [](reference-base.md)

## Quellcode

[`src/lang/base/isFunction.ts`](https://github.com/MaksymStoianov/apps-script-utils/blob/main/src/lang/base/isFunction.ts)
