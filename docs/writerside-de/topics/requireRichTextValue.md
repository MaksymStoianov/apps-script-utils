<!-- Generated by scripts/docs/generate-topics.mjs. Edits are overwritten. Source: src/appsscript/sheet/requireRichTextValue.ts. -->

# requireRichTextValue

<link-summary>Gibt den Wert als einen Rich-Text-Wert zurück oder wirft.</link-summary>

<web-summary>requireRichTextValue() — Gibt den Wert als einen Rich-Text-Wert zurück oder wirft. apps-script-utils, Deutsch.</web-summary>

<tldr>
<p>Modul: <code>appsscript/sheet</code> · Verfügbar seit: 1.11.0</p>
</tldr>

```typescript
function requireRichTextValue(
  value: unknown,
  message: string = "Expected a RichTextValue object."
): GoogleAppsScript.Spreadsheet.RichTextValue;
```

Ein Apps-Script-Dienstobjekt hat keinen Konstruktor, gegen den ein Skript prüfen könnte, also liest die Prüfung den Tag, den das Objekt über sich selbst meldet: `value.toString() === "RichTextValue"`. Ein einfaches Objekt mit demselben Tag besteht ebenfalls — in der Praxis ist das ein Test-Double.

`isX` und `nonX` beantworten eine Frage und unterbrechen den Ablauf nie; `requireX` und `requireNonX` geben den Wert zurück oder werfen. Das Schema erklärt [](validation-conventions.md).

## Parameter

| Parameter                                                     | Typ       | Beschreibung                                                               |
| :------------------------------------------------------------ | :-------- | :------------------------------------------------------------------------- |
| `value`                                                       | `unknown` | Der zu validierende Wert.                                                  |
| `message` _(optional)_ `= "Expected a RichTextValue object."` | `string`  | Die Meldung der Ausnahme. Ohne Angabe wird eine Standardmeldung verwendet. |

## Rückgabewert

`GoogleAppsScript.Spreadsheet.RichTextValue` — denselben Wert, typisiert als einen Rich-Text-Wert.

## Ausnahmen

| Ausnahme                   | Bedingung                              |
| :------------------------- | :------------------------------------- |
| `IllegalArgumentException` | der Wert ist nicht ein Rich-Text-Wert. |

## Beispiele

### Was besteht und was nicht

```javascript
const value = SpreadsheetApp.newRichTextValue().setText("a").build();

requireRichTextValue(value); // => value
requireRichTextValue({}); // throws IllegalArgumentException
requireRichTextValue(null); // throws IllegalArgumentException
```

## Siehe auch

- [`isRichTextValue`](isRichTextValue.md)
- [`nonRichTextValue`](nonRichTextValue.md)
- [](reference-appsscript.md)

## Quellcode

[`src/appsscript/sheet/requireRichTextValue.ts`](https://github.com/MaksymStoianov/apps-script-utils/blob/main/src/appsscript/sheet/requireRichTextValue.ts)
