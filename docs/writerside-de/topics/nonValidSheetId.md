<!-- Generated by scripts/docs/generate-topics.mjs. Edits are overwritten. Source: src/appsscript/sheet/nonValidSheetId.ts. -->

# nonValidSheetId

<link-summary>Prüft, ob ein Wert **nicht** eine Blatt-Id ist.</link-summary>

<web-summary>nonValidSheetId() — Prüft, ob ein Wert **nicht** eine Blatt-Id ist. apps-script-utils, Deutsch.</web-summary>

<tldr>
<p>Modul: <code>appsscript/sheet</code> · Verfügbar seit: 1.11.0</p>
</tldr>

```typescript
function nonValidSheetId(value: unknown): boolean;
```

`nonValidSheetId(value)` ist dieselbe Prüfung wie `!isValidSheetId(value)`, so geschrieben, dass eine Wächterklausel sich wie ein Satz liest und nicht wie eine Verneinung.

Apps Script nummeriert Blätter ab null, eine Blatt-Id ist also eine nicht negative sichere Ganzzahl — dieselbe Regel wie bei `isCountable`. Das erste Blatt einer Tabelle hat die Id `0`.

`isX` und `nonX` beantworten eine Frage und unterbrechen den Ablauf nie; `requireX` und `requireNonX` geben den Wert zurück oder werfen. Das Schema erklärt [](validation-conventions.md).

## Parameter

| Parameter | Typ       | Beschreibung          |
| :-------- | :-------- | :-------------------- |
| `value`   | `unknown` | Der zu prüfende Wert. |

## Rückgabewert

`boolean` — `true`, wenn der Wert nicht eine Blatt-Id ist; sonst `false`.

## Beispiele

### Was besteht und was nicht

```javascript
nonValidSheetId(-1); // => true
nonValidSheetId(1.5); // => true
nonValidSheetId("0"); // => true
nonValidSheetId(0); // => false
nonValidSheetId(1234567890); // => false
```

## Siehe auch

- [`isValidSheetId`](isValidSheetId.md)
- [`requireValidSheetId`](requireValidSheetId.md)
- [](reference-appsscript.md)

## Quellcode

[`src/appsscript/sheet/nonValidSheetId.ts`](https://github.com/MaksymStoianov/apps-script-utils/blob/main/src/appsscript/sheet/nonValidSheetId.ts)
