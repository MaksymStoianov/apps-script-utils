<!-- Generated by scripts/docs/generate-topics.mjs. Edits are overwritten. Source: src/appsscript/sheet/requireValidSheetName.ts. -->

# requireValidSheetName

<link-summary>Gibt den Wert als einen Blattnamen zurück oder wirft.</link-summary>

<web-summary>requireValidSheetName() — Gibt den Wert als einen Blattnamen zurück oder wirft. apps-script-utils, Deutsch.</web-summary>

<tldr>
<p>Modul: <code>appsscript/sheet</code> · Verfügbar seit: 1.11.0</p>
</tldr>

```typescript
function requireValidSheetName(
  value: unknown,
  message: string = "Expected a valid sheet name."
): string;
```

Was Google Tabellen akzeptiert: höchstens 100 Zeichen, nicht leer, keines von `\ / ? * [ ]` und nicht der reservierte Name `history` in beliebiger Schreibweise.

`isX` und `nonX` beantworten eine Frage und unterbrechen den Ablauf nie; `requireX` und `requireNonX` geben den Wert zurück oder werfen. Das Schema erklärt [](validation-conventions.md).

## Parameter

| Parameter                                                 | Typ       | Beschreibung                                                               |
| :-------------------------------------------------------- | :-------- | :------------------------------------------------------------------------- |
| `value`                                                   | `unknown` | Der zu validierende Wert.                                                  |
| `message` _(optional)_ `= "Expected a valid sheet name."` | `string`  | Die Meldung der Ausnahme. Ohne Angabe wird eine Standardmeldung verwendet. |

## Rückgabewert

`string` — denselben Wert, typisiert als einen Blattnamen.

## Ausnahmen

| Ausnahme                   | Bedingung                         |
| :------------------------- | :-------------------------------- |
| `IllegalArgumentException` | der Wert ist nicht ein Blattname. |

## Beispiele

### Was besteht und was nicht

```javascript
requireValidSheetName("Data"); // => "Data"
requireValidSheetName("Report 2026"); // => "Report 2026"
requireValidSheetName("history"); // throws IllegalArgumentException
requireValidSheetName("Data/2026"); // throws IllegalArgumentException
```

## Siehe auch

- [`isValidSheetName`](isValidSheetName.md)
- [`nonValidSheetName`](nonValidSheetName.md)
- [](reference-appsscript.md)

## Quellcode

[`src/appsscript/sheet/requireValidSheetName.ts`](https://github.com/MaksymStoianov/apps-script-utils/blob/main/src/appsscript/sheet/requireValidSheetName.ts)
