<!-- Generated by scripts/docs/generate-topics.mjs. Edits are overwritten. Source: src/appsscript/sheet/getValues.ts. -->

# GetValuesConfig

<link-summary>Wie `getValues` ein Blatt liest.</link-summary>

<web-summary>GetValuesConfig — Wie `getValues` ein Blatt liest. apps-script-utils, Deutsch.</web-summary>

<tldr>
<p>Modul: <code>appsscript/sheet</code> · Verfügbar seit: 1.11.0</p>
</tldr>

```typescript
interface GetValuesConfig
```

`headerRow` benennt die Zeile der Spaltennamen und versieht jede Zeile mit diesen Schlüsseln. `display` liest die Werte so, wie ein Mensch sie sieht, statt wie sie gespeichert sind. `filter` behält Zeilen, `offset` und `limit` blättern durch das Behaltene, `mapper` bestimmt, was daraus wird.

Die Reihenfolge steht fest: erst filtern, dann überspringen, dann begrenzen, dann abbilden — ein Limit zählt also passende Zeilen, nicht gelesene.

## Beispiele

### Form

```javascript
const config = {
  headerRow: 1,
  filter: (row) => row.record.status === "active",
  limit: 100
};
```

## Siehe auch

- [`getValues`](getValues.md)
- [`Row`](Row.md)
- [](reference-appsscript.md)

## Quellcode

[`src/appsscript/sheet/getValues.ts`](https://github.com/MaksymStoianov/apps-script-utils/blob/main/src/appsscript/sheet/getValues.ts)
