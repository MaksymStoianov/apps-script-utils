<!-- Generated by scripts/docs/generate-topics.mjs. Edits are overwritten. Source: src/appsscript/slide/isSlide.ts. -->

# isSlide

<link-summary>Prüft, ob ein Wert eine Folie ist.</link-summary>

<web-summary>isSlide() — Prüft, ob ein Wert eine Folie ist. apps-script-utils, Deutsch.</web-summary>

<tldr>
<p>Modul: <code>appsscript/slide</code> · Verfügbar seit: 1.5.0</p>
</tldr>

```typescript
function isSlide(value: unknown): value is GoogleAppsScript.Slides.Slide;
```

Ein Apps-Script-Dienstobjekt hat keinen Konstruktor, gegen den ein Skript prüfen könnte, also sucht die Prüfung die Methoden, die den Typ ausmachen — `getObjectId`, `getPageElementById`. Ein Stub, der sie umsetzt, besteht, und genau das macht Code mit dieser Prüfung außerhalb der Laufzeit testbar.

`isX` und `nonX` beantworten eine Frage und unterbrechen den Ablauf nie; `requireX` und `requireNonX` geben den Wert zurück oder werfen. Das Schema erklärt [](validation-conventions.md).

## Parameter

| Parameter | Typ       | Beschreibung          |
| :-------- | :-------- | :-------------------- |
| `value`   | `unknown` | Der zu prüfende Wert. |

## Rückgabewert

`boolean` — `true`, wenn der Wert eine Folie ist; sonst `false`.

In TypeScript verengt die Prüfung `value` innerhalb des Zweigs auf `GoogleAppsScript.Slides.Slide`.

## Beispiele

### Was besteht und was nicht

```javascript
const slide = SlidesApp.getActivePresentation().getSlides()[0];

isSlide(slide); // => true
isSlide({}); // => false
isSlide(null); // => false
```

## Siehe auch

- [`nonSlide`](nonSlide.md)
- [`requireSlide`](requireSlide.md)
- [Class Slide](https://developers.google.com/apps-script/reference/slides/slide)
- [](reference-appsscript.md)

## Quellcode

[`src/appsscript/slide/isSlide.ts`](https://github.com/MaksymStoianov/apps-script-utils/blob/main/src/appsscript/slide/isSlide.ts)
